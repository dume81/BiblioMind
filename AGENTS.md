# 비블리오마인드(BiblioMind) — 프로젝트·코딩 규칙

> 2026-08-28 저장소 통합(스냅샷 이식 — `docs/DECISIONS.md`)으로 구 bibliomind(문서)·GraphRAG_1st(코드)의 규칙 두 벌을 이 한 벌로 병합했다. **AGENTS.md(Codex용)와 동일 내용 — 개정 시 양쪽을 함께 갱신한다.**
> 절충: 이 규칙은 속도보다 신중함에 기울어 있다. 사소한 작업에는 판단력을 사용한다.

# 1부 — 프로젝트 작업 규칙

## 프로덕트: 비블리오마인드(BiblioMind)

- 이 프로젝트의 제품명은 **비블리오마인드(BiblioMind)** — Biblio(지식 집합소) + Mind(LLM의 지적 체계). 사용자가 "비블리오마인드" 또는 "BiblioMind"라고 하면 이 작업물·완성물을 가리킨다. (2026-08-21 명명, DECISIONS.md)

이 프로젝트는 **문서 기반 개발(docs-first)** 방식을 따른다. 모든 기획·설계 결정은 `docs/`에 기록되어 있으며, 문서가 코드보다 우선하는 기준점이다.

## 사용자 프로필과 협업 방식

- 사용자는 **SAP FI 컨설턴트 경험이 있는 프로그래밍 비전문가**다. 비유(집짓기·제품 제조)로 요청하면 기술 요구로 번역해 확인하고, 전문용어는 풀어서 설명한다. ERP 개념(프로세스, 마스터데이터, 결재 흐름)으로 설명하면 잘 통한다.
- **호칭: "이사님"** (2026-08-21 사용자 지정. "사장님" 아님)
- **실질적 요청·질의마다 각 분야·직무 전문가(Workflow 에이전트 패널)를 투입**해 분석·검증 후 진행한다.
- 역할 분담은 `docs/COLLABORATION.md`를 따른다. 사용자가 직접 해야 하는 액션(계정·키 발급·결제·공개 push)은 "사용자 액션 필요"로 명시해 구분한다.

## 최상위 기준: PROCESS.md

- 전체 작업 흐름은 `docs/PROCESS.md`에 정의된 Phase 0~6과 하위 작업 순서를 따른다.
- **모든 세션 시작 시** `docs/PROCESS.md`의 "현재 위치"를 확인하고, 그 Phase의 규칙과 산출물 범위 안에서만 작업한다.
- 각 Phase는 산출물 완성 + 사용자 승인(게이트)을 받아야 넘어간다. 여러 Phase를 한 턴에 진행하지 않는다.
- Phase가 바뀌거나 하위 작업이 완료되면 `docs/PROCESS.md`의 "현재 위치"를 갱신한다.

## 개발 시 필수 참조 규칙

- **구현을 시작하기 전에** 반드시 다음 문서를 읽고 그 범위 안에서 작업한다:
  - `docs/PRD.md` — 무엇을, 왜 만드는가 (기능 범위, 사용자 시나리오, 안 만들 것)
  - `docs/TECH-SPEC.md` — 어떻게 만드는가 (스택, 데이터 모델, 아키텍처, 폴더 구조)
  - `docs/ROADMAP.md` — 지금 무엇을 만들 차례인가 (수직 슬라이스 순서, 진행 상태)
- 문서에 없는 기능을 임의로 추가하지 않는다. 필요해 보이면 먼저 사용자에게 제안하고, 승인되면 문서를 갱신한 뒤 구현한다.
- PRD의 "안 만들 것(Non-Goals)" 목록에 있는 것은 사용자가 명시적으로 요청해도 문서 변경을 먼저 확인한다.

## 문서-코드 동기화 규칙

- 구현 중 문서와 다르게 만들 수밖에 없는 상황이 생기면, **코드를 바꾸기 전에** 이유를 설명하고 사용자 승인 후 문서를 먼저 갱신한다.
- 중요한 기술 결정(라이브러리 선택, 스키마 변경, 외부 서비스 도입)은 `docs/DECISIONS.md`에 날짜·결정·근거·대안을 한 항목으로 기록한다.
- 슬라이스 하나가 완료되면 `docs/ROADMAP.md`의 해당 항목 상태를 갱신한다.

## 구현 사이클 규칙

- 한 번에 로드맵의 **슬라이스 하나만** 진행한다: 구현 → 실제 실행으로 검증 → 테스트 작성 → 커밋.
- 정보가 부족하면 추측으로 채우지 말고 질문한다.
- 매 응답 끝에 "현재 단계 / 다음 액션"을 한 줄로 표시한다.

## 저장소 구성 (2026-08-28 통합 반영)

- **코드 = 이 저장소 루트** (npm workspaces 모노레포: `shared`·`pipeline`·`mcp-server`·`visualization-3d`). **기획·설계 문서 = `docs/`**, 판정 보조 도구 = `tools/`. (구 표기 "GraphRAG_1st/"·"bibliomind/docs/"는 통합 전 워크스페이스 배치의 역사 기록이다.)
- **.mcp.json 유효 범위**: 이 저장소를 프로젝트 루트로 열 때만 적용. 다른 위치에서 열면 `npm run setup`이 출력하는 절대경로 등록 명령 사용.
- **GSD 방법론(.planning) 미사용**: 프로세스 정본은 `docs/PROCESS.md`. `/gsd-*` 스킬은 사용자 명시 호출 시에만(2026-08-21 스킬 조정 A안 — DECISIONS.md).
- **최우선 규칙**은 `~/.claude/CLAUDE.md`(사용자 전역)에 있다 — *"요청·지시·질의의 핵심과 맥락을 분석하여 관련 탑티어 전문가를 투입하라."* **오너 상시 규칙 8조**는 오너 워크스페이스 루트 `CLAUDE.md` 말미가 정본이며 그 제1조가 위 전역 규칙의 시행 세칙이다(2026-08-22 채택). 로드되지 않은 환경이면 `docs/OPERATING-RULES-7.md`를 참조한다.

# 2부 — 코딩 규칙 (Karpathy-inspired 채택본, 2026-08-21 오너 승인)

## 1. 구현 전에 생각하라 (Think Before Coding)

**가정하지 말 것. 혼란을 숨기지 말 것. 트레이드오프를 드러낼 것.**

- 가정은 명시적으로 선언한다. 불확실하면 질문한다(추측 금지).
- 해석이 여러 개면 제시한다 — 조용히 하나를 고르지 않는다.
- 더 단순한 접근이 있으면 말한다. 필요하면 반대 의견을 낸다.
- 무언가 불명확하면 멈추고, 무엇이 혼란스러운지 이름 붙여 질문한다.
- 이 프로젝트에서는: 사소하지 않은 불확실성·결정 사항은 게이트 보고에 취합해 오너 확인을 받는다.

## 2. 단순성 우선 (Simplicity First)

**문제를 푸는 최소한의 코드. 투기적인 것은 없다.**

- 요청받은 것 이상의 기능 금지. 문서(PRD·TECH-SPEC)에 없는 기능은 임의 추가 금지.
- 1회용 코드에 추상화 금지. 요청받지 않은 "유연성·설정 가능성" 금지.
- 불가능한 시나리오에 대한 에러 처리 금지.
- 자기 점검: "시니어 엔지니어가 과하다고 말할까?" 그렇다면 단순화한다.

## 3. 외과수술식 수정 (Surgical Changes)

**바꿔야 할 것만 건드린다. 치우는 것은 내가 어지른 것만.**

- 인접 코드·주석·포맷을 "개선"하지 않는다. 고장 나지 않은 것을 리팩터링하지 않는다.
- 기존 스타일을 따른다 — 내 취향과 달라도.
- 무관한 죽은 코드를 발견하면 언급만 하고 삭제하지 않는다.
- 내 변경이 만든 고아(사용처 없어진 import·변수·함수)는 제거한다. 기존 죽은 코드는 지시 없이 제거하지 않는다.
- 판정 기준: **변경된 모든 라인이 요청으로 직접 소급되는가.** (실례: 모노레포 전환 시 기존 시각화 테스트 148케이스 무수정 통과가 완료 판정이었다)

## 4. 목표 주도 실행 (Goal-Driven Execution)

**검증 가능한 완료 조건을 정의하고, 확인될 때까지 반복한다.**

- 과업을 검증 가능한 목표로 변환한다: "검증 추가" → "잘못된 입력 테스트를 쓰고 통과시킨다" / "버그 수정" → "재현 테스트를 쓰고 통과시킨다" / "리팩터링" → "전후 테스트 통과 확인".
- 다단계 작업은 단계별 `→ 확인:` 계획을 먼저 세운다.
- 이 프로젝트의 완료 조건 문법은 ROADMAP의 "실행해서 확인 가능한 것" 열이 정본이다.

## 저장소 컨벤션

- **문서가 코드보다 먼저**: 설계 변경은 오너 승인 → 문서 갱신 → 코드 순서. (정본: `docs/` — PRD·TECH-SPEC·ROADMAP·DECISIONS)
- **언어·스타일**: Node.js ≥22.12, ESM(`"type": "module"`) + JSDoc 타입 주석. **TypeScript 금지.** 의존성 최소주의 — 신규 의존성은 문서 승인 후에만.
- **명령**: `npm run dev:all`(시각화) / `npm test`(전 워크스페이스) / `npm run lint` / `npm run setup`(멱등) / `npm run mcp:smoke`(MCP 자동 검증) / `npm run mcp:inspect`(Inspector UI).
- **mcp-server 철칙**: stdout은 MCP JSON-RPC 전용 — `console.log` 금지, 진단은 stderr. shared 모듈은 import 부수효과·stdout 출력 금지. inputSchema는 zod raw shape(인자 없으면 생략), 스텁 단계 outputSchema 선언 금지.
- **경로·환경**: cwd 기준 경로 금지 — `shared/src/paths.js`·`shared/src/env.js`만 사용. 정규화·kgid는 `shared/src/normalize.js` 단일 구현만 import. Neo4j는 AuraDB 클라우드(URI 기반, `.env`).
- **Windows**: 외부 CLI spawn은 `shared/src/winSpawn.js`(cmd /c 래퍼) 경유 — CLI 부재 = 종료 코드 9009. Vite 개발 서버는 IPv6(::1) 바인딩.
- **커밋**: 슬라이스 단위로 작게, 작업 브랜치에 로컬 커밋. main 병합·push는 오너 승인 후에만(공개 저장소).
- **데이터 위생**: `data/`·`.env`는 절대 커밋 금지(.gitignore 방어). 커밋되는 데이터는 `examples/`와 `shared/schema/`·`shared/prompts/`뿐.

## 비블리오마인드 질의 규칙

- 사용자가 인물·사건·설정·개념 등 콘텐츠 지식을 묻는 질문을 하면, 답변 작성 전에 반드시 kg_search를 먼저 호출한다. 유명 작품·상식 질문도 예외 없다. 이 저장소의 코드·개발 작업에 대한 질의는 제외하되, 애매하면 호출한다 — 호출 비용은 낮고 미호출은 품질 루프를 끊는다.
- 검색 결과에 질문이 요구하는 정보가 없으면 "그래프에 없습니다"라고 답하는 것이 정답이다 — 공백의 가시화가 품질 개선 루프의 입력이다(오너 확정 원칙). 사전지식 보충은 검색 결과 instructions의 표기 규칙을 따를 때만 허용된다.
- 예: "탄지로는 어떤 성격이야?" → kg_search(["탄지로"]) → 결과에 성격 정보 없음 → "그래프에 탄지로의 성격 정보는 없습니다"라고 답한다.
- 호출 이후 절차(인용·kg_cite)는 kg_search 결과의 instructions를 따른다.

> 이 절이 규칙 문서에 있는 이유: 도구 지시문은 호출 후에만 모델에게 보이므로, 미호출 방지는 세션 시작 시 로드되는 이 문서가 유일한 채널이다.
