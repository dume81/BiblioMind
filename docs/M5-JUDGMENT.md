# M5 판정 실행표 — Codex 표면 21문 (2026-08-26 착수)

> **목적**: 제품 성립이 클라이언트 1종(Claude) 특성이 아님을 증명한다 — **21문 전량 + 단일 클라이언트 규율** `[문서] MAINTENANCE-PLAN:23`. 완료 조건 = ROADMAP 슬라이스 1 완료 조건 전 항목 + 이 판정표 `[문서] MAINTENANCE-PLAN:90`. 통과 시 **슬라이스 1 ✅ → main 병합·공개 push 승인 절차**로 진행한다.
> **정본 관계**: 문항 원문 = `spike-eval-questions.md` · Claude 표면 실적(대조쌍) = `spike-judgment-log.md`(21/21) · 방법론 원형 = `M4-REJUDGMENT.md`(무수정 보존 — 이 문서가 M5용 개정판이다).
> **판정 환경**: **예시 그래프(귀멸의 칼날 29노드·55관계)** — Claude 표면 판정과 동일 조건. 판정 종료 후 `kg_rebuild` 1회로 실자료(readians) 완전 복원(원본 진실 = 로컬 Reviewed/, 손실 0).

---

## 0. 시작 전 점검

| # | 확인 | 방법 | 상태 |
|---|---|---|---|
| 1 | Aura 활성 + 예시 그래프 | `inject-example` 실행 | ✅ **집계자 완료 [실측]** — 29노드·55관계·인덱스 2종 ONLINE·cjk (buildId `20260827T044116`) |
| 2 | 개발 서버 | 허브 8787 + Vite 5173 | ✅ **집계자 완료 [실측]** — `dev:all` 기동, `GET /api/health` = `{ok:true}` |
| 3 | Codex MCP 등재 | `%USERPROFILE%\.codex\config.toml` | ✅ **집계자 확인 [실측]** — `[mcp_servers.bibliomind]` 등재 |
| 4 | 코드 동결점 | `git log --oneline -1` | ✅ `8cc3a94`(슬라이스 8) — 판정 중 코드 변경 금지 |
| 5 | **Codex 데스크탑 재시작** | 앱 완전 종료 후 재실행 | 🔲 **이사님** — MCP 서버는 클라이언트 기동 시 스폰되므로 재시작 없이는 구버전 프로세스가 남는다(M4 A3 실사고) |
| 6 | **서버 버전 확인 (v2.12 신기준)** | Codex에서 `kg_status` 1회 | 🔲 **이사님** — **실수치가 보이면 최신**: "자료 파이프라인 — Input 28 …"과 **"Neo4j — 노드 29·관계 55"**(예시 그래프). *"스캐폴딩 스텁 응답"* 또는 *"0.5 스파이크 준비"* 가 보이면 **구버전** — 재시작 재확인. (종전 기준(§0-7 스텁 문구)은 슬라이스 8이 kg_status를 완성하며 낡았다) |
| 7 | 화면 | 크롬 `http://localhost:5173` | 🔲 **이사님** — 창을 **화면에 보이게** 유지(백그라운드 탭이면 렌더 지연으로 ⑤ 오염) |
| 8 | **단일 클라이언트** | 판정 중 bibliomind MCP 연결 = Codex 1개 | 🔲 **이사님** — GraphRAG_1st를 루트로 연 Claude Code 창이 있으면 닫는다. (집계자의 현 세션은 워크스페이스 루트라 bibliomind MCP 미접속 — 간섭 없음. 집계자 브라우저 탭은 SSE 뷰어일 뿐 MCP 연결이 아니다) |

## 1. 세션 규율 (M4 §1과 동일 원리)

- **D1·D2·D3·E1·E2·E3 (6문)**: 문항마다 **새 대화**에서 실행 — 이 6문이 재는 것은 호출률이며, 앞 문항의 검색 결과가 남아 있으면 지표가 무효가 된다. Codex 데스크탑에서 새 대화를 만드는 방법(메뉴 명칭)은 `[미확인]` — 새 채팅/새 스레드 생성이면 충분하다.
- **나머지 15문**: 연속 대화 그대로 진행.
- **진행 순서**: A1→A2→A3→A4→A5→B1→B2→B3→B4→B5→C1→C2→C3→C4→C5 (연속) → 새 대화×6: D1 / D2 / D3 / E1 / E2 / E3.
- 질문은 **원문 그대로**(한 글자도 바꾸지 않는다).
- **협업 방식**: 이사님이 문항 1개를 실행할 때마다 이 창(집계자)에 **"A1 완료" + Codex 응답 전문 붙여넣기** → 집계자가 판정 ④ 스냅샷 대조·기록 후 "다음 문항 진행하세요"로 응답한다. `last-searches.json`이 5건 롤링이라 **문항 직후 대조가 필수**다.

## 2. 21문 (원문 · Claude 표면 실적 = 대조쌍)

| # | 질문 원문 | Claude 실적 `[문서] spike-judgment-log` |
|---|---|---|
| A1 | 귀살대는 어떤 조직이야? | ✓ 4/4 verified · 5.0s |
| A2 | 우로코다키 사콘지는 누구야? | ✓ T1 · 5/5 · 7.2s |
| A3 | 일륜도가 뭐야? | ✗→반증 완료(모델 키워드 손상 — 이후 M2 복원 기능 추가. 복원 문구가 뜨면 정상 출력) |
| A4 | 전집중 호흡은 뭐야? | ✓ T1 · 2/2 · 5.0s |
| A5 | 다이쇼 시대에 무슨 일이 있었어? | ✓ T1 · 2/2 · 7.5s |
| B1 | 탄지로는 어디 소속이야? | ✓ T2 · 3/3 · 4.3s |
| B2 | 네즈코는 어떻게 도깨비가 됐어? | ✓ T2 시드 5 · 4/4 · 5.6s (M2 후 시드 4·11n·18r가 기대값) |
| B3 | 기유가 한 일을 알려줘 | ✓ T2 · 7/7 · 4.3s |
| B4 | 호타루는 무엇을 다루는 사람이야? | ✓ T2 · 3/3 · 8.4s |
| B5 | 최종선별이 뭐야? | ✓ T2 · 4/4 · 5.2s |
| C1 | 탄지로와 귀살대는 무슨 관계야? | ✓ 13/13 · 5.9s |
| C2 | 사비토와 마코모는 무슨 관계야? | ✓ T1×2 · 최단경로 보강 · 6.1s |
| C3 | 카마도 가족에게 무슨 일이 일어났어? | ✓ 3/3 · 5.2s |
| C4 | 네즈코와 재갈은 무슨 관련이 있어? | ✓ 3/3 · 6.2s |
| C5 | 우로코다키와 탄지로 사이에 무슨 일이 있었어? | ✓ T2×2 · 8/8 · 5.7s |
| D1 | 귀멸의 칼날 주인공이 누구야? | ✓ 호출 · 2/2 |
| D2 | 탄지로는 어떤 성격이야? | 침묵 실패 2회 → 질의 규칙 적용 후 ✓ (규칙은 이미 배포됨) |
| D3 | 네즈코와 탄지로는 남매야? | ✓ 호출 · Family 경로 |
| E1 | 무잔은 누구야? | ✓ unmatched·빈 인용 (부정 대조) |
| E2 | 탄지로의 아버지 이름은 뭐야? | ✓ 만점 (관계 부재 고지) |
| E3 | 사부로 영감의 직업은 뭐야? | ✓ 만점 (속성 부재 고지) |

**회귀로 오인 금지 — 설계된 출력 2종**: `키워드 자동 재검색 N건: …`(복원) · `1층의 n건 중 m건을 인용했습니다 — …강조 대비가 낮습니다`(밀도 안내). **보이면 안 되는 문구**: `과잉 인용 의심` · `스캐폴딩 스텁 응답` — 구버전 신호.

## 3. 기록 양식 (집계자가 문항마다 채운다)

| 문항 | ①호출 | ②시드(계층) | ③인용 | ④3상태(자동) | ⑤소요(육안) | 세션 | 비고 |
|---|---|---|---|---|---|---|---|
| E3 | **✓ 호출** | 사부로 영감 T2(추정 — 기대대로 적중) | **1/1 verified(기계 확인)** — 사부로 영감 노드 인용 | **✓ 11/11** (`s-20260827-200654-e8jn`) | 미기재 | **새 대화** | **부정 대조 만점**: 속성 부재 수준 특정("직업 속성이나 관련 관계 미등록") — 직업 미조작. 부재 주장(직업 속성) → **역검증 대기열 9건째** |
| E2 | **✓ 호출** | 카마도 탄지로 T2(추정 — 기대대로 시드는 적중) | **1/1 verified(기계 확인)** — 탄지로 노드 인용 | **✓ 11/11** (`s-20260827-195654-qsob`) | 미기재 | **새 대화** | **부정 대조 만점**: 관계·속성 부재 수준 특정("관련 속성이나 부자 관계가 없습니다") + 그래프 밖 지식 명시 구분. 부재 주장(부자 관계) → **역검증 대기열 8건째**(관계 부재 유형) |
| E1 | **✓ 호출** | unmatched(기대대로 — 무잔 노드 부재) | **빈 인용 제출 ✓** — citation none 확정(기계 확인) | **✓ 11/11** (`s-20260827-195459-e1ga`) | 미기재 | **새 대화** | **부정 대조 만점**: "그래프에 없습니다" + 그래프 밖 지식 명시 구분 + 빈 인용 확정 — 환각·허위 인용 0. D3 잔상 소거 확인. 부재 주장(무잔) → **역검증 대기열 7건째**(M4에서도 0건 검증된 항목 — 회차 재수집) |
| D3 | **✓ 호출** | 네즈코+탄지로 복수 시드(추정) | **1/1 verified(기계 확인)** — 형제 관계(`r_b7a4…`) 인용 | **✓ 11/11** (`s-20260827-194615-2ejw`) | 미기재 | **새 대화** | 1층 24n·42r(복수 시드 합집합). 2층 = 탄지로–네즈코–형제 관계 정확히 3요소 — Family 경로 인용(Claude 동형) |
| D2 | **✓ 호출 1차 통과**(Claude에선 침묵 실패 2회 후 사다리로 교정된 문항 — 질의 규칙이 Codex에서 첫 시도부터 작동) | 카마도 탄지로 T2(추정) | **1/1 verified(기계 확인)** — 탄지로 노드 인용 | **✓ 11/11** (`s-20260827-194235-2pd4`) | 미기재 | **새 대화** | 속성 부재 + 수준 특정("속성은 주인공 역할뿐") 모범 형식 — Claude 재측정 통과본과 동형. 부재 주장(성격 속성) → **역검증 대기열 6건째** |
| D1 | **✓ 호출**(화면 실증 — 사전지식 유혹에도 검색 실행) | 시드 0건('귀멸의 칼날' 노드 부재 — 시드 합격선 분모 밖) | **빈 인용 제출 ✓** — citation none 확정(기계 확인) | **✓ 11/11** (`s-20260827-193520-rh7u` — 빈 1층 푸시·잔상 소거·citation=none 정합) | 미기재 | **새 대화** | Claude 대조쌍(2/2 인용)과 키워드 선택 갈림: Codex는 작품명으로 검색→미적중→"그래프에 없습니다" + **"그래프 밖 일반 지식" 명시 구분 보충**(표기 규칙 준수 — 모범 부재 처리). C5 하이라이트 잔상이 빈 푸시로 소거됨(잔상 방지 실증). 부재 주장("귀멸의 칼날 노드 없음") → **역검증 대기열 5건째** |
| C5 | ✓(원문 기계 일치) | 우로코다키+탄지로 T2×2(추정) | **3/3 verified(기계 확인)** | **✓ 11/11** (`s-20260827-192211-mgpr`) | 미기재 | 연속 | **1층 20n·33r = Claude 대조쌍 정확 동일**. 2층 4n·3r = '탄지로의 수련' 경유 사제 관계 축(TRAINED·수련 관계) — 대조쌍과 동형 진단 |
| C4 | ✓(원문 기계 일치) | 카마도 네즈코+재갈 복수 시드(추정 — 재갈 T1은 워터폴 단락 구조상 확실) | **3/3 verified(기계 확인)** | **✓ 11/11** (`s-20260827-191945-ilsp`) | 미기재 | 연속 | 1층 11n·18r. 직접 관계 부재 → **사건 경유 3관계**(기유가 재갈 물린 사건 — HAS_INSTRUMENT 축) 정확 진단 = Claude 대조쌍과 동형. 집계자 브라우저 재접속 후 재생 규칙으로 상태 회수(§5.1 재생 실증 덤) |
| C3 | ✓(원문 기계 일치) | 카마도 가족 T1(추정) | **1/1 verified(기계 확인)** — TARGETED 직접 관계 | **✓ 11/11** (`s-20260827-161836-2oed`) | 미기재 | 연속 | 1층 4n·3r = Claude 대조쌍 동일. **M4에서 집계자 오염으로 ④ 무효였던 문항이 이번엔 정상 측정·통과**. 3상태 분리(1층 전용 2n·2r 렌더) |
| C2 | ✓(원문 기계 일치) | 사비토+마코모 복수 시드(추정) | **2/2 verified(기계 확인)** | **✓ 11/11** (`s-20260827-160920-2rs0`) | 미기재 | 연속 | **최단경로 보강 작동 실증**: 1층 3n·2r = Claude 대조쌍 동일 — 직접 관계 없는 두 시드를 잇기 위해 **탄지로(`n_bcfe…`) 경유 포함 기계 확인**. 직접 관계 부재 정직 진단 → **역검증 대기열 4건째**(관계 부재 유형) |
| C1 | ✓(원문 기계 일치) | 탄지로+귀살대 복수 시드(추정) | **1/1 verified(기계 확인)** — MEMBER_OF 직접 관계만 절제 인용 | **✓ 11/11** (`s-20260827-160704-t2h7`) | 미기재 | 연속 | 1층 21n·34r(두 시드 1홉 합집합). Claude 대조쌍은 13/13 전량 인용(질문을 "모든 연결"로 해석) vs Codex는 직접 관계 1건(관계 자체만 답함) — **인용 스타일 차이일 뿐 양쪽 다 존재성 검증 통과**. 경로 하이라이트 성립 |
| B5 | ✓(원문 기계 일치) | 귀살대의 최종선별 T2(추정 — 부분 이름→정식 명칭 해석, Claude와 동일 패턴) | **5/5 verified(기계 확인)** | **✓ 11/11** (`s-20260827-155917-0sa9`) | 미기재 | 연속 | 1층 5n·4r — 수련→합격→첫 임무 사슬. 인용에 '탄지로의 수련'(`n_201c…`) 포함 — B2 정정 때 확정한 kgid가 정상 활용됨 |
| B4 | ✓(원문 기계 일치) | 하가네즈카 호타루 T2(추정) | **3/3 verified(기계 확인)** | **✓ 11/11** (`s-20260827-155428-pezy`) | 미기재 | 연속 | **임계값 양방향 증명 완성**: B2에서 잘린 호타루(`n_69fd…`)가 이 질문에선 **1층 포함 기계 확인** — 정탐 생존. 1층 3n·3r = Claude 대조쌍 동일. 부분 이름("호타루")→정식 명칭 해석 |
| B3 | ✓(원문 기계 일치) | 토미오카 기유 T2(추정) | **7/7 verified(기계 확인)** — 1층 관계 전량 인용 | **✓ 11/11** (`s-20260827-155231-j3jx`) | 미기재 | 연속 | **1층 6n·7r = Claude 대조쌍과 동일 크기·인용 수까지 동일(7/7)**. "한 일 전부" 질문이라 전량 인용이 정당(M4에서 오탐 판정된 그 패턴) — 밀도 안내는 설계된 출력. 답변 3개 행위가 전부 관계 근거 보유 |
| B2 | ✓(원문 기계 일치) | 카마도 네즈코 T2(추정) | **3/3 verified(기계 확인)** | **✓ 11/11** (`s-20260827-154907-51zf`) | 미기재 | 연속 | **M2 오탐 제거 직격 실증**: 1층 **11n·18r = 기대값 정확 일치** + 하가네즈카 호타루 kgid(`n_69fd…`) **미포함 기계 확인** — M4 핵심 실증 ①이 Codex 표면에서 재현. *(정정 자진 기재: 최초 기록은 `n_201c…`를 호타루로 오참조 — B4 판정 중 발각, DB 대조로 `n_201c…`=탄지로의 수련·호타루=`n_69fd…` 확정. 바른 kgid로 재검증해도 미포함 결론 불변)* 부재 고지("원인·과정 없음" — M4 제품 신호의 행위자 공백과 정합) → **역검증 대기열 3건째** |
| B1 | ✓(원문 기계 일치) | 카마도 탄지로 T2(추정) | **1/1 verified(기계 확인)** — MEMBER_OF 관계 인용 | **✓ 11/11** (`s-20260827-154006-hxxd`) | 미기재 | 연속 | **1층 20n·31r = Claude 대조쌍과 동일 크기(무개입 실증)**. 2층=탄지로+귀살대+MEMBER_OF — 3상태 완전 분리(1층 전용 18n·30r 렌더). 오너가 "A5 결과"로 오기했으나 내용·스냅샷 모두 B1 |
| A5 | ✓(원문 기계 일치) | 다이쇼 시대 T1(추정) | **2/2 verified(기계 확인)** | **✓ 11/11** (`s-20260827-153800-rvx2`) | 미기재 | 연속 | 1층 3n·2r(다이쇼 시대–몰살 사건–이야기 구간). 답변이 사건+매체 구간을 정확히 그래프 범위 내로 한정. Claude 대조쌍 2/2·7.5s |
| A4 | ✓(원문 기계 일치) | 전집중 호흡 T1(추정 — Claude 대조쌍 T1 확정) | **2/2 verified(기계 확인)** | **✓ 11/11** (`s-20260827-153018-ak9h`) | 미기재 | 연속 | 1층 2n·1r(전집중 호흡–탄지로). 속성 부재 고지("원리·효과 없음") → **역검증 대기열 2건째**. Claude 대조쌍 2/2·5.0s와 동형 |
| A3 | ✓ | 일륜도 T1(추정 — 원문 `b95c` 코드포인트 기계 확인·1층 3n·2r=S03 기대값 일치) | **3/3 verified(기계 확인)** | **✓ 11/11** (`s-20260827-152305-l3vb`) | 미기재 | 연속 | **손상 미발생 시행**(재검색 문구 없음 — 정상, 표본 특성상 확률 5.4%). 속성 부재 정직 고지("특징·용도 없음") → **부재 주장 1건 역검증 대기열 등재**(합격선 7). 밀도 안내 서술형 = 신버전 재확인. Claude 1차의 거짓 부재가 이번엔 정상 적중 — M2 개선 이후 첫 Codex 표본 |
| A2 | ✓ | 우로코다키 사콘지 T1(추정) | **3/3 verified(기계 확인)** | **✓ 11/11** (`s-20260827-152009-g86j`) | 미기재 | 연속 | 2층⊊1층 — 1층 전용 1n·1r 실표시(3상태 분리 표본). 대조쌍 Claude 5/5·7.2s |
| A1 | ✓ | 귀살대 T1(추정) | **4/4 verified(기계 확인)** — 스냅샷 citation {verified·4·4} | **✓ 11/11** (`s-20260827-151429-olb7`) | 미기재 → 오너 회신 대기 | 연속 | §0-5·6은 집계자가 객관 대체 확인 [실측]: MCP 프로세스 6개 전부 codex.exe 자식·전부 슬라이스 8 이후 기동(타 클라이언트 0) — 다중 프로세스는 Codex의 대화별 스폰, 도구 호출은 활성 대화 1개뿐이라 판정 유효. 1층 5n·4r 소규모 → 2층=1층 전체(밀도 안내는 설계된 출력). last-searches에 시드 계층 미기록[실측]이라 ②는 추정 — 이후 문항부터 도구 반환의 시드 줄 공유 요청 |

- **④**: 집계자가 브라우저에서 `window.__bibliomind` 스냅샷 → `node scripts/check-highlight.js --snapshot` 실행·기록.
- **⑤ 소요 = 육안 측정(질문 전송 → 3D 1층 표시, 초)**. `measure-latency.mjs`는 **Claude Code 대화 기록 전용이라 Codex를 못 잰다** `[실측: 소스의 ~/.claude/projects 고정 경로]` — 스파이크 1차와 같은 육안 방식으로 폴백. 이사님이 어림값(예: "약 7초")만 불러주시면 된다.

## 4. 합격선 (정본 = ROADMAP 슬라이스 1 완료 조건 — 제3조: 기존 완료 조건이 있으면 그것이 합격선)

| # | 명제 | 임계값 | 직전 실적 | 측정 수단 · 판정 주체 |
|---|---|---|---|---|
| 1 | kg_search 호출률 | **21문 전건 호출**(미호출 시 §1.14-6 사다리 적용 후 재측정) | 없음(신규 표면) — 대조쌍: Claude 21/21(사다리 1회 포함) | ①열 · 집계자 |
| 2 | kg_cite 준수율 | **≥8/10** `[문서] ROADMAP:17]` — 빈 인용 제출 포함, 분모 = 인용 대상 문항 | 없음(신규) — 대조쌍: Claude 전건 제출 | ③열 · 집계자 |
| 3 | 시드 적중 | **≥80%** `[문서] ROADMAP:17]` | 없음(신규) — 대조쌍: Claude 15/15 | ②열 · 집계자 |
| 4 | 부정 대조 | E1·E2·E3에서 **환각·허위 인용 0건**(1건이면 즉시 미달) | 없음(신규) — 대조쌍: Claude 3/3 | E군 · 집계자→오너 |
| 5 | 3상태 표시 | 문항별 `check-highlight` 통과 + **인용 없음/탈락 시 1층 유지 1케이스 포함** `[문서] ROADMAP:17]` | 없음(신규) — 대조쌍: Claude/M4 121/121 | ④열 · 집계자 |
| 6 | 소요 | **10초 예산 대비 실측 기록**(연속 대화 기준 — 새 대화 6문은 참고 표기) | 없음(신규) — 대조쌍: Claude 연속 중앙값 5.6초(스파이크)·6.9초(M4) | ⑤열 · 집계자 |
| 7 | 거짓 부재 | 부재 주장 **전건** Aura 역검증(노드·속성·관계 3유형) — 후보 0건 | 대조쌍: M4 0건 | `check-absence.js` · 집계자 |

- 미달·미측정 1건 이상 = **"부분 완료 — 미달 N건"**. Codex 결과가 임계값은 넘되 Claude 대조쌍 대비 현저히 열위면 **오너판정대기**로 올린다(대조 패널 — `[문서] MAINTENANCE-PLAN:158`).

## 5. 판정 후 처리 (집계자)

① `kg_rebuild` 1회 — 실자료(readians) 복원 + 실측 재확인 ② Codex↔Claude 대조 패널 ③ DECISIONS·ROADMAP(슬라이스 1)·PROCESS·SESSION-STATE 갱신 ④ **main 병합·공개 push 결정 요청**(오너) ⑤ 5-C·D4 대기열 복귀.

---

## 3-b. 회차 검증 실행 기록 + 기록 감사 반영 (2026-08-27 — 적대 감사 blocker 1·major 5·minor 6 처리)

> §3의 문항 행은 사초로 보존하고, 감사가 잡은 기록 결함은 이 절의 정정으로 갈음한다(역사 소급 수정 대신 정정 절 — DECISIONS 관행).

**합격선 7 채점란(감사 blocker 해소) — 부재 주장 역검증 실행 기록**:
- `node scripts/check-absence.js --fixture mcp-server/fixtures/absence-claims-m5.json` → **전건 사실 10/10 — 거짓 부재 0건** (노드 4·속성 4·관계 2. M4 오탐 회귀 케이스 '탄지로.성격 vs role=주인공' 정상 판별 포함)
- C2(사비토↔마코모 직접 관계)는 도구 형식(단일 노드 인접 유형)으로 표현 불가 → **수동 Cypher 보충: 직접 관계 0건(사실)** — 픽스처 note에 사유 기재
- **도구 회귀 2건 발견·수리 [실측]**: `check-absence.js`·`capture-golden.js`의 import가 8/23 neo4jClient 이사(mcp-server/lib→shared)에 미추종으로 실행 불능이었다 — 1줄씩 수리(스크립트는 테스트·가드 밖이라 잡히지 않았던 유형)

**합격선 5 하위 요건("인용 없음/탈락 시 1층 유지") — 자연 표본 미발생 → 서버 프로브로 실증**:
- 21문 중 빈 인용 2건(D1·E1)은 1층 자체가 빈 케이스라 "1층 유지" 실증이 아니었다(감사 지적 정당)
- 프로브 `s-20260827-201606-3xik` [실측]: 실자료 그래프에서 kg_search(시드 4건 T2·1층 58n·59r) → kg_cite 빈 목록 제출 → **citation=none·active·1층 58n·59r 전량 렌더(117/117)·2층 0** — 하위 요건의 결정론적 실증
- 프로브 중 부수 관측: 화면 그래프가 낡은 상태(예시)에서는 matched=0으로 비활성 처리됨 — N/M 격차 설계 동작의 실증. 원인은 집계자가 복원 재빌드를 도메인 함수로 직접 돌려 `graph.refresh` 푸시가 생략된 것(도구 표면과 도메인의 푸시 책임 분리 — Q15와 동계열 관찰)

**합격선 3 기계화(감사 major 해소)**: 시드 "적중"은 각 문항 스냅샷의 **1층 비어있지 않음**으로 기계 확인된다 — A~C 15문 전건 1층 비공 = **15/15 [실측]**. ②열의 (추정)은 **계층(T1/T2) 표기**에 한정된 것(last-searches가 계층 미기록 [실측] — Codex UI의 도구 카드 시드 줄은 오너 부담을 고려해 회수하지 않음). E1·D1의 시드 0건은 분모 밖(부정 대조·호출률 문항 — M4 합격선 1과 동일하게 분모 = A~C 15문).

**합격선 2 분모 확정(감사 major 해소)**: kg_cite 제출은 **21/21 전건**(verified 19 + none 확정 2 — D1·E1의 빈 인용도 규율상 제출이다). ROADMAP 임계값 "≥8/10"은 어떤 분모 선택(10문이든 21문이든)에서도 상회.

**인용 격차 5건 해명(감사 major 해소)**: A2(3/3 vs Claude 5/5)·B1(1/1 vs 3/3)·B2(3/3 vs 4/4)·C3(1/1 vs 3/3)·C5(3/3 vs 8/8) — 전건 Codex의 **절제 인용 스타일**(직접 근거만 인용)이며, 제출 전건 verified·탈락 0·환각 0이므로 질 열위가 아니라 스타일 차이로 판정한다. C1 해명과 동일 논리. **오너 확인 항목으로 병기**(§4-b 판정 4와 함께).

**소소한 정정 4건(감사 minor)**: ①전 21문의 원문 일치는 각 행 searchId의 스냅샷 `question` 문자열로 기계 확인됐다 — ①열 표기 불균일(8행)은 누락이지 미확인이 아님 ②1층 크기 대조값 출처 = `spike-judgment-log.md:34~43` `[문서]` ③합격선 3의 대조쌍 "Claude 15/15"는 **M4 재판정**(복원 기능 배포 후, `M4-JUDGMENT-LOG.md`) 값 — 스파이크 1차 A3 손상 회차와 다름 ④C4 비고의 "§5.1" = **TECH-SPEC §5.1**(허브 재생 규칙), "확실" 표현은 "추정(워터폴 단락 구조 근거)"으로 정정. §0-5·6은 집계자 대체 확인(프로세스 계보·기동 시각 — 간접 증거임을 명시)으로 갱신하며, §0-7(창 전면 유지)은 ⑤가 육안 어림이라 실해 없음.

## 4-b. 최종 판정표 (제4조)

| # | 명제 | 임계값 | 실측값 | 판정 |
|---|---|---|---|---|
| 1 | kg_search 호출률 | 21문 전건 | **21/21** (전건 searchId 스냅샷 증빙 — D2 침묵 실패 이력 문항도 1차 통과) | **달성** |
| 2 | kg_cite 준수 | ≥8/10 | **21/21 전건 제출**(verified 19·none 확정 2·탈락 0) | **달성** |
| 3 | 시드 적중 | ≥80% (분모 A~C 15문) | **15/15 = 100%** (1층 비공 기계 확인) | **달성** |
| 4 | 부정 대조 | E군 환각·허위 인용 0건 | 3/3 만점(집계자 판정 — 그래프 밖 지식 명시 구분·빈 인용 규율) | **오너판정대기** (판정 주체 = 집계자→오너) |
| 5 | 3상태 표시 | 문항별 check-highlight 전항 + 1층 유지 1케이스 | **21/21문 전항 통과**(총 231항) + 하위 요건 프로브 실증(`s-…-3xik`) | **달성** |
| 6 | 소요 | 10초 예산 대비 실측 기록 | **오너 육안 어림 [실측·전문가 증언]: "대체로 5~10초 안, 10초 초과 기억 없음"** — 예산 내. 한계 병기: 계측기가 Codex 미지원이라 어림치 — M4식 정밀 퇴행 검출(+23%급) 불가 | **달성**(육안 폴백 — 실행표 §3 명시 방식) |
| 7 | 거짓 부재 | 후보 0건 | **역검증 10/10 + 수동 1건 전건 사실 = 0건** | **달성** |

**종합: 6 달성 + 1 오너판정대기 → "부분 완료 — 오너판정대기 1건"** (E군 부정 대조의 오너 승인 시 완료 전환. 인용 격차 5건 해명 수용 여부도 같은 회신에 포함 요청).
