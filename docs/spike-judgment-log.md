| **12.3s** | Fable || **9.3s** | Fable || **15.6s** | Fable || **9.7s** | Fable || **6.3s** | Fable || **11.6s** | Fable || **5.9s** | Fable || **5.2s** | Fable || **7.5s** | Fable || **5.0s** | Fable || **5.0s** | Fable |# 스파이크 판정 실기록 (2026-08-21 — 슬라이스 1)

> 세션·모델이 바뀌어도 이 파일 하나로 판정을 이어받는다. 집계 기준은 spike-eval-questions.md.
> 판정 표면: Claude Code(GraphRAG_1st 루트) · 모델 Fable 5 (한도 95% — 소진 시 Opus 5 전환, 문항별 모델 기록)

## 재개 절차 (2026-08-22 — Opus 5로 이어받기)

1. **집계 창(워크스페이스 루트, 이 대화)**: 모델을 Opus 5로 전환 후 "spike-judgment-log.md 읽고 이어서"라고 지시 — 이 파일과 SESSION-STATE.md가 전체 맥락의 정본. 협업 방식(전문가 패널·문서 먼저·게이트·호칭 "이사님"·매 응답 끝 현재 위치 표기)은 bibliomind/CLAUDE.md 준수.
2. **판정 창(GraphRAG_1st 루트)**: 모델 Opus 5 → `npm run dev:all` 켜고 크롬 5173 열기 → **나머지 10문(A2·A3, B1~B4, C2~C5)** 진행. 연속 대화 허용, **문항마다 ⑤ 소요 시간(질문→화면 표시, 대략 초) 기록 필수 — 유일하게 빈 지표**. 모델란에 Opus 기재.
3. 10문 결과를 집계 창에 공유 → 문항별 판정 → **집계표 확정**(시드 적중률: 남은 10문 중 7문 이상 적중 시 ≥80% 충족) → DECISIONS에 §1.14 기준 대조 기록.
4. 이후: **Codex 표면 21문**(Codex 데스크탑, AGENTS.md에 질의 규칙 반영됨) → 통과 시 main 병합 승인 요청 → 판정 후 처리 목록(아래 3번 항목) 착수 — 첫 건은 오너 지시 예고된 **관계 유형 전면 한글화 개정안 게이트**.
5. 주의: Fable 5 한도는 8/25(화) 오전 11시 재설정 — 그 전까지 Fable 필요 작업 없음(D2 재측정 완료됨). 판정 로그 갱신은 문항 판정마다 커밋.

## 문항별 판정 (Claude Code 표면) — **완료 11/21문**

> 산입 규칙: 문항당 **최종 시도** 기준(A1·E2 재실행, D2 재측정 = 최종치. 1차 실패·구버전 실행은 비고에 관찰 보존). 범례: '질문 일치' = 세트 원문 그대로 / ②시드 '확정' = 도구 내역에서 tier 문자열 직접 확인, '추정' = 구조상 확실하나 미확인. **Codex 표면 21문은 동일 형식의 별도 표를 신설한다.**

| 문항 | 질문 일치 | ①호출 | ②시드(계층) | ③인용 | ④3상태 | ⑤소요 | 모델 | 비고 |
|---|---|---|---|---|---|---|---|---|
| A1 | ✓ | ✓ | 귀살대 ✓(T1 추정) | 4/4 verified | ✓ | **5.0s** | Fable | 1차=구 지시문(답변 생략), 재실행으로 형식 확인 |
| A4 | ✓ | ✓ | 전집중 호흡 ✓(**T1 확정**) | 2/2 verified | ✓ | **5.0s** | Fable | 속성 부재 정직 고지 모범. 과잉 인용 오탐 관찰 |
| A5 | ✓ | ✓ | 다이쇼 시대 ✓(T1 추정) | 2/2 verified | ✓(전달 확인) | **7.5s** | Fable | 그래프 밖 지식(1912~1926) 자진 구분 |
| B5 | ✓ | ✓ | 귀살대의 최종선별 ✓(T2 추정) | 4/4 verified | ✓ | **5.2s** | Fable | 부분 이름→정식 명칭. 그래프 밖 지식 본문 제외 |
| C1 | ✓ | ✓ | 탄지로+귀살대 ✓(4건) | **13/13 verified** | ✓(3상태 전부 육안 구분) | **5.9s** | Fable | 경로 하이라이트 성립. 관찰 3호: 별칭 심문 12/12 기계 대조 통과 |
| D1 | ✓ | **✓ 호출** | (호출률 문항) | 2/2 verified | ✓ | **11.6s** | Fable | 오염 주석: 같은 질문 재실행(연속 대화) |
| D3 | ✓ | **✓ 호출** | Family 경로 인용 ✓ | 3/3 verified | ✓ | **6.3s** | Fable | 오염 주석: 연속 대화(질문은 최초). 맥락 유혹에도 재검색 |
| D2 | ✓ | 1차·2차 **✗ 미호출** → **재측정 ✓ 통과** | 탄지로 T2 | 3/3 verified | 허브 꺼짐(무-throw 안내 정상) | **9.7s** | Fable | 침묵 실패 2회 재현 → 사다리 1단(질의 규칙) 적용 → 즉시 통과. 부재+수준 특정(항목 8 형식) 완벽 |
| E2 | ✓ | ✓(규칙 명시 인용) | 탄지로 T2(기대대로) | 빈 인용 제출 ✓·무표기 정확 | 1층만 표시 ✓ | **9.3s** | Fable | /clear 재실행(반영 후 주석). 부재 첫머리+수준 특정(노드·관계 부재) — 항목 8 실전 2호. 만점 |
| E1 | ✓ | ✓(2회—재검색 준수) | unmatched(기대대로) | 빈 인용 제출 ✓ | 잔상 소거+기본 화면 ✓ | **15.6s** | Fable | /clear 청정. 표기 無(정확), 그래프 밖 고지 후 보충. 만점 |
| E3 | ✓ | ✓ | 사부로 영감 ✓ | 2/2 verified | ✓(WARNED·HOSTED 강조) | **12.3s** | Fable | /clear 청정. 직업 미조작+속성 부재 수준 특정. 만점 |
| C4 | ✓ | ✓ | (답변 미회수) | — | — | (5.4s) | Fable | **8/21 20:05 실행 기록 발견**(자동 측정 부산물) — 구 지시문 시기·답변 미판정이라 **오늘 재실행 대상**. 소요만 참고 기록 |
| A2 | ✓ | ✓ | 우로코다키 사콘지 ✓(**T1 확정**) | 5/5 verified | ✓(육성자 경로 강조) | **7.2s** | Opus | 1층 노드 5·관계 4. 과잉 인용 오탐 3호(1층 9건 중 5건 인용 — 소규모 구조상 불가피) |
| A3 | ✓ | ✓(2회—재검색 준수) | **✗ 미적중 — 판정 보류(패널 검토 중)** | 빈 인용 제출 ✓ | 1층 없음(정상) | **4.9s** | Opus | **거짓 부재 1호**: 그래프에 "일륜도"(U+B95C) 존재하나 모델이 키워드를 "일륨도"(U+B968)로 생성 → 시드 0건. 시스템(정규화·저장·검색) 정상 확인, 원인은 모델 키워드 손상. **B1 검색 중 모델이 자발적으로 정정** |
| B1 | ✓ | ✓ | 카마도 탄지로 ✓(**T2 확정**, 시드 3건) | 3/3 verified | ✓(JOINED·PASSED·MEMBER_OF 강조) | **4.3s** | Opus | 1층 노드 20·관계 31. 연속 대화 최속 사례. A3 자발 정정 동반 |
| B2 | ✓ | ✓ | 카마도 네즈코 ✓(**T2 확정**, 시드 5건) · 도깨비는 키워드 미사용이나 1홉 확장으로 1층 포함·인용됨 | 4/4 verified | ✓(도깨비화 인과 사슬 강조) | **5.6s** | Opus | 1층 노드 13·관계 21. **관계 부재("어떻게"의 원인) 진단 + 추출 품질 개선 후보 자진 제안** — 공백 가시화 원칙의 선순환 실증 |
| B3 | ✓ | ✓ | 토미오카 기유 ✓(**T2 확정**, 시드 2건) | **7/7 verified**(1층 관계 전량) | ✓(기유 5개 행위 사슬 강조) | **4.3s** | Opus | 1층 노드 6·관계 7. **과잉 인용 오탐 4호 — 질문이 "한 일 전부"라 1층 대부분이 실제 근거**(모델이 경고 부적절성을 스스로 설명·인용 유지) |
| B4·C2·C3·C4·C5 | — | — | — | — | — | — | — | 미실시 |

## 집계 스냅샷 (11:00 8/25 재설정 전 기준)

- 시드 적중률(A~C): **5/5** (기준 ≥80% — 15문 중 12) · kg_search 호출률: 반영 전 9/10(D2 실패 2회 재현) → **반영 후 D2 재측정 통과(사다리 1단 효과 실증)**
- kg_cite 준수: **11/11**(빈 인용 2건 포함 전 문항 제출) · verified 누적 **35건**·탈락 0 (검산: 4+2+2+4+13+2+3+3+0+0+2) · 부정 대조(E군): **3/3 확정 — 기준 충족(치명 실패 0)**
- ⑤ 소요 실측: **자동 측정 완료(2026-08-22, 판정 세션 대화 기록 파싱 — 육안 측정 대체)** — 유효 17건: **평균 8.0초 / 중앙값 6.4초 / 최대 15.6초**(10초 예산). 내역 평균 = **모델 판단 5.4초 + 도구 왕복 2.6초**(서버 순수 구간은 직접 측정 0.45초 — 지연의 지배 요인은 챗 모델의 도구 호출 판단이지 그래프 시스템이 아님). **패턴: /clear 직후 첫 질문 12~16초(컨텍스트·규칙 로드) vs 연속 대화 내 5~7초** — 예산 초과 4건은 전부 새 컨텍스트 첫 질문. 측정 도구 = tools/measure-latency.mjs
- 형식(표기+답변 본문+요약): 신 지시문 이후 전 문항 충족

### 집계·측정 규칙 (인계 감사 반영 확정)

- **호출률**: 분모 = 21문(문항당 최종 시도). 합격선은 별도 없음 — §1.14-6은 측정 + 미달 시 사다리 적용이 기준. 현재 최종 시도 기준 11/11 호출(반영 전 D2 미호출 2회 = 사다리 1단 발동 근거로 비고 보존).
- **확정 기록 위치**: 이 파일이 실기록 정본 → 21문 완료 시 spike-eval-questions.md 집계표 서식으로 최종치를 옮겨 적고 DECISIONS에 §1.14 기준 대조 ADR 기록.
- **⑤ 소요 측정 프로토콜(2026-08-22 오너 지시로 자동화 — 육안 측정 폐지)**: 판정 후 `node bibliomind/tools/measure-latency.mjs` 실행이면 끝. 판정 세션 대화 기록(~/.claude/projects)의 타임스탬프에서 질문 전송→첫 kg_search 반환(=1층 표시)을 자동 산출하며, 재검색·사용자 중단 호출은 지표에서 제외한다. (참고) 구간 정의:  (a) **시스템 구간** = 스크립트로 정밀 측정(위 실측 완료, 재측정 시 data/tmp에 임시 스크립트로 kg_search 왕복 시간 측정) (b) **사용자 체감 총시간** = 이사님이 **크롬 창을 화면에 보이게 둔 채**(백그라운드 탭이면 렌더 지연으로 오염 — 8/21 수정된 특성) 질문 전송~1층 표시까지 **초 단위 대략치**. (b)는 챗 모델의 판단 시간이 지배적이라 문항마다 잴 필요 없고 **대표 2~3문항**이면 충분 — 5개 지표 중 제품 성립 판정 가중치가 가장 낮음.

## 확정 원칙·대기 작업 큐 (세션 무관 이어받기용)

1. **오너 확정 원칙(2026-08-21)**: "그래프에 없으면 '없다'가 정답 — 공백의 가시화가 품질 개선 루프('없다'→수집·주입→품질 상승)의 입력. 사전지식으로 공백을 메꾸면 루프가 끊긴다." + 부재 진단은 수준 특정(①노드 부재 ②속성 부재 ③관계 부재 — ④스키마 수준은 슬라이스 8 schema_get 연계).
2. **반영안 — 패널 검증 후 적용 완료(2026-08-21 밤, GraphRAG_1st 커밋 e362d35 — 인계 감사에서 [A]~[E] 전부 실코드 적용 검증됨). D2 재측정 통과로 효과 실증. 재적용 금지 — 아래는 적용된 원문의 보존본:**

   **[A] GraphRAG_1st/CLAUDE.md·AGENTS.md 말미 신설 절 (두 파일 동일 문안 — 동기화 의무):**
   ```
   ## 비블리오마인드 질의 규칙

   - 사용자가 인물·사건·설정·개념 등 콘텐츠 지식을 묻는 질문을 하면, 답변 작성 전에
     반드시 kg_search를 먼저 호출한다. 유명 작품·상식 질문도 예외 없다. 이 저장소의
     코드·개발 작업에 대한 질의는 제외하되, 애매하면 호출한다 — 호출 비용은 낮고
     미호출은 품질 루프를 끊는다.
   - 검색 결과에 질문이 요구하는 정보가 없으면 "그래프에 없습니다"라고 답하는 것이
     정답이다 — 공백의 가시화가 품질 개선 루프의 입력이다(오너 확정 원칙).
     사전지식 보충은 검색 결과 instructions의 표기 규칙을 따를 때만 허용된다.
   - 예: "탄지로는 어떤 성격이야?" → kg_search(["탄지로"]) → 결과에 성격 정보 없음 →
     "그래프에 탄지로의 성격 정보는 없습니다"라고 답한다.
   - 호출 이후 절차(인용·kg_cite)는 kg_search 결과의 instructions를 따른다.

   > 이 절이 코딩 규칙 문서에 있는 이유: 도구 지시문은 호출 후에만 모델에게 보이므로,
   > 미호출 방지는 세션 시작 시 로드되는 이 문서가 유일한 채널이다.
   ```

   **[B] kgSearch.js CITE_INSTRUCTIONS 항목 8 신설 (항목 7 뒤, 3칸 행잉 인덴트, 백틱 금지) — 오너의 수준 특정 요구 통합:**
   ```
   8. 질문이 요구하는 정보가 위 결과에 없으면(항목 5의 재호출 후에도 시드 미적중,
      또는 해당 속성·관계 부재), 답변 본문 첫머리에 그 부재를 명시하라 —
      "그래프에 없습니다"가 정답이다. 이때 무엇이 없는지 특정하라(노드 자체 부재 /
      노드는 있으나 해당 속성 부재 / 관계 부재). 부재를 밝힌 뒤에만 사전 지식을
      덧붙일 수 있고, 이때 항목 1에 따라 "그래프 밖 일반 지식"임을 표기하라.
   ```

   **[C] kg_search 도구 description 끝에 1문장** — "질문 정보가 결과에 없으면 \"그래프에 없다\"고 답하는 것이 정답이다." (호출 전에도 보이는 유일한 도구 측 채널 — 이중 안전망)

   **[D] 시드 0건 요약 lines 1줄 추가** — "재호출 후에도 시드 0건이면 \"그래프에 없다\"고 답하는 것이 정답입니다." ("재호출 후에도" 필수 — 항목 5 무력화 방지)

   **[E] 워크스페이스 루트 CLAUDE.md에 포인터 1줄** — GraphRAG_1st 밖에서 시작한 Q&A 세션은 하위 CLAUDE.md가 지연 로드되므로. (판정 표면은 GraphRAG_1st 루트 고정이 운영 규칙)

   패널 확인 사항: 부재 답변 = 빈 인용 kg_cite(항목 4) + [비블리오마인드 답변] 태그 미표기(항목 7)와 자연 정합 — "인용 없음" 표시 자체가 공백의 가시화. §6.5.3 문서 개정도 [B]와 동일 문안으로 (문서 먼저 원칙). TECH-SPEC §6.5.3 지시문 원문에 항목 8 추가 필요.
3. 판정 후 처리 목록: ① 관계 유형 전면 한글화 — 오너 지시 원문 취지(8/21): "관계가 영어와 _로 되어 있는데 한글로 할 수 없나" → "판정을 모두 마치면 관계에 대해 한글화를 지시할게"(예고 확정). 범위 후보: A. 데이터 전면 전환(스키마 시드 15종 한글 명칭 + 추출 프롬프트 + 예시 KG 개정 → kg_rebuild 재주입, 관계 kgid 전부 변경 — 전문가 권장. 동의어 난립 리스크는 슬라이스 5 실측 연계) / B. 표시만 매핑(비권장 검토됨). 개정안 게이트로 착수 ② 과잉 인용 경고 1층 최소 규모 문턱 — **오탐 4회 실측**(A4: 1층 3건 중 2건, A2: 9건 중 5건, B3: 13건 중 7건 — 질문이 "한 일 전부"인 정당 인용, E3 등). 후보: 1층 총량이 임계(예: 10건) 미만이면 경고 억제 ③ kg_status 스텁의 낡은 "0.5 준비" 안내 문구 정정 ④ 잔상 처리 오너 선택 대기 — A. 현행 유지(마지막 신호 승자 + 패널의 질문 원문이 맥락 표시, 권장) / B. 시간 경과 자동 소거(비권장 — 의미 검수 시간 침해) / C. 패널에 "n분 전 질문" 표기 보강(소규모) ⑤ 주제 기반 자율 수집(범위 밖 — PRD 개정 논의) ⑥ "없음 답변 → 수집 후보 기록" 기능 논의.
4. 판정 완료 시: 집계표 확정 → DECISIONS 기록(§1.14 기준 대조·미달 항목 사다리) → Codex 표면 21문 반복 → 통과 시 main 병합 승인 요청.

## 렌더·기타 확정 사항 (이미 커밋)

- 백그라운드 탭 밀집 동결 수정: cooldownTicks=300 (5df3e6d) / 인용 지시문 답변 본문 우선(09e7314) / 제품명 헤더 + [비블리오마인드 답변] 표기(f3b4ed1) / 공백 가시화 질의 규칙 + 항목 8 [A]~[E](e362d35) — **스파이크 통과 시 main 병합 승인 범위 = 1ef89f0 → e362d35 전 커밋**
- 잔상 유지 = 설계(마지막 신호 승자) · 빈 결과 푸시 = 잔상 소거(**E1에서 실증 완료**)
