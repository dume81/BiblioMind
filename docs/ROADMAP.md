# 구현 로드맵 — 비블리오마인드(BiblioMind)

> 최종 수정: 2026-08-21
> PRD v3.1의 MVP 기능을 수직 슬라이스(기능 하나를 관통) 단위로 쪼갠 목록. 근거: TECH-SPEC v2.1 §1.14(스파이크·검증 계획).
> 한 사이클에 슬라이스 하나: 구현 → 실행 검증 → 테스트 → 커밋 → 상태 갱신.

## 상태 범례

⬜ 대기 | 🔨 진행 중 | ✅ 완료

## 슬라이스 목록 (우선순위 순)

| # | 상태 | 슬라이스 | 완료 조건 (실행해서 확인 가능한 것) | 관련 PRD 기능 |
|---|------|---------|-----------------------------------|--------------|
| 0 | ✅ | 스캐폴딩 — 모노레포 4패키지 + shared 구현 + setup.js | `npm install` → `npm test` 전 통과(기존 시각화 14파일·148케이스 무수정 포함, 총 203케이스) + `setup.js` 2회 멱등 + `mcp:smoke`에서 kg_status 응답 + `dev:all` 기동(8787·5173) | — |
| 0.5 | ✅ | 스파이크 준비 — inject-example + 도구 4종 + 사용자 액션 | `node pipeline/bin/inject-example.js` **2회 연속 실행 후 노드 29·관계 55로 동일(멱등 — delete-then-inject)** + 인덱스 2종(kg_name_key·kg_fulltext cjk) 존재를 스크립트가 자기검증 출력 + 도구 4종(kg_search·kg_cite·highlight_clear·kg_status)이 Inspector에서 응답(**이 단계에서 viewer.delivered=false는 정상** — 수신 라우트는 슬라이스 1~2) + **한국어 평가 질문 15~20개(직접·부분 이름·관계형 + 비유명 도메인 포함) 세트 작성**. **사용자 액션**: ① AuraDB 인스턴스 생성→.env ✅(8/21 완료 — cjk 실측 ✓) ② MCP 등록(Claude Code = GraphRAG_1st 루트 열기·Codex = config.toml)→클라이언트 안에서 도구 4종 표시 확인+타임아웃 키 확인 ③ 구독 로그인 확인 ④ dev:all + 도구 "항상 허용" 설정 | #6 일부 |
| 1 | ✅ | ★하이라이트 스파이크 (제품 성립 판정) | Claude Code·Codex **각각** 질문 시 3상태 하이라이트 표시(1층 무조건·2층 인용 시·**인용 없음/탈락 시 1층 유지+안내 표시 1케이스 포함**) + **kg_search 호출률**(10회 중 n회 — 유명 IP 질문 의도 포함, §1.14-6) + kg_cite 준수율 10회 중 ≥8 + **부정 대조**(그래프에 없는 사실 질문 — 빈 인용+고지 준수율) + 시드 적중 ≥80% + **질문→1층 표시 소요 실측 기록(10초 예산 대비)** → DECISIONS 기록(미달 시 §1.14 사다리). **판정 유효 범위 = 소규모·유명 도메인 기준**(규모 리허설은 슬라이스 2·7 측정) — **2026-08-27 완료(오너 판정 "승인.")**: Claude 표면 21문(M4 재판정 — `M4-REJUDGMENT.md`) + Codex 표면 21문(M5 — `M5-JUDGMENT.md` 합격선 **7/7 달성**) 양 표면 통과. 1층 크기 6문 kgid 수준 동일 = 검색 결과의 클라이언트 무관성 실증 | #6·#7 |
| 1.5 | ✅ | 엔진 스모크 | `codex exec`·`claude -p` 각 1회로 KG JSON 텍스트 수신, 플래그 세트(--output-schema 포함) 확정 → DECISIONS 기록. **사용자 액션**: 두 CLI 설치+로그인 — **2026-08-22 완료(합격선 6/6)**: 오너가 CLI 2종 설치·로그인, 각 엔진 1회 실호출로 KG JSON 수신·스키마 검증 통과(codex 6n·5r / claude 9n·15r). **정본 정정 2건** — --max-turns 부재 제거, --output-schema 구조적 비호환으로 미채택(TECH-SPEC v2.6) | #3 선행 |
| 2 | ✅ | 푸시 프로토콜·시각화 완성 (§5·§7) | POST /api/show·/api/highlight·/api/refresh + SSE(/api/events) 4종 메시지로 표시·해제·"N개 중 M개"·닫힘 감지·최신 상태 재생이 동작 — **2026-08-22 완료(합격선 5/5)**. 실측: `npm run hub:e2e` **8/8**(라우트 3종·SSE 순서·교차 type 400·상한 413·재생 규칙) + 브라우저 실왕복 `graph.show`·`graph.refresh` **2/2**. 프로토콜은 슬라이스 1이 함께 구현해 둔 상태였고, 이번 작업은 **검증기 저장소 승격 + 낡은 안내 2건 수리**였다. 상세 = `DECISIONS.md` 말미 | #7 |
| 3 | ✅ | S1 웹 크롤러 + 원장 | URL+상한 10 실행 → ≤10개 MD(파일명 규칙·프론트매터), 재실행 시 기수집 스킵·실패 재시도(성공 기준 1) + **실패 1건 이상의 성공 n·실패 m·사유·재시도 요약이 챗 응답으로 반환(성공 기준 8)** + **--force 강제 재수집 확인** + **robots.txt 차단 경로 스킵·요청 간격 검증** + ledger 단위 테스트(§1.12-1 이월분). **사용자 액션**: Jina 무료 키 발급 **2026-08-22 완료(합격선 8/8)** — 실크롤 readians.com 10/10·프론트매터 8필드 전건·멱등 재실행 시도 0건·robots 차단 실증·--force 새 파일 0개. 테스트 297→334. 실측이 잡은 결함 3건과 설계 공백 1건은 DECISIONS.md 말미 | #1 |
| 4 | ✅ | S2 문서 추출 | 디지털 PDF 1건+이미지 1건 → 외부 전송 없이 규칙 MD + extraction_quality 프론트매터(성공 기준 2). **착수 조건: 온라인 상태에서 최초 OCR 1회(언어팩 kor+eng 다운로드→ocr-cache 캐시)** — **2026-08-22 완료(합격선 7/7)**: PDF 449자 ok·2자 low·0자 empty 3단계, 한글 이미지 OCR 5줄 전건 정확(오탈자 0), 멱등 재투입 새 파일 0개, 테스트 334→343. 언어팩 캐시 완료(kor 2.2MB·eng 5.2MB) | #2 |
| 5 | ⬜ | S3 KG 생성기 + failover | Input→Generated/<stem>.kg.json + 두 엔진 각 1회 failover=false 검증(성공 기준 3) + 가짜 엔진 계약 테스트(§1.12-2)·resolveEngine 테스트 + 신규 유형 자동 등재 보고 + **--force 재생성 확인** + **챗 명령 1회의 다수 파일 연속 자동 호출 성립 여부 실측(Claude·Codex 각각 — 미성립 시 limit·타임아웃 조합을 README ⑬에 수치 확정)** + 준수율 측정(5건×2엔진)→DECISIONS. **선행: 두 클라이언트 MCP 타임아웃 상향 적용** | #3 |
| 6 | ✅ | S4 검수 워크플로 | 챗에서 review_list→3D 자동 표시→승인(Reviewed 이동)·반려(rejN+자동 재생성 1회·3회 보류) 왕복(**성공 기준 4 전반부** — 주입까지의 완결은 슬라이스 7) — **2026-08-23 완료(합격선 9/9)**: 도구 4종 구현, `npm run review:e2e` **9/9**(실 MCP stdio 왕복), 실 `data/` 6건 review_list 집계 + p04 43노드 **라벨 7종 화면 대조 전건 일치**, 테스트 387→**434**. **실사고 1건 적발·수리** — `kg_generate`가 문자열을 반환해 **챗에서 호출 자체가 불가능**했다(JSON-RPC -32602). 상세 = `DECISIONS.md` 말미 | #4 |
| 7 | ✅ | S5 주입기(전체 재빌드) | kg_rebuild 2회 연속 동일 결과(멱등) + 두 자료 병합 엔티티의 교차 하이라이트(성공 기준 6·7) + **승인분이 재빌드까지 수동 개입 없이 연결(성공 기준 4 완결)** + **소요 시간 기록(2분 예산 대비)** + Aura IN TRANSACTIONS 삭제 배치 — inject-example 씨앗 코드 흡수 — **2026-08-23 완료(합격선 6/6)**: `npm run rebuild:e2e` **8/8 실 Aura**(2회 연속 29·55 동일 · 11.2초/120초 예산 · 이중 방벽 삭제 `IN TRANSACTIONS OF 2000 ROWS`) + 실 DB에서 **2자료 병합 실증**(선착 속성 유지·보강·출처 2건) + `graph.refresh` → 3D 배너 확인. **미측정 2건은 오너가 p05·p02를 승인한 뒤 같은 날 마감**: 승인 2건 → `kg_rebuild` **39노드·59관계(5.7초)** 수동 개입 0 + `리디안솔루션` **병합 1건**(출처 2파일·관계 19개 수렴) + 검색 1회가 **p02 16경로 + p05 3경로를 함께 하이라이트**(교차 실증). 상세 = `DECISIONS.md` 말미 | #5 |
| 8 | ✅ | 부속·스키마 도구 | source_remove(모드 선택·Rejected 포함 완전 제거) + schema_get/update + kg_status 완성 — **2026-08-26 완료(합격선 7/7)**: 도구 **13종** `tools/list` 실왕복(직전 10종) · `toolShape` 13/13 핸들러 SDK 스키마 통과 · remove 격리 검증 18케이스(쌍둥이 전수·부분 실패 멱등·프론트매터 키 복원·rej 앵커) · schema 왕복 11케이스(실변경 시만 버전 +1·빈 지시문 거부) · kg_status 실환경 실측(Input 28·승인 6·Neo4j 132·207·buildId 일치·부분 성공 규정) · `hub:e2e` 9/9(GET /api/health 신설) · 기존 테스트 무수정(개정 명시분 toolShape·smoke 제외) 총 519→**559**. 3렌즈 반박 패널의 blocker 2건 반영(TECH-SPEC v2.12). 상세 = `DECISIONS.md` 말미 | #6 잔여 |
| 9 | ⬜ | README 셋업 가이드 | §1.15 목차 **17항**(v2.2 — AuraDB 절차·일시정지/30일 삭제·복구 절차(자격증명 재발급 포함)·7687·언어팩 고지 포함) 충족 + 클론 스모크 순서 재현(성공 기준 9). **사용자 액션**: Aura 콘솔 스크린샷 채집, GitHub 저장소명 최종 확정 | #8 |
| 10 | ⬜ | 통합 검증(MVP 완료) | 성공 기준 1~9 전 항목 체크(엔진 2종×클라이언트 2종 매트릭스) → Phase 4 진입 | 전체 |

## 테스트 이월 명시 (§1.12-1·2)

- resolveEngine 단위 테스트 → 슬라이스 5 / ledger 단위 테스트 → 슬라이스 3 / 가짜 엔진 계약 테스트(failover 포함) → 슬라이스 5.
- 스캐폴딩(슬라이스 0)의 몫은 shared 5종(naming·urlNormalize·normalize+kgid·kgSchemaValidate·renderSchema) — 완료.

## 완료 기록

- 2026-08-27 — 슬라이스 1 **완료(✅ — 오너 판정 "승인.")**: 제품 성립 판정 양 표면 통과 — Claude 21문(M4 재판정, 거짓 부재 0·시드 15/15) + Codex 21문(M5, 합격선 7/7 — 호출 21/21·인용 제출 21/21(verified 19·none 확정 2·탈락 0)·시드 15/15·E군 부정 대조 3/3(오너 판정)·3상태 231항 자동 대조 전항 통과·소요 오너 육안 "5~10초 안"·거짓 부재 역검증 0건). 정본 = `M5-JUDGMENT.md`. 이로써 판정축(M1~M5) 완결 — 잔여 슬라이스는 5(S3 생성기)·9(README)·10(통합 검증)
- 2026-08-21 — 슬라이스 0 스캐폴딩 완료 (브랜치 phase2-scaffolding, 검증 수치는 DECISIONS.md "스캐폴딩 전문가 패널 검수 반영 + 실행 검증 결과" 참조)
- 2026-08-21 — 슬라이스 1 **구현분 완료(🔨 — 스파이크 판정 대기)**: 푸시 허브(라우트 3종+SSE+보관·재생, 127.0.0.1)+mapper RKEntity 제외+3D 3상태 수신(kgid 색인·오버라이드 레이어·2층 파티클·HighlightPanel v2.2 문구·N/M·자동 전환) 구현, 검수 패널(4렌즈+반박 검증) 확정 13건 전량 반영(DECISIONS 참조), 테스트 248케이스(기존 148 무수정+신규 35)·린트, 실왕복 검증 = kg_search 실호출 `delivered:true` → 크롬 자동 전환·질문·citation 문구 DOM 확인 + 부정 대조("무잔"→"검색 결과 없음") + clear 소거 (커밋 1ef89f0). **잔여 = 스파이크 판정 진행 중: 실기록 정본 docs/spike-judgment-log.md (Claude 표면 11/21문 완료 · 8/22 Opus로 잔여 10문 → Codex 21문 → 집계·DECISIONS 기록)**
- 2026-08-21 — 슬라이스 0.5 **완료(✅)**: 사용자 액션 ② 양 클라이언트 실검증 — **Codex 데스크탑·Claude Code(GraphRAG_1st 루트) 각각에서 kg_status 실호출 성공**(등록·서버 기동·왕복·요약 전달 전 구간). ③ 구독 로그인 확인(양쪽 실호출로 증명). ④(dev:all+항상 허용)는 슬라이스 1 절차의 첫 단계로 이월. 코드분 상세: inject-example 실주입(AuraDB에 29노드·55관계, 2회 연속 동일 = 멱등 판정 통과, 인덱스 2종 ONLINE) + 도구 4종 실호출 검증(tools/list 4종 노출, "탄지로" **T2 cjk 부분 일치 실증**·"귀살대" T1, kg_cite 교집합 검증 verified/none/partial 전 경로, keywords 누락 재호출 안내, 무-throw 푸시 delivered:false 정상) + 평가 질문 세트 21문 작성(spike-eval-questions.md) + 전 테스트 213케이스 통과. **(후속: ②③ 8/21 실검증 완료, ④는 슬라이스 1 판정 진행 중 충족)**
