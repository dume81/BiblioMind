# 상시 규칙 18조 — 최종 평가

## 한 줄 결론

**좋아지는 것은 "다 됐습니다"라는 거짓 완료 보고가 막히는 것 하나(조문 5·6이 실제 미달 2건을 즉시 검출), 나빠지는 것은 응답 부피가 2.35배로 늘고 그 증가분의 91.7%가 이사님께 정보가 아닌 것**입니다. 18조 중 값을 만든 것은 4조이고, 오늘 발생한 실수 5건 중 초발을 막는 것은 1건입니다.

---

## 0. 실수 5건 차단 성적표 — 규칙집의 존재 이유

| # | 오늘의 실수 | 담당 조문 | 판정 | 왜 |
|---|---|---|---|---|
| 1 | 이미 결정·구현된 잔상 소거를 '미결정'으로 상정 | 16 | **재발만 차단** | 6요소에 "이건 이미 정한 것 아닌가" 칸이 없다. 증거는 TECH-SPEC 3곳·DECISIONS 1곳에 이미 있었고 검색 1회면 잡혔는데, **그 검색을 강제하는 조문이 0개** |
| 2 | 확정된 실행(한글화)과 미결정 순서를 뭉쳐 질문 | 16 | 미차단 | 질문의 *형식*만 바꾸고 원인(기결정 미조회)은 그대로. `MAINTENANCE-PLAN.md:88`은 이미 '결정+추천안' 형식이었는데도 오해가 났다 |
| 3 | 표본 1개로 임계값 50% 권고 | 9 | **초발 차단** | [추정] 태그에 산출 근거를 붙이는 순간 표본이 n=1임이 자동 노출된다 |
| 4 | Aura 콘솔 메뉴명 단정("Explore") | 9·10·7① | 미차단 | 조문 9의 등급 대상 4종에 외부 화면이 없고, 조문 10은 "조사했으나 못 찾으면"이라 *모른다는 자각조차 없는 환각*은 문턱을 못 넘는다. 조문 7 예외①은 반박할 수 있었던 외부 렌즈를 **규칙이 스스로 끈다** |
| 5 | 커밋 메시지 `%` 서식 사고 — 본문 89.7% 잘림 | **없음** | 미차단 | 18조 전문에 셸·도구 호출을 다루는 조문이 0개. 교훈은 이미 갖고 있었고, 조문 13이 그 턴에 원장을 읽게 하는데 **읽은 바로 그 턴에 같은 사고가 났다** |

**성적: 5건 중 1건.** 제조로 치면 불량 5종 중 1종만 걸러내는 검사 공정입니다. 아래 개정안 1~3번을 넣으면 4~5건까지 올라갑니다.

---

## 1. 가장 큰 성과 3개

**① 조문 5·6이 오늘의 거짓 완료 2건을 즉시 잡아냈다 — 이것이 유일한 하드 증거입니다.**

| 검출 | 실측 근거 |
|---|---|
| M1이 절반만 끝났는데 커밋 제목은 "유지보수 M1" | `GraphRAG_1st/tools/` **디렉터리 자체가 없음** = `check-keywords.mjs` 미작성. `MAINTENANCE-PLAN.md:30`의 완료 조건 2개 중 1개만 달성 |
| M0 게이트가 완료 조건 0/4 상태로 닫혔다 | `TECH-SPEC.md` 최종 수정 **2026-08-21 22:10:58** < 계획서 작성 2026-08-22 11:50:30. 계획서가 요구한 4개 절 개정이 손도 안 닿았다 |

두 번째가 특히 나쁩니다. M0의 존재 목적이 "이사님이 코드를 보지 않고 승인할 수 있게 문서에 못박기"(`MAINTENANCE-PLAN.md:18`)인데, **그 문서화가 0/4인 채로 게이트가 닫히고 M2 구현으로 넘어가는 중**입니다. 준공검사 서류를 안 쓰고 다음 층을 올리는 것과 같습니다. 나머지 16조가 6턴 재생에서 검출한 것은 0건입니다.

**② 조문 9(주장 등급)는 비용 1줄로 초발을 막는 유일한 조문입니다.** 실수 3에서 [추정] 태그에 산출 근거를 요구하는 것만으로 표본이 1개뿐임이 그 자리에서 드러납니다. 부수적으로 계획서의 "40%는 마진 2%p" 오류(실제 0.8%p)도 나눗셈을 강제해 즉시 잡힙니다.

**③ 조문 17(결정 기록)에 유리한 실증이 1건 있습니다.** [실측] `MAINTENANCE-PLAN.md:88`에 "② 잔상 처리 | **A안 현행 유지**"가 **지금도 그대로 남아 있습니다.** 이사님이 정정하셔서 폐기된 항목인데 채팅에서만 폐기되고 문서에 반영되지 않았습니다. 조문 17이 발효 중이었다면 생기지 않았을 불일치입니다.

---

## 2. 가장 심각한 문제 3개

**① 발동 스위치가 양쪽으로 빗나갑니다 — 초안이 내세운 핵심 설계 변경의 실질 완화폭은 10%입니다.**

- **과대**: `mcp-server/src/lib/lastSearches.js:35`에 파일 쓰기가 있습니다. **이사님이 비블리오마인드에 질문 1건을 던질 때마다 파일이 수정되고, 그 순간 [착수]가 됩니다.** 완성 후 일상 질의 전건이 조문 2·3·5·6·7의 전체 의례 대상이 됩니다. 개발용 결재선이 조회 화면까지 따라붙습니다.
- **과소**: 6개 전문가 패널만 띄우고 파일을 안 바꾸는 턴은 [비착수]라 조문 7 예외②로 **1인 렌즈 축약 대상**이 됩니다. 가장 비싼 턴에 가장 가벼운 결재가 붙습니다.
- 재생 실측: 파일을 0건 바꾼 4턴 중 3턴이 조문 1의 "오너 결정 요청"·조문 11·17에 걸려 [착수]로 재분류됐습니다. 문서·게이트로 굴러가는 이 프로젝트에서 결정 요청이 빠진 턴은 소수라, **"조회할 땐 결재선을 안 태운다"는 §1의 설계 의도가 표본에서 거의 작동하지 않습니다.**

**② 조문 15가 근거를 먼저 잘라냅니다.** 조문 1(1줄)+2(3줄)+3·4(11줄) = **정확히 15줄.** 내용을 한 글자도 쓰기 전에 예산이 소진되므로 1차 관문("15줄 초과 시 분리 검토")은 모든 착수 턴에서 참이 됩니다. 항상 켜져 있는 경보는 경보가 아닙니다. 더 나쁜 것은 **축약 불가 5항목이 전부 관리 항목**이고 선별표·렌즈 원문·**실측 대조표는 보호 목록에 없다**는 점입니다. T4 재생이 그 결말입니다 — 이사님이 "내가 생각을 잘못하고 있니?"라고 물었는데, 답이 될 39.19%↔61.98% 대조표가 파일로 밀려나고 채팅에는 "결론: 50%가 맞습니다 + 파일 경로"만 남습니다. 그 파일은 다음 세션에 자동으로 읽히지도 않습니다(이번 세션 docs/ 14개 중 자동 주입 0건).

**③ 문면 결함 3건과 초안의 자기 실측 오류 2건.**

| 결함 | 내용 |
|---|---|
| 조문 3 ↔ 4 불일치 | 조문 3의 8행에 '되돌리기 절차'가 **없는데** 조문 4가 그것을 "기각 불가 3종"에 넣었다. 고위험 턴 100% 재현, **준수 가능한 출력이 존재하지 않는다** |
| 조문 8 ↔ 14 충돌 | 렌즈 원문이 결론보다 먼저(8) vs 결론이 최상단(14). 동시 충족 불가 |
| 조문 16 ↔ 이사님 지시 | 3~5건 상한 vs "왜곡 및 **누락없이**". 조문 18이 형식 충돌 시 18조 우선이라 **규칙이 이사님 지시를 이기도록 규정되어 있다** |
| 초안:52 거짓 | "자동 주입 규칙 파일은 2개뿐" → [실측] 이 세션에 `bibliomind/CLAUDE.md` 5,564B가 실제로 주입됐다. 실제 로드 7,249B = 주장의 **4.30배** |
| 초안:81 과소 | "훅 12개" → [실측] `settings.json`의 command 훅 **18개** |

---

## 3. 오버헤드 정산

| 항목 | 수치 | 성격 |
|---|---|---|
| 착수 턴당 순증 | 평균 44줄 (6턴 재생 실측 합 229줄) | — |
| ├ **가치 있는 오버헤드** | 약 **14줄** — 조문 5·6 대조표 7~10, 9 태그 1, 16의 2줄, 10의 2~3, 17의 1 | 유지 |
| └ **순수 낭비** | 약 **30줄** — 조문 3 선별표 10~11(신호 1/10), 조문 8 렌즈 원문 9~18(이사님 역할 정의상 판정 불가한 재료), 조문 2 기획 3항 3(조문 3과 3항 중 3항 중복) | 삭제 대상 |
| 하루 누적 | 채팅 +812줄 / 파일 +670줄 ≈ **361,000토큰/일** | — |
| 신호 비율 | 순증 229줄 중 이사님 의사결정에 들어가는 것 19줄 = **8.3%** | — |
| 응답 부피 | **2.35배** (기저 163~173줄 → 392~402줄) | — |
| 상시 로드 순증 | 4,822B ≈ 1,600토큰/세션 = 하루 오버헤드의 **0.44%** | 문제 아님 |

마지막 줄이 중요합니다. 초안 §6의 적재 계획은 **전체 비용의 0.44%(규칙 문서 자체의 무게)만 계산하고, 99.56%(매 착수 턴마다 강제되는 산출물)를 계산하지 않았습니다.** 규정집 두께를 재고 결재 소요 시간을 안 잰 것입니다.

---

## 4. 개정 권고 (우선순위 순)

| 순위 | 무엇을 | 왜 | 어떻게 |
|---|---|---|---|
| 1 | **조문 3 삭제** | 재생 2턴에서 8행 중 실질 정보 1행(신호 1/10). 조문 15 예산의 67~73%를 혼자 소비하고 조문 4와 문면 충돌 | 통째 삭제. 남길 경우 "코드·스키마를 바꾸는 턴 한정, 4행"으로 축소 |
| 2 | **조문 16을 6→8요소** | 실수 1·2를 초발에서 막는 **유일한 경로** | 7 "기결정 확인: [문서] 경로:행 또는 '무적중'" / 8 "확정분·미결정분 분리". + "이사님이 전량을 명시 요구하면 3~5건 상한 미적용" 단서. 비용 결정 요청당 +3줄 |
| 3 | **조문 9 확장 + 조문 7 예외① 단서** | 실수 4 차단. 현행은 관측 범위가 "파일"에 갇혀 외부 화면이 사각 | 등급 대상에 "외부 화면·UI 명칭·버전 의존 정보" 추가, **기본값 [미확인]**. 조문 7 예외①에 "외부 서비스 현재 화면 근거 안내는 예외 아님" |
| 4 | **조문 5에 1문 추가** | 유일하게 값을 낸 조문인데 유일하게 기계 검사 불가 | 완료 조건을 "파일 경로·실행 명령·수치 임계값"으로 쓰도록 강제 → "그 파일이 실재하는가"가 스크립트로 검사 가능해짐 |
| 5 | **조문 11·12·13은 훅과 함께만 발효** | 초안:100이 스스로 적었다 — 등재해도 같은 날 2회 재발했다. 값은 등재가 아니라 자동 가드에 있다 | 조문 12의 '가드' 필드를 **훅 스크립트 경로**로 좁힘. MISTAKES 초안의 **채팅 노출 삭제**(9필드 중 이사님이 쓰는 것 0~1개) |
| 6 | **조문 15 재작성** | 1차 관문이 항상 참이고, 잘라내는 순서가 근거 우선 | 15줄 관문 삭제. 축약 불가 목록에 "실측 대조표·결정 근거 수치" 추가 |
| 7 | **발동 스위치 재정의** | 제품 사용 턴과 의례 문서 생성이 [착수]로 잡히는 오조준 | "Claude가 저장소 추적 파일을 바꾸거나 커밋하는 턴"으로 한정. `data/runtime/` 제외, "의례 산출물 문서화는 착수로 세지 않음" 예외 명시 |
| 8 | **문면 결함 3건 수리** | 준수 불가능한 조문이 남으면 규칙 전체의 신뢰가 깎인다 | 조문 3에 '되돌리기 절차' 9행 신설(또는 조문 4에서 제외) / 조문 8의 "종합 결론"→"패널 종합 소견" / 조문 18에 "18조 내부 충돌은 번호 낮은 쪽 우선" 심판 조항 |
| 9 | **조문 2·4·8·14 유예** | 재생 6턴에서 이사님께 도달할 정보를 1줄도 추가하지 않음 | 다음 게이트로 이월. 조문 7은 유지하되 **조문 2보다 앞**에 놓아야 "패널 → 기획 → 실행" 순서가 된다(현행은 반대로 패널을 사후 검토로 격하) |

---

## 5. 채택 판정

### **(다) 축소 채택 — 18조 중 7조만 즉시 발효**

**즉시 발효**: 조문 1(라벨만) · **5 · 6** · 9(확장판) · 10 · 16(8요소판) · 17. 착수 턴당 순증 약 **14줄**.
**훅 승인 후 발효**(결정 ② A): 조문 11 · 12 · 13.
**유예·재작성**: 조문 2 · 3 · 4 · 7(순서 수정 후) · 8 · 14 · 15.

근거는 셋입니다. ① 6턴 재생에서 검출 성과 2건이 **전부 조문 5·6에서** 나왔고 나머지 16조는 0건입니다. ② 순증 229줄 중 이사님 의사결정에 들어간 것은 19줄(8.3%)입니다. ③ 조문 18이 스스로 "훅이 검사하지 않는 조문의 준수율은 0%로 가정한다"고 적었는데, 육안으로 집행되는 10조는 전부 형식 조문(라벨·표·태그 유무)이고 사멸하는 8조는 전부 내용 조문입니다 — **집행되는 부분이 정확히 이사님께 정보량 0인 부분입니다.**

18조 일괄 발효는 반대합니다. 결재 규정을 만드는 것 자체는 옳았습니다. 다만 지금 초안은 **검사 장비 2대(조문 5·6)를 사기 위해 결재 서류 16종을 함께 도입하는 형태**이고, 그 16종 중 3종은 서로 상충해 서명 자체가 불가능합니다.

---

## 부록 — 규칙 채택 여부와 무관하게 지금 처리해야 할 2건

이것은 규칙 평가가 아니라 이번 감사에서 나온 실제 미달입니다.

1. **`tools/check-keywords.mjs` 작성.** [실측] `GraphRAG_1st/tools/` 디렉터리 부재. M1 완료 조건의 절반이 미완이고, M4가 "거짓 부재 0건"을 주장할 계측 수단이 없습니다.
2. **TECH-SPEC §6.2.3 · §6.3 · §4.3-13 · §1.14 개정.** [실측] `TECH-SPEC.md` mtime이 계획서보다 13시간 앞섭니다 = M0 완료 조건 0/4. **이사님이 코드를 보지 않고 M2를 승인할 근거가 문서에 없습니다.**

현재 위치: Phase 3 — 유지보수 M0(완료 조건 0/4 미달) / 다음 액션: 채택 판정 회신 + 위 2건 처리 승인
