# 유지보수 계획 — 슬라이스 1 스파이크 후속 개선 (2026-08-22)

> 오너 지시(2026-08-22): **B안 채택** — 개선 먼저 → 재판정 → Codex 표면 판정. "계획부터 수립하고 달성 목표와 Phase를 설정해 빈틈없이 처리할 것."
> 수립 근거: 전문가 패널 3렌즈(백로그 스코핑·M2 기술 설계·검증 설계, 2026-08-22). 판정 실기록은 `spike-judgment-log.md`, 결정 이력은 `DECISIONS.md`.

## 달성 목표 (이 유지보수가 끝났을 때 참이어야 하는 것)

1. **거짓 부재 0건** — 그래프에 있는 자료를 "없습니다"라고 답하는 경로가 제거되고, 그것이 결정론적 테스트로 증명된다.
2. **거짓 시드 차단** — 부분 문자열이 우연히 겹친 노드가 시드로 승격되어 인용 검증까지 통과하는 잠복 경로가 막힌다.
3. **도구 안내 무결** — 도구가 클라이언트에게 틀린 다음 행동·부적절한 경고를 주지 않는다.
4. **판정 유효성 보존** — 개선이 이미 통과한 21문의 판정을 되돌리지 않았음이 자동 검증으로 확인된다.
5. **프로세스 무결** — 반증된 가정이 문서에 등재되고, 판정 오염을 막는 운영 규칙이 명문화된다.

## Phase 구성

| Phase | 목표 | 포함 | 게이트 |
|---|---|---|---|
| **M0** 개선 설계 확정 | 오너가 코드를 보지 않고 승인/반려할 수 있게 변경 파일·행·임계값을 문서에 못박는다 | 설계 문서화 + 오너 결정 3건 | **필요** |
| **M1** 계측기 | 재판정이 "거짓 부재 0건"을 주장할 수 있는 측정 수단을 먼저 갖춘다 | `tools/check-keywords.mjs` + 골든 픽스처 추출 | 불요(제품 코드 아님) |
| **M2** 검색 정확도 수정 | 거짓 부재·거짓 시드 두 경로를 함께 막는다 | 질문 원문 앵커 복원 + T2 점수 하한 | **필요**(지표를 움직이는 유일 Phase) |
| **M3** 도구 문구 정정 | 도구가 주는 틀린 안내 제거 | kg_status 안내·과잉 인용 경고 문구 | 불요(M2 게이트에 동승) |
| **M4** 재판정 ✅ | 개선이 통과 판정을 되돌리지 않았음 + 거짓 부재 0건 실증 | 3단 재판정(아래) | **게이트 조건부 승인 완료(2026-08-22)** — 8/8 달성 |
| **M5** Codex 표면 21문 | 제품 성립이 클라이언트 1종 특성이 아님을 증명 | 21문 전량 + 단일 클라이언트 규율 | **필요** → 통과 시 main 병합 승인 |
| **Phase N**(별건) | 관계 유형 전면 한글화 | 자체 PROCESS 사이클 | M5 통과 후 착수 |

### 각 Phase 완료 조건 (실행해서 확인 가능한 것)

**M0** — TECH-SPEC 개정안 4곳(§6.2.3 앵커 복원 절차 / §6.3 단계 2 점수 하한 / §4.3-13 과잉 인용 문구 / §1.14 가정표 7행) + DECISIONS ADR 초안에 **변경 파일:행과 임계값 수치가 명시**. 오너 결정 3건 회신.

**M1** — `node scripts/check-keywords.js` 실행 시 픽스처의 keywords↔question을 자모 편집거리로 대조해 손상을 검출하고, 정상 키워드는 무경보(known-answer test). 골든 픽스처 `mcp-server/fixtures/golden-searches.json` + 키워드 프로브 `mcp-server/fixtures/keyword-probes.json` 커밋(대화 기록은 휘발 자산).

> **완료 조건 정정 (2026-08-22 실측 — 초안의 "기 발생 2건 재현 검출"은 달성 불가능이었다)**: 손상 2건 중 **검출 가능한 것은 1건**이다.
> - `일륨도` → **검출됨**. 정상 표기 "일륜도"가 같은 호출의 question("일륜도가 뭐야?")에 있다.
> - `혐귀` → **검출 불가로 확정.** 정상 표기 "혈귀"가 ① question에 없고 ② 그 호출 직전 어시스턴트 텍스트 2000자에도 없으며(모델이 자기 지식에서 직접 생성) ③ 그래프에도 "혈귀" 노드가 없어 그래프 대조 채널로도 잡히지 않는다. **비교 대상이 어디에도 존재하지 않으므로 어떤 오프라인 계측기로도 검출할 수 없다.** 결과 영향은 0(대상 자체가 그래프에 부재).
> ⇒ 개정 완료 조건: **검출 가능 1건 전건 검출 + 나머지 정상 토큰 32개 무경보 + 오경보 0.** 검출 불가 사례는 픽스처 `expectedDamaged[].detectable=false`로 명시해 "미검출"과 "검출 불가"를 구분한다.

**M2** — 6개 조건 전부:
1. `keywords=['일륨도']` + question에 '일륜도' → **시드 1건(일륜도:T1)** + 교정 사실 공개 (A3 반전)
2. `keywords=['혐귀']` + question에 '혈귀' → **여전히 시드 0건** (E1 부정 대조 불변 — 복원이 "없다"를 깨지 않음)
3. `keywords=['네즈코']` → 하가네즈카 호타루(0.6402 = 1위의 39.2%) **시드 탈락**, **시드 5건 → 4건 유지** *(2026-08-22 정정: 최초 "정탐 3건"은 오기 — 1.3561점이 서로 다른 두 노드에 중복 부여돼 "점수 종류 3개"를 "건수 3건"으로 잘못 옮겼다. 생존 kgid = `n_691d4dc82874a1c3`·`n_bf66ca5b78cc47f0`·`n_a45effd08fb519bd`·`n_1be68e8ea0ddbf02`, 탈락 = `n_69fdefdb99c1eb70`)*
   - **동반 효과(오너 승인)**: 탈락 노드를 경유해서만 도달하던 1홉 이웃도 함께 빠진다 — S07·M03 **13노드·21관계 → 11노드·18관계**(제거 노드 `n_69fdefdb99c1eb70`·`n_acec44b628d14f03`). 회귀가 아니라 설계된 결과이며 골든 픽스처에 kgid 단위로 선언한다.
4. **무개입 회귀** — 적중 키워드(탄지로·귀살대·우로코다키·전집중 호흡)의 시드 계층·1층 규모가 판정 기록과 동일(앵커 복원은 unmatched 분기에서만 동작)
5. question 부재(null) 시 현행 동작 무해 폴백
6. 기존 **248케이스 무수정 통과**

> ### M2 실행 결과 — 2026-08-22 배선 완료, **게이트 승인 완료**(오너 회신 "M2 승인" — DECISIONS.md 말미)
>
> 구현 순서는 `M2-DESIGN.md` §8의 0~8단계를 그대로 따랐다(계측 확장 → 순수 함수 → 배선 → 재캡처).
>
> | # | 완료 조건 | 임계값 | 실측값 | 판정 |
> |---|---|---|---|---|
> | 1 | 일륨도 + question '일륜도' → 시드 1건 + 교정 공개 | 시드 1(일륜도:T1)·1층 3n·2r | 골든 패스 B S19 = `일륜도:T1 restoredFrom:일륨도`·3n·2r·unmatched `[]`, layer1이 정상 표기 케이스 S03과 **완전 동일** | **달성** |
> | 2 | 혐귀 + question '혈귀' → 여전히 시드 0건 | 개선 전과 완전 동일 | 골든 S20 패스 A·B 모두 digest 9필드 **바이트 동일** | **달성** |
> | 3 | 네즈코 → 오탐 1건 탈락, 정탐 4건 유지 | 시드 5→4, 13n·21r→11n·18r | S07·M03 11n·18r / M05 13n·20r, 제거 노드·관계 kgid가 선언 집합과 일치, `재갈`·`도깨비` T1 시드 불변 | **달성** |
> | 4 | 적중 경로 무개입 | 나머지 21케이스 불변 | 패스 A·B 각각 변화 케이스 = 선언 집합뿐(A: S07·M03·M05 / B: +S19), **미선언 케이스에 새 kgid 추가 0건** | **달성** |
> | 5 | question 부재 시 무해 폴백 | 패스 A에서 S19 여전히 미적중 | 패스 A S19 `unmatched:['일륨도']`·0n·0r 불변 | **달성** |
> | 6 | 기존 테스트 무수정 통과 | 262 → 287(신규 25) | `npm test` **287 통과**, 기존 테스트 파일 **수정 0건**(git diff 기준), `npm run lint` 클린, `npm run mcp:smoke` 정상 | **달성** |
>
> - 수용 판정 자동 대조: `M2-DESIGN.md` §6-6 표를 기계 판정해 **33/33 통과**(개선 전 `golden-searches.json` ↔ 개선 후 `golden-searches-postM2.json`).
> - 사람이 보는 문구(§8 7단계) 실호출 확인: 상태가 **'부분 성공' → '성공'** 으로 반전, 요약에 재검색 공개 2줄 출력, 허브 푸시 `delivered:true`(3D 앱 패널에 질문 원문 표시 확인).
> - 결정론: 25케이스 × 2패스 × 2회 실행 **전건 일치**.
> - 관측 채널 신설: T2 하한에 잘린 후보가 있으면 stderr 한 줄 JSON(`[bibliomind] seed-floor …`) + `data.seedFloorCuts` — 슬라이스 2 재조정 데이터셋의 입력.

**M3** — kg_status 응답의 "다음 행동"에서 "0.5 스파이크 준비" 소멸 · 과잉 인용 경고가 비난형이 아닌 서술형 · 248케이스 무수정 통과.

> ### M3 실행 결과 — 2026-08-22 완료 (M2 게이트 승인에 동승)
>
> | # | 완료 조건 | 임계값 | 실측값 | 판정 |
> |---|---|---|---|---|
> | 1 | kg_status "다음 행동"에서 "0.5 스파이크 준비" 소멸 | 문자열 0건 | 실호출 요약 = `ROADMAP의 현재 슬라이스 진행 (이 도구의 실제 상태 수집은 슬라이스 8)`. 저장소 전체 `0.5 스파이크 준비` **잔존 0건** | **달성** |
> | 2 | 인용 밀도 안내가 서술형 | 비난형 문구 0건 | 실호출 요약 = `1층의 5건 중 3건을 인용했습니다 — 2층이 1층과 거의 같아 강조 대비가 낮습니다.` (TECH-SPEC §4.3-13 v2.3 문안과 일치). `과잉 인용 의심`·`OVERCITE` **잔존 0건** | **달성** |
> | 3 | 기존 테스트 무수정 통과 | 287 | **287 통과**, 테스트 파일 수정 0건, lint 클린, mcp:smoke 정상 | **달성** |
>
> **문턱 미도입 확인**: 1층 최소 규모 문턱(예: 10건 미만 억제)은 실측 오탐 3건 중 B3(13건)을 못 잡으므로 도입하지 않았다 — 비율 상수(0.5)는 그대로다.

**M4** — **합격선·실행 절차 전부 `M4-REJUDGMENT.md` §7이 정본**(v2, 2026-08-22). 이 줄은 요약이며 충돌 시 §7이 우선한다.

> ### ✅ **실행 결과 — 8/8 달성 · 게이트 조건부 승인(2026-08-22)**
>
> 문항별 실기록 정본 = `M4-JUDGMENT-LOG.md`. **재실행 불요.**
> 1단 챗 11문 **11/11** · 2단 서버 프로브 10문 **전건 무변경** · 3단 손상 프로브 2문(S19 복원 적중·S20 불변).
> 합격선 8개: 시드 **15/15** · kg_cite **11/11**(탈락 0) · 부정 대조 **3/3**(치명 실패 0) · 거짓 부재 **0** · 소요 **6.9초** · 회귀 **무변경** · 3상태 **121/121**(색 포함) · 호출률 **11/11**. 미달·미측정 **0건**.
>
> **승인 조건 1건 — 이행 상태**: 소요가 개선 전 **5.6초 → 6.9초(+1.3초)** 로 퇴행했다. 임계값 10초는 충족하므로 판정은 달성이나, **원인 규명을 "M5 이후 개선 큐"에 등재**하는 것이 조건이다 → **등재 완료**(아래 개선 큐 절). 지금 검색 로직을 건드리면 M4 기준선이 무효가 되므로 M5 전에는 손대지 않는다.

> **정본 단일화(2026-08-22)**: 이전 판은 이 문서가 합격선 정본이라 하고 M4-REJUDGMENT는 그 반대를 가리켜 **순환 참조**였고, 두 문서의 시드 임계값이 서로 달랐다(≥80% vs 14/15). 상시 규칙 8조 제3조대로 읽으면 상향된 임계값이 문면상 무효가 되므로, M4에 한해 **합격선 정본을 §7로 내린다.**
>
> 요약(8개): ①시드 적중률 **14/15 이상**(v1의 ≥80%=12문은 개선 전 실적 14/15보다 낮아 퇴행해도 통과했다) ②kg_cite 챗 11문 전건 제출 ③부정 대조 3/3(치명 실패 0) ④거짓 부재 0건 ⑤연속 대화 소요 중앙값 ≤10초 ⑥회귀 감시 ⑦3상태 11문/11문 ⑧kg_search 호출률 전건.

**M5** — ROADMAP 슬라이스 1 완료 조건 전 항목 + Codex 표면 별도 판정표.

## 패널 신규 발견 3건 (백로그에 없던 것)

1. **T1/T2/T3 점수 척도 불일치** — T1은 리터럴 1.0, T2는 Lucene 원점수(최대 1.63), T3은 0.5인데 전역 시드 절단이 **계층 구분 없이 원점수로 정렬**한다(`searchEngine.js:206-209`). 시드가 상한 15에 포화하면 **T2 오탐(1.0123)이 T1 정확 일치(1.0)를 밀어낸다.** 소규모라 미발현, 슬라이스 2 규모 리허설에서 즉시 발현. → **이번 범위 밖(요청에 소급되지 않음), 개선 큐 등재 + 슬라이스 2 관찰 항목.**
2. **한글화 실제 범위가 3배 이상** — 시드 15종이 아니라 **48종**(예시 KG 실측), 명명 규칙 하드게이트 2곳(`kgSchemaValidate.js:9→117`, `inject-example.js:70`)이 한글을 무조건 거부, **기존 테스트 2건 수정 강제**(`renderSchema.test.js:16-17`, `kgSchemaValidate.test.js:132·134`), 관계 kgid 55건 전량 변경 → 재주입. "기존 테스트 무수정 통과" 원칙을 만족할 수 없는 유일 항목 ⇒ **Phase N 별건 승격**(드롭 아님 — 오너 예고 항목).
3. **제안 임계값 2건이 실측과 어긋남** — T2 하한 40%는 오탐(0.6402 = 1위 1.6334의 **39.19%**)과 마진 **0.8%p**로 취약 → **50% 확정**(오탐 대비 10.8%p·최저 정답 62.0% 대비 12.0%p 양방향 여유). *(2026-08-22 정정: 이 줄은 최초 "마진 2%p"로 기재됐으나 실측 나눗셈 결과 0.8%p — 40%안은 초안이 적은 것보다 더 취약했다.)* 과잉 인용 문턱 10은 실측 오탐 3건 중 B3(13)을 못 잡음 → **문턱 도입 대신 문구만 서술형으로 수정**(1줄).

## 이번 유지보수에서 제외 (범위 확대 ≠ 빈틈없음)

| 제외 항목 | 처리 |
|---|---|
| 관계 유형 한글화 | **Phase N 별건 슬라이스** — M5 통과 후 착수(오너 예고 항목이므로 명시 편성) |
| 주제 기반 자율 수집 / "없음→수집 후보 자동 기록" | PRD 개정 필요(Phase 0 회귀) — ROADMAP 후보로 **이름만 등재**, 착수 안 함 |
| 잔상 처리 B·C안 | **A안(현행 유지, 코드 0줄) 확정 권고** — C안은 3D 표면 변경이라 재판정 범위가 넓어짐 |
| 다중 클라이언트 간섭의 **코드 수정** | 슬라이스 2(푸시 프로토콜) — **단, 판정 운영 규칙 3조항은 이번에 명문화**(가장 값싼 빈틈 메우기) |
| 임계값 정밀 튜닝 | 메커니즘 + 보수적 상수만. 수치 확정은 슬라이스 2 규모 데이터로 이월 |
| kg_status 실제 상태 수집 | 슬라이스 8 — 이번엔 문구 2줄만 |

## 재판정 범위 (M4) — 21문 전체 아님, 3단 구조

1. **챗 실사용 11문** — 개선 영향권(A3·C4·B2 계열 + 부정 대조 E군 + 호출률 D군)
2. **서버 프로브 회귀 6~9문** — 골든 픽스처와 대조하는 무개입 확인(챗 불요)
3. **신규 손상 프로브 2문** — 일륨도/혐귀 결정론적 주입

근거: 워터폴 단락 구조상 **T1 적중 키워드에는 T2 하한이 도달 불가능**(`searchEngine.js:164`) — 코드로 증명되는 무변경 경로는 챗 재실행이 불필요하다.

> **재판정 시 회귀로 오인하지 말 것 (M3 반영, 2026-08-22)**: 개선 후 요약에 **새 문구 2종**이 나타난다 — ① 손상 키워드 복원 시 `키워드 자동 재검색 …` 2줄(A3에서 발생) ② 인용이 1층의 50%를 넘으면 `1층의 n건 중 m건을 인용했습니다 …`(옛 "과잉 인용 의심"의 서술형 교체본). **둘 다 설계된 출력**이며, ②는 A3처럼 1층이 작은 문항에서 정상적으로 뜬다. 반대로 **사라져야 할 문구**는 `과잉 인용 의심`과 `0.5 스파이크 준비` 2종이다.

## 판정 오염 방지 프로토콜 (신설·필수)

1. 판정 중에는 **동시 1개 클라이언트만** 연결 — 다른 클라이언트는 MCP 미사용
2. 집계자·검증 프로브는 **판정 문항 사이에 실행 금지**(전/후에만) — 2026-08-22 C3 실사고
3. 위반 시 해당 문항 ④는 무효 처리 후 재실행

## 구현 함정 (패널 경고 — 반드시 준수)

- **T2 하한은 tier 확정보다 먼저 적용**할 것. 현재 `t2.records.length > 0`으로 tier='T2'를 먼저 굳히므로, 하한을 뒤에 끼우면 "tier=T2인데 records 0건"이 되어 **T3 폴백을 건너뛰는 조용한 미적중**이 생긴다. `if (kept.length > 0) { tier='T2'; ... }` 형태로 써서 kept가 비면 T3로 자연 강등되게 할 것.
- 앵커 복원은 **unmatched 분기에서만** 동작해야 한다(정상 적중 경로 무개입).
- 임계값은 **명명 상수로 노출**(`SEEDS_MIN_SCORE_RATIO=0.5`) — 슬라이스 2에서 재측정 조건 명시.

## 오너 결정 (M0 게이트) — 2026-08-22 전건 마감

| # | 결정 항목 | 결과 |
|---|---|---|
| ① | T2 점수 하한 임계값 | **50% 확정**(비율 하한, 명명 상수 `SEEDS_MIN_SCORE_RATIO`). 20종 전수 실측으로 확증 — 탈락 1건(오탐 39.2%), **정답 손실 0건**. 40%는 마진 **0.8%p**로 기각(위 초안의 "2%p"는 오기 — 0.6402/1.6334=39.19% 실측 나눗셈으로 정정), 80%는 정답(62.0%)을 잘라 기각 |
| ② | ~~잔상 처리~~ | **폐기 — 결정 대상이 아니었음.** 소거 3경로(새 검색 시 갈아끼움 §5.3 / 시드 0건 시 빈 1층 푸시 TECH-SPEC:1110·1553, E1 실증 / `highlight_clear` §4.3-14)가 **이미 확정·구현·실증**되어 있었다. 미결정이었던 것은 "시간 경과 자동 소거 타이머 추가" 1건이며 이미 비권장 판정 존재. 오너 지적으로 적발 |
| ③ | 한글화 범위 | **실행 확정 · Phase N 별건 · 순서는 유지보수 이후.** 실행 여부는 오너 예고(8/21)로 이미 확정돼 있었고 미결정은 순서뿐이었다. 순서 근거 = 관계 kgid 55건 전량 변경·재주입이 M4 기준선을 무효화 |
| ④ | 개선 전 기준선 캡처 | **완료**(GraphRAG_1st `3358353`) — 아래 M1 진행 상태 참조 |

### M0·M1 진행 상태 (2026-08-22 14:30 재감사 — **전건 달성**)

| 항목 | 완료 조건 | 실측 | 판정 |
|---|---|---|---|
| M0 오너 결정 | 결정 회신 | 4/4 마감 | **달성** |
| **M0 문서화** | TECH-SPEC 4곳 개정 + DECISIONS ADR | `TECH-SPEC.md` **v2.3** — §6.2.3 앵커 복원 / §6.3 T2 하한 / §6.4 상한 3행 / §4.3-13 인용 밀도 / §1.14 가정 7행 **전부 반영**(커밋 `fb3a9ea`) | **달성** |
| **M1 골든 픽스처** | `fixtures/golden-searches.json` | 25케이스·결정론 2회 일치·판정 기록 교차 대조 8/8 | **달성** |
| **M1 계측기** | **`GraphRAG_1st/scripts/check-keywords.js`** (경로 정정 — `tools/*.mjs` 아님) | `node scripts/check-keywords.js` → *"known-answer 통과 — 검출 1건, 오경보 0"*, 종료 코드 0. 입력 = `mcp-server/fixtures/keyword-probes.json`(24프로브·33토큰) | **달성** |

> **이력**: 13:30 자체 감사 시점에는 M0 문서화 0/4·M1 계측기 미작성으로 **미달 2건**이었고, "M0 게이트 마감"이라는 집계자 보고가 부정확했다. 14:00~14:10에 두 항목을 완료해 **전건 달성**으로 전환(커밋 `fb3a9ea`·`59ed6a3`). **새 세션은 이 둘을 다시 만들지 말 것.**

## 전문가 배치 (Phase별)

| Phase | 투입 패널 |
|---|---|
| M0 | (완료) 스코핑·기술설계·검증설계 3렌즈 |
| M1·M2·M3 | **구현 검증 패널** — 설계 준수·회귀 위험·오탐 대조 3렌즈(구현 직후 적대적 검증) |
| M4 | 판정 집계는 자동 도구 + 오염 프로토콜 준수 확인 |
| M5 | Codex 표면 결과와 Claude 표면 대조 패널 |
| Phase N | 한글화 개정안 설계 패널(명명 규칙·번역 대응표 48종·재주입 절차) |

## M5 이후 개선 큐 (2026-08-22 신설 — 흩어져 있던 등재분을 여기로 모음)

> 이 절이 "개선 큐"의 정본이다. 이전 절들이 *"개선 큐 등재"* 라고만 적고 목록이 없던 것을 수리했다. **여기 있는 것은 M5 통과 전에는 손대지 않는다** — 검색 로직을 건드리면 M4 기준선(8/8)이 무효가 되기 때문이다.

| # | 항목 | 근거·증상 | 등재 사유 |
|---|---|---|---|
| Q1 | **소요 퇴행 원인 규명** | 개선 전 연속 대화 중앙값 **5.6초 → 6.9초(+1.3초, +23%)**. 임계값 10초는 충족 | **M4 게이트 조건부 승인의 조건**(2026-08-22 오너 회신). 지목된 원인 **[추정]** = 앵커 복원이 `records.length === 0` 분기에서만 `matchKeyword`를 재호출하므로 **시드 미적중 문항에서만** T1→T2 왕복이 1회 추가. 확정 수단 = `seed-floor` stderr 로그에 단계별 소요를 붙여 복원 발생 문항과 미발생 문항을 분리 측정 |
| Q2 | **T1/T2/T3 점수 척도 불일치** | 전역 시드 절단이 계층 구분 없이 원점수 정렬(`searchEngine.js:206-209`) → 상한 15 포화 시 T2 오탐(1.0123)이 T1 정확 일치(1.0)를 밀어냄 | 소규모라 미발현. **슬라이스 2 규모 리허설에서 즉시 발현** |
| Q3 | **"어떻게/왜" 질문 유형의 구조적 공백** | M4에서 4문항 연속 같은 공백 — 일륜도 설명 속성 / 탄지로 계급·부대 / 도깨비화 원인·행위자 / 사부로 영감 직업 | 같은 `Event` 라벨인데 `기유가 네즈코에게 재갈을 물린 사건`은 행위자·대상·도구 완비, `네즈코의 도깨비화`는 행위자 부재 ⇒ **어떤 조건에서 행위자가 붙는가**가 추출 프롬프트 점검의 축 |
| ~~Q4~~ | ~~**M4 계측기 미수리 4건**~~ → **2026-08-22 해소(3건 수리 · 1건 강등)** | ~~①`--fresh` 문항 단위~~ **호출 회차 단위로 수리**(`A3#2`) / ~~②부재 역검증 정규식 오분류~~ **`scripts/check-absence.js` 신설 + 속성 계열 사전으로 수리**(9건 전건 사실·오탐 0) / ~~③파싱 실패 = 시드 0건~~ **A1-3에서 수리**(`parseFailed`) / **④`--since`는 두 도구 코드가 동일 — 기재 내용이 무엇을 가리켰는지 불명확해 `[미확인]`으로 강등**, 다음 판정 전 실측 | 도구·스크립트만 변경 — 제품 코드 무변경이라 M4 기준선 8/8 유지 |
| **Q5** | **나열을 소속 관계로 만든다 — 원문에 없는 추론** | p02 검수 실측: 원문 *"SAP GRC 구축 및 운영관리 수행 (S 전자, S* 그룹 외 다수)"* 는 **고객사 나열**인데 그래프는 `S 전자 --PART_OF--> S* 그룹`을 만들었다. 원문에 그 소속 관계는 **없다** | 재생성으로는 안 낫는 유형(원문 문장이 추론을 유도) ⇒ **추출 프롬프트(§2.1.5) 규칙 추가로 고친다**: *"괄호·쉼표 나열은 병렬 항목이다. 나열된 항목 사이에 소속·포함 관계를 만들지 말 것 — 원문이 명시한 경우에만."* 2026-08-23 오너 승인(**승인+기록**) |
| **Q6** | **같은 대상을 파일마다 다른 라벨·다른 이름으로 붙인다 → 병합 실패** · **2026-08-23 재빌드 후 실측 확정: 152노드 그래프에서 10개 대상이 갈라짐. 최악은 `리디안솔루션`(관계 28) ↔ `리디안솔루션(주)`(관계 34) — 회사 자신이 반으로 쪼개졌다** | 실측(Generated 6건): **병합 성립 12건 · 라벨 불일치로 분리 8건**. `SAP GRC`·`SAP BTP`·`SAP SAC`·`SAP GTS`·`SAP ESG`·`Cloud`가 `Concept`/`Object`로 갈렸고, `SAP ABAP/4`는 `Technique`/`Concept`, `R-TAPs`는 `Object`/`Work`. **이름도 갈린다** — `리디안솔루션` vs `리디안솔루션(주)`, `SAP BTP` vs `SAP Business Technology Platform` | 병합 키가 (라벨, 이름) 쌍이라 **같은 대상이 그래프에 둘로 존재**하게 된다. 대응 후보 = ①추출 프롬프트에 "기존 유형·정식 명칭 우선 재사용" 강화 ②재빌드의 **유사 이름 쌍 보고**(슬라이스 7 구현분)를 검수 루프에 편입 ③별칭 사전(후순위) |
| **Q7** | **시점(when)이 관계에 실리지 않는다** | p04(연혁) 실측: 원문 *"리디안솔루션(주) 설립 (본점: 서울시 종로구 삼봉로 57)"* → 그래프는 `LOCATED_IN {note:"본점"}`. **"설립 당시"가 없다.** 병합 결과 회사에 주소 3개(본사·연구소·본점)가 시점 없이 나란히 선다 | 연혁·이력 문서 전반에 걸리는 유형. 대응 후보 = ①추출 프롬프트에 "원문이 시점을 밝히면 관계 속성 `as_of`·`period`에 적을 것" ②현재/과거 구분이 필요한 관계는 Event 경유로 모델링 |
| **Q8** | **노드 속성이 챗에 도달하지 않는다 — 답변이 있는 사실을 "없다"고 한다** | `[실측]` `searchEngine.js:99` `pickSummary`는 ①**문자열 속성만** 본다(`typeof value !== 'string'` continue) ②그중 **가장 긴 것 1개**만 160자로 잘라 돌려준다. 회사 노드는 속성 **17개**를 가졌는데 챗이 받은 것은 `description` **하나**뿐. `founded_year=2016`은 **숫자라 원천 배제**됐고, `tel`·`fax`·`headquarters`·`representative`·`partner_level`·`services`도 전달되지 않았다. ⇒ 2026-08-23 실질의에서 답변이 *"설립 연도는 그래프에 없다"* 고 한 것은 **챗 입장에서는 정직한 보고**였다 — 도구가 주지 않았다. | **노드가 된 사실은 보이고 속성으로 남은 사실은 안 보인다.** 대응 후보 = ①`kg_search` 반환에 속성 화이트리스트(연도·수치·연락처 등) 추가 ②`kg_node_detail`류 조회 도구 신설(§6.3 경량화 계약과의 균형) ③추출 프롬프트가 중요한 수치를 노드로 승격. **주의**: §6.3이 경량화를 계약으로 두고 있어 무제한 확대는 챗 컨텍스트를 태운다 — 화이트리스트가 현실적 |
