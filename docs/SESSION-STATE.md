# 세션 상태 스냅샷 — 컨텍스트 압축(/compact) 대비

> 작성: 2026-08-21. **압축 후 이 문서 하나만 읽으면 전체 맥락이 복원되도록** 작성됨. 상세의 원본 진실은 각 문서.

## 제품과 문서 지도

**비블리오마인드(BiblioMind)** — Biblio(지식 집합소) + Mind(LLM의 지적 체계). 자료(PDF·이미지·웹)를 지식그래프로 만들어 **Neo4j AuraDB 클라우드**에 쌓고(원본 진실은 로컬 Reviewed/), 챗(Claude 앱/Codex) 질문 시 답의 근거 경로가 크롬 3D 앱에서 하이라이트되는 로컬 GraphRAG 도구. 기존 자산 = 워크스페이스 `GraphRAG_1st/`(3D 시각화 웹앱, github.com/dume81/GraphRAG_1st).

| 문서 (bibliomind/docs/) | 상태 |
|---|---|
| PRD.md | **v3.2** — 요구사항의 원본 진실 |
| TECH-SPEC.md | **v2.13** (2026-08-27 슬라이스 8.5까지 반영) — 설계의 원본 진실. v1은 TECH-SPEC-v1-backup.md(동결) |
| ROADMAP.md | 수직 슬라이스 0~10 — **0·0.5·1·1.5·2·3·4·6·7·8·8.5 ✅ 완료**(1은 M5 오너 승인으로 완결). **잔여 = 5(착수 대기 — 오너 A 결정·5-C 선행 충족)·9·10** |
| **M5-JUDGMENT.md** | **M5 판정 정본 — Codex 21문 합격선 7/7 완료(2026-08-27 오너 승인·§4-c)** |
| **PREVENTION-MECHANISM.md** | **재발 방지 기전 정본**(2026-08-22 신설) — 4기전·A1 대조표·검출력 천장 |
| DECISIONS.md | 모든 확정 결정의 ADR — 여기 없는 결정은 없다 |
| PROCESS.md | 전체 공정과 "현재 위치" 트래커 |
| COLLABORATION.md | R&R (사용자/Claude/사용자 전용 액션) |
| interview-round1.md | 인터뷰·웹 검증 기록 (역사 문서) |
| MAINTENANCE-PLAN.md | 유지보수 정본 — M0~M5 완료 조건과 실행 결과 대조표 |
| M2-DESIGN.md | M2 구현 설계 정본 (배선 완료) |
| **M4-REJUDGMENT.md** | **M4 재판정 실행표 — 사전점검·세션 운영·챗 11문·기록 양식·자동 대조·합격선** |
| spike-judgment-log.md | 개선 전 판정 실기록 21/21 (M4 대조 기준선) |

## 현재 위치와 다음 행동 — **2026-08-27 시점 (최신)**

> 이 절이 최신이다. 아래 "2026-08-23 마감 시점" 이하는 이력.

### 2026-08-26 상태 — Q10 완료 · Q7 재검수 게이트 대기

- **Aura 재개**: 3일 무쓰기 일시정지 발생 → 오너 Resume → 접속 정상. **관계 수 정정 [실측]**: 정상값은 **217**(콘솔·직접 쿼리·Reviewed 병합 계산 3중 일치) — 종전 기재 "223"은 병합 전(152노드) 수치의 오이월이었다.
- **Q10 ✅ 완료**(오너 A안 · GraphRAG_1st 커밋 `6c76a51` · TECH-SPEC **v2.10**): 속성 충돌 가시화 + 속성 승자 사전 `property_overrides`. 실 Aura 재빌드 2회 — **headquarters=새문안로 89**, 멱등 확인, 테스트 **497→519**. 첫 실행에서 **미해결 충돌 13건 발굴**(선착 유지 중 — 큐 후보).
- **Q7 ✅ 수리·수용**(오너 B안 · 재검수 승인 2026-08-26 · TECH-SPEC **v2.11** · 스키마 **v3**): 시점 규율 지시문 2줄(Event 단일화 — `_as_of` 접미는 반박 패널 블로커로 미채택) → p04 반려(rej1) → claude 재생성 → **오너 승인·재빌드 완료**. 그래프 = **132노드·207관계**(buildId `20260826T234342`). 신 p04는 시점 규율 준수(headquarters에 과거 값 0·설립 Event에 "설립 당시 기준" note·시점 22/22 보존). 대기 22건은 생성 시점부터 v3 적용.
- **Q6 재개봉 3건 → 해소**: 신 p04의 약칭 `Product/R-TAPs`·`R-DACS`·`K-Pack`을 canonical_entities 변형으로 등재(씨앗·런타임) — 병합 후 약칭 잔존 노드 0. **미해결 충돌 12건·유사 이름 쌍 8건이 재빌드 요약에 상시 노출 중**(품질 루프 — 신규 1건 `GRC K-PACK.full_name`은 약칭 병합이 드러낸 것).
- **신설 큐**: Q11(관계 속성 병합) · Q12(registerNewTypes updated_at) · Q13(review_reject 안내문). 정본 = MAINTENANCE-PLAN 말미(큐 가드에 "오너 승인 시 예외" 단서 추가됨).
- ~~다음 행동①②~~ **완료(2026-08-26 같은 날)**: p04 게이트 승인·집행 완료 + 오너 "8" 택일 → **슬라이스 8 완료(합격선 7/7)** — MCP 도구 **13종** 가동(`source_remove`·`schema_get`·`schema_update` 신설 + `kg_status` 실수치 완성), GET `/api/health` 신설, `hub:e2e` 9/9, 테스트 **559**. TECH-SPEC **v2.12**. 3렌즈 반박 패널 blocker 2건(쌍둥이 원장 전수 처리·kg_status 테스트 심) 반영. 신설 큐 Q14(first_seen 미기록)·Q15(reject 후 refresh 미푸시), Q13은 자연 해소.
- **M5 착수(2026-08-26 오너 지시 — 순서 M5 → D4)**: 실행표 정본 = **`docs/M5-JUDGMENT.md`**(신규 — M4-REJUDGMENT는 무수정 보존). 준비 완료 [실측]: 예시 그래프 전환(29·55, buildId `20260827T044116`) · dev:all 기동(허브 health ok) · Codex config.toml 등재 확인. **⚠ 지금 DB는 예시 그래프(귀멸의 칼날)다** — 판정 종료 후 `kg_rebuild` 1회로 실자료(readians 132·207) 복원, 그 전까지 실자료 질의 불가. 소요 계측기는 Codex 미지원[실측] → 육안 폴백. 이사님 몫: Codex 재시작 → kg_status 실수치 확인(29·55) → 21문(문항마다 이 창에 결과 공유 → 집계자가 ④ 자동 대조).
- **M5 완주(2026-08-27)**: Codex 표면 **21/21문** — 합격선 **6 달성 + 1 오너판정대기**(부정 대조 E군 — 판정 주체 규정). 정본 = `M5-JUDGMENT.md`(§3 사초·§3-b 검증 실행·감사 정정·§4-b 최종 판정표). 기록 감사 패널이 잡은 blocker·major는 §3-b로 해소. **DB는 실자료로 복원 완료**(132n·207r·headquarters 유지·buildId `20260827T200950`) — 예시 그래프 아님. 검증 스크립트 import 회귀 2건 수리(GraphRAG_1st `044f4f7`).
- **M5 완료 확정(2026-08-27 오너 "승인.")**: E군 만점 확정 + 인용 격차 5건 해명 수용 → 합격선 **7/7 = "완료"**(`M5-JUDGMENT.md` §4-c) → **슬라이스 1 ✅**(ROADMAP 갱신 — 판정축 M1~M5 완결). 기록 = DECISIONS.md 말미.
- **공개 push 완료(2026-08-27 오너 A안)**: 3렌즈 감사(blocker 0) → GraphRAG_1st ff 병합·push(origin/main=`044f4f7`, 39커밋 백업) + bibliomind push. **오너 방향 확정 2건**: ①최종 프로덕트는 `dume81/BiblioMind.git`으로 통합(방식은 미결 — 슬라이스 9와 묶어 설계) ②타 PC 클론 실행 가능 요건(정적 감사 blocker 0 — README가 남은 몫).
- **슬라이스 8.5 ✅ 완료(2026-08-27 오너 A안 → 같은 턴 완주)**: D4 해소 — `collect_web`·`collect_docs` 표면 2종으로 **설계 15종 전 도구 가동**. TDD·3렌즈 반박 패널(major 3 반영: robots 차단=실패 판정·blocked force 거짓 안내 제거·§4.1 5건 상한)·왕복 e2e 7/7·테스트 **580**·TECH-SPEC **v2.13**. 커밋 `0304334`(phase2-scaffolding 로컬 — **push는 오너 승인 대기**).
- **8.5 push 완료(2026-08-27 오너 승인)**: 증분 위생 스캔(실비밀 0) → ff 병합 → 양측 push·격차 0 — GraphRAG_1st `0304334` · bibliomind `b06e0a4`. **5-C 키 확보(종전 [미확인] 해소)**: Codex `tool_timeout_sec`(기본 60초 — **상향 필수**) [문서] / Claude Code `MCP_TOOL_TIMEOUT` 벽시계 기본 ~28시간·stdio 유휴 30분 — **limit 기본 1에서는 상향 불요** [문서, claude-code-guide 조회].
- **5-C ✅ 완료(2026-08-27 오너 집행)**: Codex `[mcp_servers.bibliomind]` `tool_timeout_sec=900` + 재시작 + `kg_status` 왕복 전항 기대값 일치(부분 성공 = 허브 꺼짐의 정직 보고). Claude Code는 기본값 충분 판명 — 상향 불요. 900초 실효는 슬라이스 5 첫 kg_generate가 겸검증(ROADMAP 반영).
- **슬라이스 5 ✅ 완료(2026-08-28)**: 재개 점검 3/3 → 오너 A(검증 최소분) 승인 → 준수율 **10/10 무교정**(codex 100~233초/건 — **5-C 사후 실증**: 미적용이면 전건 60초 타임아웃) → 오너 챗 실측 **양 표면 성립**(각 limit 2 단일 호출·Codex 4분36초 = 900초 실효 확정) → 실물 교차 검증 합치. 엔진 소모 14회. **현 상태: Generated 검수 대기 9건 · 미생성 13건 · 스키마 v8**(신규 유형 14종 — 남발 여부는 검수에서). 기록 = DECISIONS.md 말미.
- **검수 마감(2026-08-28 오너)**: 표본 2건 판정 — 승인 1(p08)·반려 1(p09 "테스트 목적" — 반려 계약 첫 실전 작동)·**대기 8건 보존**(상시 게이트). 재빌드 → **그래프 151노드·226관계**(buildId `20260828T132211`). 유사 이름 쌍 9건 상시 노출 중(AI Tech 계열 신규).
- **저장소 통합 ✅ 완료(2026-08-28 오너 B안 — 스냅샷 이식)**: BiblioMind.git main `9b9455d` = 코드 루트 + docs/ 단일 저장소. **새 클론 스모크 전항 통과**(클론→install→setup→테스트 584→도구 15종). 로컬 자산(data/·.env) 이관 완료 — 통합 폴더 kg_status 실환경 정확 보고. 코드 세부 이력 = GraphRAG_1st.git@719f280 보관. **⚠ 이후 세션은 `bibliomind/` 폴더를 루트로 연다**(.mcp.json 자동 적용 — 챗 도구 로드). 구 GraphRAG_1st/ 폴더는 동결.
- **README 전면 작성 완료(2026-08-28)** + 구 저장소 포인터 push(`e4d78ec`). 적대 사실 검증이 오류 1(NEO4J_DATABASE 누락)·부정확 2·사소 6을 잡아 전건 수리. **슬라이스 9 = 부분 완료 — 오너판정대기 1건**(§1.15-② "스크린샷 포함"만 미충족 — 텍스트 절차 완결, 스크린샷은 오너 채집 항목). Codex 경로는 오너가 교체 완료·**재시작 대기**(그때까지 Codex는 구 폴더 스테일 서버를 봄).
- **슬라이스 9 ✅ 완료(2026-08-28 오너 "수용")** — 스크린샷은 오너 채집·공유(Aura 콘솔 151n·226r 교차 일치 [실측·첨부 화면])하되 이메일·인스턴스 ID 노출로 README 미수록(마스킹 규칙). Codex 재시작 완료(오너).
- **슬라이스 10 🔨 진행 중**: 검증 계약 정본 = **`docs/MVP-VERIFICATION.md`**(성공 기준 1~9 × 이월 증거 × 신환경 재검증 R1~R5). 집계자분 완료 — R1 **7/7**·R5 **9/9**. **잔여 = 오너 R2~R4**(클라이언트당 1세트: kg_status → 교차형 질문 1문+3D 확인 → 검수 1건(한쪽)·생성 1건(한쪽)).
- **슬라이스 10 완주(2026-08-28)**: R1~R5 전부 완료 — **성공 기준 9/9 전항 달성**(정본 = `MVP-VERIFICATION.md` 최종 판정표). 오너 실측 화면 3장 교차 검증 — Codex 새 서버 확정(재빌드 시각 증거)·양 클라이언트 verified 인용·멱등 재빌드 재실증.
- **🎉 MVP 1차 완성 선언(2026-08-28 오너 "승인.")**: 전 슬라이스 0~10 ✅ · 성공 기준 9/9 · **Phase 4(품질/테스트) 진입**. 착수(8/21)부터 8일.
- **MVP 이후 마감분(2026-08-28)**: ①PRD 완성도 감사 — 공백 5건 귀속(핵심: **규모 성능 미실측 → Phase 4.5**) ②클론 재현 실검증 전항 통과(GitHub→install→584→15종→실DB 검색) ③README 검토(이슈 16) → **수리 12건 반영·push `c2bf115`·게시 확인**(차단 2·주의 5 전건 해소 — 엔진 CLI 설치 절·기본 엔진 고지·자동화 검증 기준 2026-08 등). setup 출력 900초 줄은 별도 칩 세션이 완결(`081fae3`).
- **다음 행동**: ① **오너 실전 전량 주행**(시나리오 전달됨 — 요지: ⑴collect_web readians.com 상한 30, "저장 0건"까지 반복 ⑵kg_generate 2건씩 반복(미생성 12+α, 건당 2~4분) ⑶review_list→N번 표시→승인/반려(약 21건 — 유사 이름은 반려 사유에 명칭 통일 지시) ⑷kg_rebuild 1회 ⑸의미 검수 질문 → 결과 공유 시 집계자 교차 검증) ② 완주 후 **Phase 4 착수 계획 상신**(4.1 엣지·4.3 코드 리뷰·4.4 보안·4.5 성능(규모 리허설 1순위)·실패/재시도/멱등성 — 게이트 = "배포 가능" 승인) ③ 개선 큐 9건·(선택) GraphRAG_1st 아카이브.

### 🔜 차기 세션 재개 절차 — 슬라이스 5 착수 (이 절이 신규 세션의 첫 지도다)

1. **문서 순서**: 이 문서 → `PROCESS.md` 현재 위치 → **`ROADMAP.md` 슬라이스 5 행(합격선 정본)** → TECH-SPEC §4.3-3(kg_generate 계약 — limit 기본 1·한도 전환·보류 해제)·§1.4(엔진 플래그·failover 규칙).
2. **환경 점검(재부팅 후 필수 3건)**: ① **Aura 일시정지 확인** — 마지막 쓰기 2026-08-27(재빌드)이라 무쓰기 3일이 지나면(≈8/30~) 일시정지된다. 일시정지면 **오너가 콘솔에서 Resume**(8/26 선례 — 데이터 무손실, 재개 후 접속만 확인) ② **엔진 CLI 로그인 상태** — `codex`·`claude` 재부팅 후 세션 만료 가능, 가벼운 호출 1회로 확인(만료 시 오너 재로그인) ③ `npm test` 1회 — 기준선 **580케이스**.
3. **슬라이스 5의 잔여는 검증·측정이 본체다**(생성기 코드·테스트는 기구현 — p04 재생성 실사용 이력): ⑴ 두 엔진 각 1회 failover=false 검증 ⑵ **챗 명령 1회의 다수 파일 연속 자동 호출 실측**(Claude·Codex 각각 — 오너 참여, 미성립 시 limit·타임아웃 조합을 README ⑬에 수치 확정) ⑶ 준수율 측정 5건×2엔진 → DECISIONS ⑷ 신규 유형 자동 등재 보고·--force 재생성 확인 ⑸ **900초 실효 검증(5-C 이월분)** — 첫 kg_generate 실호출이 겸한다.
4. **엔진 소모 계획을 착수 직전 오너 확인** — 준수율 측정만 엔진 호출 10회+α(구독 소모). 대상 데이터 = Input 28건 중 **미생성 22건**.
5. **로컬 미push 문서 커밋** — bibliomind main이 origin보다 앞서 있으면(이 세팅 마감 시점 3커밋) push 승인을 먼저 물어 백업을 닫는 것을 권함.

### 한 줄 요약

**2026-08-27 마감 시점 — 이 줄이 최신이다.** 슬라이스 **0·0.5·1·1.5·2·3·4·5A·5B·6·7·8 완료**(잔여 = 5·9·10). **슬라이스 1(제품 성립 판정) 오너 승인으로 완결** — Claude(M4 재판정)·Codex(M5) 양 표면 21문 통과. MCP 도구 **13종**·테스트 **559케이스**·TECH-SPEC **v2.12**. 개선 큐 해소 **Q4·Q6·Q7·Q8·Q10·Q13**(대기 Q1·Q2·Q3·Q5·Q9·Q11·Q12·Q14·Q15 — 정본 = MAINTENANCE-PLAN 말미). 전역 규칙 「코드 품질 3조」(이력은 `DECISIONS.md` 말미).

**그래프 = 이사님 실자료** — Neo4j에 readians **132노드·207관계**(승인분 6건 병합 · 2026-08-26 Q7·Q10 수리 반영. 이력: 138·217 → p04 재생성·약칭 병합으로 압축. 종전 기재 223은 병합 전 수치의 오이월로 정정됨). 예시 그래프(귀멸의 칼날)는 `node pipeline/bin/inject-example.js`로 복원 가능.

### ~~🔜 압축 후 첫 안건 — Q7·Q10 수리 방식 회신~~ (2026-08-26 이력 — 둘 다 수리 완료. 최신 재개 절차는 위 "차기 세션 재개 절차" 절)

오너가 *"Q7 수리 착수"* 를 지시해 **진단까지 마쳤고 실행은 안 했다**(스키마·코드 무변경). 진단 중 **더 큰 원인 Q10을 발견**해 안건이 갈렸다.

**증상**: 실답변이 스스로 신고 — *"회사 `headquarters`는 삼봉로 57인데 장소 노드로는 새문안로 89가 `본사`다. 어느 쪽이 현행인지 확정할 수 없다."*

**원인 2개** `[실측]`:
- **Q7(상류)** — p04(연혁)의 *"설립 (본점: 삼봉로 57)"* 에서 **"설립 당시"가 추출되지 않았다.** 원문에만 있어 **재생성 필수**(엔진 호출 듦).
- **Q10(하류)** — `headquarters`가 p04=삼봉로(과거)·p07=새문안로(현재)로 다른데, §2.3.2 병합이 **파일명 오름차순 선착 우선**이라 `p04 < p07`로 **과거 값이 이겼다**. **코드로 고칠 수 있고 엔진 호출 0회.**

**오너 회신 필요 (A/B/C 택1)**: **A. Q10만 먼저(엔진 0회·추천)** / B. Q7만 먼저(재생성 1~2회) / C. 둘 다. 상세·미채택 근거는 `DECISIONS.md` 말미 *"Q7 수리 착수"* 항목.

> **B·C의 위험 1건**: 재생성은 **Q6를 다시 열 수 있다** — 새 표기 변형이 나오면 정본 사전(`data/schema.json`의 `canonical_entities`)에 추가해야 한다.

> **백업 상태 [실측] 2026-08-23**: `bibliomind`는 **`dume81/BiblioMind.git` main에 push 완료**(공개 저장소 · 원격=로컬 `cdc8854` 일치 · 차이 0). ⚠️ **`GraphRAG_1st`의 `phase2-scaffolding` 브랜치는 여전히 백업 없음** — origin에 올라간 적이 없다(no upstream). 코드 전량이 이 PC에만 있다. **오너 결정 대기**: 보관용 `GraphRAG_1st.git`에 브랜치 push할지. 상세는 `DECISIONS.md` 말미 *"공개 push 실행"* 항목.

> **⚠️ 세션 시작 시 확인 — superpowers 설치됨 [실측] 2026-08-23**: `superpowers 6.3.0`(스킬 14 · SessionStart 훅 1 · 상시 ~688토큰)을 user 범위로 설치·활성화했다(`obra/superpowers-marketplace`). **설치 세션에서는 미적용이었고 재시작 후부터 발동**한다. 이 세션에서 스킬이 보이면 적용된 것이다 — `using-superpowers`가 *"어떤 응답보다 먼저 스킬을 발동하라"* 를 주입하는데, **전역 `CLAUDE.md` 최우선 규칙이 우선한다**(로딩 순서상). 거슬리면 `claude plugin disable superpowers` 1회. 상세는 `DECISIONS.md` 말미 *"코드 품질 5조 축소 + superpowers 설치"* 항목.

**이사님 몫(대기)**: ①**5-C** MCP 타임아웃 상향 + 챗 연속 호출 실측 ②**M5** Codex 데스크탑 21문. 둘 다 1차 완성 선언 전 필수.

### 오늘(8/23) 한 일 요약 — 압축 후 재작업 금지

| 항목 | 결과 |
|---|---|
| 슬라이스 6 (검수 워크플로) | 도구 4종 · `npm run review:e2e` **9/9** |
| 슬라이스 7 (주입기) | `kg_rebuild` · `npm run rebuild:e2e` **8/8 실 Aura** · 합격선 **6/6** |
| 검수 | **6건 전건 승인**(전부 *승인+기록*) → 재빌드 152→138노드 |
| **Q6** 엔티티 분리 | **정본 엔티티 사전**(`canonicalize.js`·TECH-SPEC v2.8) — 갈라짐 10→**0건** |
| **Q8** 속성 미전달 | `pickSummary`→**`nodeFacts`**(TECH-SPEC v2.9) — 회사 속성 1→**19개**, **거짓 부재 0건 확정** |
| 시각화 배너 결함 | `graph.show`가 재빌드 배너를 안 지우던 것 수리 |
| **실사고 1건** | `review-e2e`가 빈 `Reviewed/`로 재빌드해 **실 DB를 비웠다** → 안전핀을 스크립트 시작 시점으로 이동. 복구 완료 |
| **아키텍처 도해** | `TECH-SPEC` 3.2절 뼈대 + **코드 실측 현황도**를 excalidraw로 작도 · 검증 라운드에서 문자열 **8건 수정** |

> **⚠️ `npm run rebuild:e2e`는 지금 실행하지 말 것** — DB를 **예시 그래프로 덮어쓴다.** 복구는 `kg_rebuild` 1회(손실 없음).

### 아키텍처 도해 — **완료 (2026-08-23, excalidraw)**

`TECH-SPEC.md:802`(3.2절)을 뼈대로 쓰되 **상태 표기는 전부 코드 실측**으로 채운 **현황도**를 그렸다(설계도가 아니다 — 미구현·우회 경로를 지우지 않고 그림에 남겼다).

- **excalidraw MCP는 `/clear` 후 세션에서 정상 로드된다 [실측]** — `create_view`·`read_me` 2종. **한글 렌더링 정상**(오너 확인으로 `[미확인]` 해소).
- **체크포인트**: `909e033aff754a58b4`(1차) → `beda004a780643f9aa`(2차) → **`492ee99bd6964f6593`(현재)**. 다시 그릴 때 전체 재작도 불요 — `{"type":"restoreCheckpoint","id":"492ee99bd6964f6593"}` 로 이어 붙이고 바꿀 요소만 `delete` + 재추가한다.
- **도해가 드러낸 구멍**: `collect_web`·`collect_docs` 미구현 → **자료 수집만 터미널 전용**(`pipeline/bin/collect.js`). *"챗이 조종석"* 원칙에 뚫린 유일한 구멍이다.
- 검증 라운드 **문자열 8건 수정** — 용어 정본 위반 2 · 조사 띄어쓰기 1 · 라우트 누락 2 · 기타 3. 상세 표는 `DECISIONS.md` 말미 *"도해 검증 라운드"* 항목.

#### 도해에서 나온 후속 4건 — **넷 다 이번에 고치지 않았다** (도해 요청 범위 밖)

| # | 발견 | 근거 | 처리 제안 |
|---|---|---|---|
| **D1** | `TECH-SPEC` 3.2절의 **`:5173`은 근거가 없다** | [실측] `visualization-3d/vite.config.js`에 `server.port` **미지정** · 저장소 `grep 5173` **적중 0건**. Vite 기본값일 뿐이라 포트 점유 시 5174로 밀린다. (`:8787`은 `localServer.js:12`+프록시로 **근거 있음**) | 3.2절에 *"Vite 기본값"* 단서 1줄 |
| **D2** | `mcp-server/src/index.js:41` **주석이 낡았다** | [실측] *"나머지 **6종**"* → 실제 잔여 **5종** | 주석 1줄 수정 + 잔여 5종 명시 |
| **D3** | **정본과 코드가 어긋남** — 3.3절은 *"MCP 서버는 로직 보유 금지"* 인데 **검색 로직이 `mcp-server/src/lib/searchEngine.js`에 있다** | [실측] `kgSearch.js`가 `../lib/searchEngine.js`를 import | 코드를 옮길지 / 3.3절을 고칠지 **오너 결정 필요** |
| **D4** | ⚠️ **`collect_web`·`collect_docs`를 소유한 슬라이스가 없다** | [실측] `ROADMAP.md`·`MAINTENANCE-PLAN.md` 전문 `grep` **적중 0건**. 슬라이스 3·4는 ✅ 완료지만 그 합격선이 **CLI 실행 기준**이라 MCP 도구 표면을 요구하지 않았고, 슬라이스 8은 `source_remove`+`schema_get/update`+`kg_status`만 담는다 [문서] `ROADMAP.md:25`. ⇒ 두 도구는 **의도적 유예가 아니라 로드맵의 구멍** | ROADMAP에 귀속 슬라이스 **신설 또는 8번에 편입** — **오너 결정 필요** |

> **어휘 확정 (2026-08-23)**: **`미구현`** 을 쓴다. 한때 쓰인 `미실장`은 프로젝트 어휘가 아니었다 — [실측] 문서 계수 미구현 **18회** vs 미실장 **2회**(2회 모두 같은 날 집계자 기재). 코드 품질 3조 제2조(선언된 이름을 취향으로 바꾸지 않는다).

### 개선 큐 현황 (정본 = `MAINTENANCE-PLAN.md` 말미)

| # | 내용 | 상태 |
|---|---|---|
| Q1·Q2 | 소요 퇴행 · 점수 척도 | 대기(M5 전 손대지 않음) |
| Q5 | 나열→소속 추론 | 대기(재생성 필요) |
| **Q6** | 엔티티 분리 | ✅ **수리**(합격 5/6 · `Concept` 편중 44%만 미달) |
| **Q7** | 시점 소실 | **진단 완료·실행 대기** |
| **Q8** | 노드 속성 미전달 | ✅ **수리·수용** |
| Q9 | 관계 속성 미전달 | 대기 — **M4 골든 기준선 재설정 선행** |
| **Q10** | 병합 선착 우선에 시간 개념 없음 | **신규·진단 완료** |

### ✅ 슬라이스 6 완료 (2026-08-23) — 아래 브리핑은 이력으로 보존

**도구 4종 가동 중**: `review_list` · `review_show` · `review_approve` · `review_reject`. 검증기 = `npm run review:e2e`(**9/9**, 실 MCP stdio 왕복·엔진 호출 0회). 테스트 387 → **434**.

**이 슬라이스가 잡은 실사고**: `kg_generate`가 문자열을 반환해 **챗에서 호출 자체가 불가능**했다(JSON-RPC `-32602`). `mcp:smoke`는 `tools/list`만, 단위 테스트는 도메인 함수만 봤기 때문에 아무도 못 잡았다. 이제 `toolShape.test.js`가 **9종 전체 핸들러를 실제로 호출해 SDK 스키마로 검증**한다.

**부수 수리**: 산출물 `meta`(§2.2)를 생성기가 안 찍고 있었다 → 수리. 기존 6건은 `engine: null`로 **정직 표시**(소급 기입하지 않음).

**슬라이스 7로 넘긴 것**: 승인분 반려 시의 자동 `kg_rebuild`(§4.3-7 ②) — 지금은 *"Neo4j에 아직 남아 있습니다"* 고지만 한다.

### 🔜 슬라이스 6 착수 브리핑 (완료 — 이력)

**완료 조건 [문서] `ROADMAP.md:23`**: *"챗에서 `review_list` → 3D 자동 표시 → 승인(Reviewed 이동)·반려(`rejN` + 자동 재생성 1회 · 3회 보류) 왕복 — 성공 기준 4 전반부(주입까지의 완결은 슬라이스 7)"*

**착수 조건은 이미 충족**:

| 전제 | 상태 |
|---|---|
| 검수 대상 | **`data/Generated`에 6건**(claude 생성, 실패 0) · 남은 대기 22건 |
| 3D 표시 경로 | 슬라이스 2에서 **`graph.show` 실왕복 검증 완료**(허브 e2e 8/8) |
| 원자 이동 | `shared/src/atomicWrite.js` 있음. Generated→Reviewed는 같은 볼륨 rename |
| 반려 후 재생성 | `generateKg`(5-A) 재사용 — 가짜 엔진으로 시험 가능 |

**만들 것**: MCP 도구 4종 — `review_list`(§4.3-4) · `review_show`(→ `/api/show` 푸시) · `review_approve`(Reviewed 이동 + `reject_count` 리셋) · `review_reject`(`<stem>.kg.rejN.json` + 자동 재생성 1회 + `reject_count>=3` 보류).

**설계 정본**: `TECH-SPEC.md` §4.3-4~7(도구 표면) · §2.4.1(폴더 상태도·스킵 판정) · §2.4.2(`rejN` 파일명) · §2.4.4(`reject_count` 리셋 규칙) · §5.2 `graph.show`(푸시 스키마).

**주의 2건**: ①**`review_approve` 성공 시 `reject_count`를 0으로 리셋**한다(§2.4.4 v2 확정) ②`held`는 `reject_count>=3`의 **파생 값**이지 저장 상태가 아니다.

### 재개 절차 (압축 후 이 순서로)

**0. `cd GraphRAG_1st && npm run setup`으로 Aura 활성부터 확인한다.** Free 인스턴스는 **3일 무쓰기면 자동 일시정지**되고 **마지막 쓰기가 2026-08-23 `kg_rebuild`**다(2026-08-22 하루 동안 Aura 쓰기 작업 **0건** — 검색은 읽기라 시계를 되돌리지 않는다). ⇒ **8/24 이후 재개하면 첫 단계부터 막힌다.** 일시정지 시 Aura 콘솔에서 resume 또는 `node pipeline/bin/inject-example.js` 재실행(멱등).

**0-b. 개발 서버는 이미 기동 중일 수 있다** — 2026-08-22 마감 시점 실측: 허브 **8787**·Vite **5173** LISTENING(PID 3076·3080). `npm run dev:all`을 무턱대고 다시 돌리지 말고 포트 점유를 먼저 확인할 것. `EADDRINUSE`는 고장이 아니라 **이미 켜져 있다는 뜻**이다.

**0-c. 엔진 CLI는 설치·로그인 완료** — `claude 2.1.239`(`loggedIn:true`) · `codex-cli 0.149.0`(`auth configured`). **한도도 남아 있다**(2026-08-22 오너 확인: Claude 5시간 12%·주간 80%, GPT 52% 남음).

1. **이 절(한 줄 요약 ~ 재개 절차)만 읽으면 이어갈 수 있다.** 문서 뒤쪽의 슬라이스별 절은 **이력**이며, 그 슬라이스를 다시 볼 때만 읽는다.
2. 상시 규칙이 **8조로 발효 중**이다 — 워크스페이스 루트 `CLAUDE.md` 말미 절 + `~/.claude/CLAUDE.md`(사용자 전역 최우선 규칙). 매 턴 `[착수]`/`[비착수]` 라벨, 완료 보고는 대조표, 주장에는 `[실측]`·`[문서]`·`[추정]`·`[미확인]` 등급. **2026-08-22 추가: 근거 확보 의무** — 기억이 아니라 1차 출처(공식 문서·`--help`·레지스트리·설정 원문)로 답한다.
3. **M4까지 끝났고 게이트도 조건부 승인됐다.** 실기록 = `docs/M4-JUDGMENT-LOG.md`(8/8). **재실행 불요.** 개선 큐(`MAINTENANCE-PLAN.md` 말미 Q1~Q4)는 **M5 통과 전에 손대지 않는다** — 검색 로직을 건드리면 M4 기준선이 무효가 된다.
4. **다음 행동 — 이 순서가 정본이다 (2026-08-23 갱신)**: ① 위 *"압축 후 첫 안건"* 의 **Q7·Q10 수리 방식 회신**(대기 중) → ② 슬라이스 **5**(S3 생성기 완결 — 선행이 **5-C** MCP 타임아웃 상향) 또는 슬라이스 **8**(부속·스키마 도구) 중 택일 → ③ **D4 `collect_web`·`collect_docs` 귀속 결정**(위 *도해에서 나온 후속* 표). M5·5-C는 오너 몫으로 defer 중이며 슬라이스 8은 그것과 무관하게 진행 가능하다. **종전 문면(*"다음 행동은 슬라이스 8 착수 여부 회신"*)은 Q7·Q10 안건이 생기기 전 것이라 갱신했다** — 같은 문서 안에서 "다음 행동"이 서로 달랐다.
5. **커밋 전 가드 훅이 상시 가동 중**이다(`.githooks/pre-commit` — `npm run setup`이 활성화). 커밋이 거부되면 고장이 아니라 **가드가 회귀를 잡은 것**이다.
5. **재발 방지 기전은 설계·실행·기록까지 끝났다** — 오너 결정 3건 전건 소진, 정본 = `docs/PREVENTION-MECHANISM.md`. **커밋 전 가드 훅이 상시 가동 중**이다(`.githooks/pre-commit`, `npm run setup`이 활성화). 커밋이 거부되면 고장이 아니라 **가드가 회귀를 잡은 것**이다.

**M5 준비 상태 — 2026-08-22 마감 시점 실측 (전부 녹색)**

| 항목 | 실측 |
|---|---|
| 허브 8787 · Vite 5173 | 둘 다 **LISTENING**(PID 3076·3080) — `npm run dev:all` 재실행 불요 |
| Codex MCP 등록 | `%USERPROFILE%\.codex\config.toml`에 `[mcp_servers.bibliomind]` **등재됨** |
| Aura | **접속 성공**(`npm run setup`) — 3일 시계는 쓰기 시점 기준 |
| MCP 서버 코드 | M4 판정 시점과 **동일**(A1은 tests·scripts·fixtures만 변경, `searchEngine.js` 바이트 동일 확인) |

**M5 시작 전 1회**: Codex 데스크탑을 **재시작**한 뒤 `kg_status`의 "다음 행동"이 `ROADMAP의 현재 슬라이스 진행 (이 도구의 실제 상태 수집은 슬라이스 8)`인지 확인한다(M4-REJUDGMENT §0-7의 서버 버전 확인 — MCP 서버는 클라이언트 기동 시 spawn되므로 낡은 프로세스가 붙어 있으면 M3 문구 수정 전 동작을 보게 된다).

### M2 구현 — **배선 완료 (2026-08-22 · 커밋 `532b151`)**

M2-DESIGN §8의 0~8단계를 순서대로 이행했다. **재작업 금지 — 아래는 이미 저장소에 있다.**

| 단계 | 대상 | 결과 |
|---|---|---|
| 1 | `scripts/capture-golden.js` 2패스화 | `--out` · `PROBE_QUESTIONS`/`anchorQuestion` · `digest`의 조건부 `restoredFrom` · `anchoredCases` 신규 필드. **코드 변경 전에** 캡처해 기존 `cases` 25건 전건 동일 확인(계측 확장 무오염 자체 증명) |
| 2 | `shared/src/hangul.js` | 이미 완료(커밋 `59ed6a3`) — shared 64케이스 |
| 3 | `anchorRestore.test.js` #1~#19 | mcp-server 13 → 32 |
| 4 | `searchEngine.js` | `matchKeyword` 헬퍼 추출 + T2 하한(tier 확정보다 먼저 적용) + `seedFloorCuts` + `question` 시그니처 + `records.length===0` 분기에서만 앵커 복원(복원어는 T1→T2까지, T3 배제) + `seedsReport`의 조건부 `restoredFrom`. `seedFloor.test.js` 4케이스 |
| 5 | `kgSearch.js` | `question` describe 개정·전달 · `restorationLines()` 신설 · stderr 한 줄 JSON 관측 로그 · `data.seedFloorCuts` 조건부. `anchorRestore.test.js` #20·#21 → **287케이스** |
| 6 | 재캡처·대조 | `golden-searches-postM2.json` 생성 → §6-6 수용 판정표 **33/33 통과** |
| 7 | 문구 육안 확인 | 실호출로 상태 **'부분 성공'→'성공'** 반전, 재검색 공개 2줄 출력, `delivered:true` |

**대조표는 `MAINTENANCE-PLAN.md`의 "M2 실행 결과" 절이 정본**(완료 조건 6/6 달성).
**픽스처 2종의 역할**: `golden-searches.json` = 개선 **전** 기준선(M4까지 보존 — 덮어쓰지 말 것, 재캡처는 반드시 `--out`) / `golden-searches-postM2.json` = 개선 **후** 실측.

### 오너 결정 — 전건 마감 (다시 묻지 말 것)

| 결정 | 결과 |
|---|---|
| T2 점수 하한 | **50%** 확정. 20종 실측에서 탈락 1건(오탐 39.2%), **정답 손실 0** |
| 잔상 처리 | **결정 대상 아님** — 소거 3경로가 이미 확정·구현·실증. 집계자 오상정을 오너가 적발 |
| 관계 한글화 | **실행 확정 · Phase N 별건 · 유지보수 이후.** 48종 중 42종(87.5%) 1회성이라 "정리 + 번역" 공사 |
| 기준선 캡처 | **완료**(GraphRAG_1st `3358353`) |
| 일륜도 동반 소실 | **승인** — 오탐 제거 시 그 노드 경유 1홉이던 일륜도도 1층에서 빠짐(13n·21r → 11n·18r) |
| 2글자 키워드 복원 제외 | **승인**(`ANCHOR_MIN_KEYWORD_CHARS=3`). 2글자 허용 시 마음→마을 등 오복원 5종 |
| 복원 실패 표기 | **표기 안 함**(현행 유지) — E1 부정 대조 표면을 흔들지 않기 위함 |
| 상시 규칙 | **축소 7조 채택 → 전문가 투입 제1조 신설로 8조** |

### 남은 게이트·이사님 몫

- ~~M2 게이트~~ 승인 완료 · ~~M4 재판정~~ **완주(8/8)** → **M4 게이트 승인 대기** · **M5 Codex 21문 → main 병합 승인**(공개 저장소, 이사님 승인 필수)
- **이사님이 직접 해야 하는 것**: Codex 데스크탑에서 21문 판정 실행(M5) / ~~`codex`·`claude` CLI 설치~~ **2026-08-22 완료** / ~~Jina 키 발급~~ **불필요 확인**(무키 저율 동작 — `TECH-SPEC:182`) / ~~GitHub 저장소명 확정~~ **완료**(`dume81/BiblioMind.git`)

### 엔진 CLI — **설치·로그인 완료 (2026-08-22)**

| CLI | 버전 | 로그인 | 확인 명령 |
|---|---|---|---|
| `claude` | **2.1.239** | `loggedIn:true` · `claude.ai` | `claude auth status` |
| `codex` | **0.149.0** | `auth is configured` · `chatgpt` | `codex doctor` |

**설치 시 알아둘 것**: `npm install -g` 후 `approve-scripts` 경고가 떠도 **무시해도 된다** — 실행 바이너리가 패키지에 포함돼 있어 postinstall 없이 동작한다(claude.exe 337MB 실측). codex는 **데스크탑 앱과 자격증명을 공유**하지만 claude CLI는 **별도 로그인**이 필요하다(`claude auth login`).
- **다음 게이트 이월**: 훅 설치 승인(실수 원장 3조의 선행 조건) · 유예 조문 3건(계획 선별 / 실수 축적 / 심플하게) · 점수 척도 불일치(슬라이스 2 관찰) · Phase N 착수 시 "48종을 정리할 것인가"
- **오너 제안 2건 — 슬라이스 2 착수 시 처리(2026-08-22 제기, 지금 착수 안 함)**
  1. **E2E 테스트 도구(Playwright) 도입** — 오너 제안: *"만들고 있는 프로덕트가 웹이므로 생성·품질·완성도·버그 검출에 유용한 플러그인이 필요할 것 같다."* **판단: 도입 찬성, 시점은 슬라이스 2.** 근거 = ① 오늘의 백그라운드 탭 밀집 동결 버그는 탭 가시성·CPU 스로틀링 제어로 자동 재현 가능한 유형이었으나 오너 육안으로 발견됐다 ② 슬라이스 1 하이라이트 왕복(kg_search→SSE→3D)을 매번 수동 검증 중 ③ ROADMAP Phase 4(4.1 엣지·4.2 로딩/에러/빈 상태·4.6 접근성)의 수행 수단이 현재 없다. **지금 도입하지 않는 이유** = M2 구현 중 도입하면 원인 분리 불가(한글화를 뒤로 미룬 것과 같은 논리). **선행 절차** = 의존성 최소주의 철칙(`bibliomind/CLAUDE.md:47`)에 따라 **TECH-SPEC §1.3 개정 → 오너 승인 → 도입**. **한계 인지** = 3D/WebGL은 E2E로도 비결정론적이라 노드 색·위치는 단언 불가. 현실적 사정거리는 **HTML 오버레이·콘솔 에러·네트워크·N/M 카운트**.
  2. ~~**3D 앱이 현재 하이라이트 상태를 전역에 노출**~~ → **2026-08-22 오너 결정 A로 채택·구현 완료**(TECH-SPEC §7.9 · `window.__bibliomind` · 커밋은 아래 목록 참조). 판정 ④가 **기계 대조 가능**해졌다. 읽기 전용이라 렌더링은 무변경 — 미루자던 근거(3D 표면 변경)가 해당하지 않았다. **판정 시 주의**: `layer1`(수신) ≠ `rendered.layer1`(실제 그려지는 것 = 1층 − 2층). 차집합은 N/M 격차의 내역이지 결함이 아니다.
- **집계자 브라우저 도구 실측(2026-08-22)**: 이사님이 이미지를 보낼 필요 없이 Claude가 `localhost:5173`에 **직접 진입해 DOM 전문을 읽을 수 있음이 실증됨**(하이라이트 패널 질문 원문·인용 검증 문구·라벨 카운트·관계 유형 48종 전부 읽힘, 페이지 내 JS 실행 가능). **한계 2건**: ① 스크린샷은 브라우저 패널이 화면에 표시돼야 함(미표시 시 5초 타임아웃 — 실측) ② `document.querySelector('canvas')`가 null이라 WebGL 내부(노드 색·위치·파티클)는 DOM으로 못 읽음(패널 미표시 영향 여부 `[미확인]`). ⇒ 판정 ①호출 ②시드 ③인용은 도구 반환값+DOM으로 완결, ④의 시각 증거만 미해결이며 위 제안 2가 그 해법.
  - **재측정(2026-08-22 M2 배선 후)**: 스크린샷은 `tabs_select`로 탭을 앞으로 가져오면 성공 — 한계 ①은 **회피 가능**으로 정정. 한계 ②는 재현됨(`canvas` 0개).
  - **한계 ② 해소(2026-08-22 §7.9 구현 후)**: WebGL 내부를 읽을 필요가 **없어졌다**. `window.__bibliomind`가 1층·2층 kgid와 실제 렌더 상태를 JSON으로 내보내므로 픽셀·캔버스 접근 없이 판정 ④를 대조한다. 실판독 확인: C5 푸시 + 인용 2건 → `searchId`·1층 20n·33r·2층 관계 2건 전부 푸시 집합과 일치. **판정 시 이 한 줄만 실행하면 된다** — `JSON.stringify(window.__bibliomind)`.

### 2026-08-22 커밋 (양 저장소)

- `bibliomind`: `8419046` 유지보수 계획 → `6068415` M0 결정 → `c5b6306` 규칙 18조·시뮬레이션·7조안 → `65b9b59` 7조 채택 → `fb3a9ea` **TECH-SPEC v2.3(M0 완료)** → `1f3fdb8` **전문가 투입 제1조(8조)** → `6db4d46` 문서 정정
- `GraphRAG_1st`: `3358353` 골든 픽스처 → `59ed6a3` hangul·앵커 순수함수 → `532b151` **M2 배선** → `c174562` **M3 문구** → `0ada284` **§7.9 상태 노출(295케이스)** → `3baccdf` **check-highlight** → `2459430` **계측기 3건 수리** → `07121c1` **survey 다중키워드 구멍 수리** → M4 산출물
- 브랜치: `GraphRAG_1st` = `phase2-scaffolding`(**main 병합·push는 M5 통과 + 이사님 승인 후**) / `bibliomind` = `main`

### 공개 저장소 — **`https://github.com/dume81/BiblioMind.git`** (2026-08-22 오너 지시)

| 항목 | 상태 |
|---|---|
| 대상 | **코드 + 문서 단일 저장소**로 확정(오너 회신). 기존 `dume81/GraphRAG_1st.git`은 보관용 |
| 저장소 성격 | **[실측] 공개(public) · 현재 비어 있음**(익명 `ls-remote` 성공, refs 0건) |
| **지금 할 일** | **없음.** push·구조 병합은 **M5 통과 + 오너 승인 후** |
| 미루는 이유 | 병합은 폴더 배치를 바꾸는데 M5는 `GraphRAG_1st`를 루트로 연 클라이언트에서 실행된다(`.mcp.json`·`scripts/`·`.githooks/`·`paths.js`가 현 배치 전제). 판정 직전 경로 변경 = M4에서 두 번 피한 실수 유형 |
| 위생 검사 | **이미 통과** — 실값 자격증명 0건 · `.env`·`data/` 추적 0건 · `!.env.example` 예외 정상. 병합 후 1회 재확인만 |
| 남은 결정 | 병합 방식(양쪽 git 이력 보존 수단) — M5 이후 별건 |

### 집계자(Claude) 실수 기록 — 재발 방지 (DECISIONS 말미 상세)

1. 이미 결정·구현된 사안을 미결정으로 상정 → 결정 상정 전 DECISIONS·TECH-SPEC·코드 대조(8조 제7조 ⑦로 조문화)
2. 확정 실행과 미결정 순서를 뭉쳐 질문 → 확정분·미결정분 분리(제7조 ⑧)
3. 단일 표본으로 임계값 권고 → 표본 수 명시
4. 외부 도구 UI 명칭 단정 → 외부 화면 기본값 `[미확인]`(제5조)
5. **셸 서식 사고 2회**(`printf`의 `%`, 스크립트 이스케이프) → **한글 텍스트 치환은 heredoc 또는 Edit 도구.** 담당 조문 없음 — 8조가 못 막는 유일한 유형

### 이전 이력(2026-08-21 시점 — 참고용)

- **Phase 2 게이트 통과(2026-08-21 승인) → 슬라이스 0.5(스파이크 준비) 진행 중.** 게이트 확정 3건 + Karpathy 가이드라인 채택(GraphRAG_1st/CLAUDE.md·AGENTS.md 신설, 플러그인 미설치). 브랜치 운용 = 스파이크 통과 후 main 병합. 스캐폴딩 완료 내역(참고):
  - 구현: GraphRAG_1st를 npm workspaces 모노레포로 확장(`@bibliomind/shared|pipeline|mcp-server` + visualization-3d), shared 순수 함수 전부 구현+테스트, kg_status 스텁 1종, scripts/setup.js, .mcp.json, .env.example, ROADMAP 작성. **브랜치 `phase2-scaffolding` 로컬 커밋 — main 병합·push는 이사님 승인 후.**
  - 검증 수치: npm test 총 **203케이스**(기존 시각화 14파일·148케이스 무수정 포함), lint 통과, setup 2회 멱등, mcp:smoke에서 kg_status 응답, dev:all 8787·5173 HTTP 200.
  - 게이트 승인 대상 3건: §1.8 스크립트 개정(mcp:smoke·lint·-y) / instructions_ko 권고 2줄 / 브랜치 운용 — DECISIONS.md 말미 참조.
- **2026-08-21 요구 변경 반영: Neo4j Desktop → AuraDB 클라우드** (PRD v3.1·TECH-SPEC v2.1·DECISIONS ADR). 핵심 리스크 = Free 3일 무쓰기 자동 일시정지·일시정지 30일 후 인스턴스 삭제 — 원본 진실이 로컬 Reviewed/라 재주입으로 완전 복구.
- **슬라이스 0.5 완료 ✅(2026-08-21)** — 사용자 액션 ②까지 양 클라이언트(Codex·Claude Code) kg_status 실호출로 검증 완료. ④(dev:all)는 슬라이스 1 첫 단계로 이월. 코드분 상세: 사용자 액션 ①(AuraDB `bibliomind` 인스턴스·접속·cjk 실측 ✓) → inject-example 실주입(29노드/55관계, 2회 멱등 ✓, 인덱스 2종 ONLINE) → **도구 4종 구현·실호출 검증**("탄지로" T2 cjk 실증·"귀살대" T1·kg_cite verified/partial/none 전 경로·무-throw 푸시) → 평가 질문 세트 21문(docs/spike-eval-questions.md) → 테스트 213케이스. 잔여: **사용자 액션 ② MCP 등록(Claude Code = GraphRAG_1st 루트 열기 / Codex = config.toml + 재시작) ③ 구독 로그인 확인 ④ dev:all + 도구 "항상 허용"** → 슬라이스 1(하이라이트 스파이크 — 최소 허브 라우트·3상태 렌더도 이때 구현)
- **미결(보류)**: GitHub 저장소명 변경 여부 / 부록 A 오너 확인 ①②③(PRD source_remove 문언 포함)

## 핵심 확정 사항 압축 (상세·근거는 DECISIONS.md)

1. 디스코드/OpenClaw **배제** → 대화 인터페이스 = **로컬 MCP 서버** (Claude 앱·ChatGPT 데스크탑 Codex 양쪽 지원)
2. KG 생성 엔진 = Codex/Claude **선택형** + 한도 소진 시 **상호 전환(failover)** — 역할 분리는 폐기됨. Gemma는 범위 제외
3. **도메인 스키마 AI 자동 도출** — 기존 유형 재사용 우선 + 부족분 신규 도출 + 자동 등재·보고
4. 검수 2단(구조/의미) + 재빌드 롤백(Reviewed/=원본 진실) + 반려 3회 보류. 조작 원칙 = "챗이 조종석, 브라우저는 화면"
5. 하이라이트 = 3상태(밖 dim / 1층 은은·무조건 / 2층 검증된 인용 강조+파티클), 식별자 = kgid
6. 제품명 비블리오마인드(2026-08-21) · 호칭 = **"이사님"** · 예시 데이터(귀멸의 칼날) 유지 · MIT 라이선스 유지
7. 그래프 DB = **Neo4j AuraDB 클라우드** (2026-08-21 전환 — 코드는 URI 기반이라 로컬 병용 가능. **사용자명·DB명은 자격증명 .txt 값 그대로 — 신형 콘솔은 8자 생성 ID**. Free 한도 20만/40만 실측, 3일 무쓰기 일시정지·30일 삭제 정책 고지 의무)
8. 총감사(2026-08-21, 패널 8인) 반영: 스파이크 Claude 표면 = **Claude Code(GraphRAG_1st 루트 열기)**, ChatGPT 데스크탑 미설치 — **Codex 표면으로 확정**, 부록 A 3건 전부 해소(승인은 source_remove만 매번 확인·나머지 항상 허용), kg_search 호출률·부정 대조 검증 신설(§1.14-6), bibliomind/ 로컬 git 관리 시작

## 슬라이스 1 진행 상태 (2026-08-21 갱신 — 구현분 완료 ✅, 판정 대기)

**구현 완료(커밋 GraphRAG_1st 1ef89f0)**: 아래 브리핑의 §7.8 목록 전부 구현 + 검수 패널(4렌즈 18에이전트+반박 검증) 확정 13건 반영 + 테스트 248케이스(기존 148 무수정+신규 35) + 실왕복 검증(kg_search 실호출 → `delivered:true` → 크롬 자동 전환·질문·citation 문구 DOM 확인, 부정 대조 "무잔"→"검색 결과 없음", highlight_clear 소거). 상세·소결정은 DECISIONS 말미 "슬라이스 1 구현 완료" 항목이 정본.

**남은 것(이사님 몫)**: ① 사용자 액션 ④ — `npm run dev:all` + 크롬 http://localhost:5173 열어 하이라이트 육안 확인(허브가 마지막 verified 하이라이트를 보관 중 — 열면 자동 재생) + 챗 클라이언트에서 도구 "항상 허용" ② 스파이크 판정 — spike-eval-questions.md 21문 × Codex 먼저·Claude Code는 8/25(화) 이후 ③ 판정 통과 시 main 병합 승인.

**유지보수 진행(2026-08-22~)**: 슬라이스 1 스파이크 **Claude 표면 21/21 통과**(시드 14/15·인용 73건 verified·부정대조 3/3·소요 중앙값 6.1초). 오너가 **B안**(개선 먼저→재판정→Codex) 채택 → **docs/MAINTENANCE-PLAN.md가 유지보수 정본**(목표 5개·Phase M0~M5·Phase N 별건·오너 결정 3건). 현재 위치 = **M0 게이트 대기**(설계 문서화 + 오너 결정 ①T2 하한 50% ②잔상 A안 ③한글화 Phase N 편성). 다음 = M1 계측기 → M2 검색 정확도 → M3 문구 → M4 재판정 → M5 Codex.

**판정 진행 실기록(2026-08-21 밤 확정)**: **docs/spike-judgment-log.md가 유일 정본** — 문항별 판정표(**완료 21/21문(2026-08-22 완주)**: A1·A4·A5·B5·C1·D1·D2·D3·E1·E2·E3), 시드 5/5·E군 3/3, D2 침묵 실패 2회 재현 → 공백 가시화 질의 규칙 **적용 완료(GraphRAG_1st 커밋 e362d35 — [A]~[E] 실코드 검증됨)** → 재측정 통과. 오너 확정 원칙(공백 가시화 = 품질 루프)·판정 후 처리 목록 6건 포함. **주의: 이 문서의 다른 절에 남은 판정 이전 서술("Codex 먼저·8/25 이후"·"사용자 액션 ②③④ 잔여" 등)은 낡음 — 상충 시 spike-judgment-log.md가 우선.** 8/22 재개 = 이 대화창 모델을 Opus 5로 바꾸고 "spike-judgment-log.md 읽고 이어서".

## 슬라이스 1 착수 브리핑 (원본 보존 — 위 진행 상태가 최신)

1. **구현 전 정독**: TECH-SPEC **§5 푸시 프로토콜(1173~1268행)** + **§7 시각화 확장(1485행~끝, §7.8 최소 수정 작업 목록이 파일별 지침)**. §5.2 메시지 4종 스키마·§5.1 보관/재생 규칙·§7.4 kgid 색인·§7.5 우선순위·§7.6 패널 문구(v2.2 정직화 반영)가 정본.
2. **수정 대상(§7.8 — 이 목록 밖 수정 금지, 외과수술 원칙)**: `visualization-3d/server/localServer.js`(라우트 3종+SSE+보관·재생+본문 상한+127.0.0.1) / `server/core/mapper.js`(RKEntity 제외 1줄) / `src/hooks/useGraphLoader.js`(push 소스) / `src/App.jsx`(SSE 구독 훅) / 신규 3상태 오버라이드 모듈 / `src/components/HighlightPanel.jsx`(citation 문구·N/M·truncated) / 신규 단위 테스트(kgid 색인·3상태 우선순위·N/M).
3. **완료 판정**: 기존 148케이스 무수정 통과 + 신규 테스트 + `dev:all` 켜고 kg_search 실호출 시 `delivered:true`·화면 하이라이트 실표시(mcp-server 쪽은 이미 완성 — 수신 측만 만들면 연결됨).
4. **스파이크 판정 절차**: docs/spike-eval-questions.md 21문 × 클라이언트 2종. **순서: Codex 먼저 → Claude 쪽은 8/25(화) 한도 재설정 후**(2026-08-21 기준 Claude Max 76% 사용). 시작 전 `npm run setup`으로 Aura 상태 확인(8/24까지는 활성 보장 — inject 쓰기로 시계 리셋됨). 판정 표면: Claude Code(GraphRAG_1st 루트) + Codex 데스크탑.
5. **git 상태**: GraphRAG_1st `phase2-scaffolding` 브랜치(커밋: 8b10e56 스캐폴딩 → 143e888 가이드라인 → 0c35f5a 정정 → 56fee39 슬라이스 0.5). bibliomind/ 로컬 git(ebfa10f → 8a9bc67 → 3effa1a). **main 병합·push는 스파이크 통과 후 이사님 승인**.
6. **주의**: MCP 서버 코드를 고치면 챗 클라이언트가 다음 기동 시 자동 반영(stdio 스폰). Codex config.toml·Claude Code .mcp.json 등록은 이미 완료·검증됨 — 재등록 불요.

## 산출물·자산 링크

- **도해 아티팩트** (유저 플로우·정보구조도·아키텍처, 검증 반영 v2): https://claude.ai/code/artifact/b02684d0-9743-4480-a8ce-cb3722145b3d
- 예시 KG JSON: `GraphRAG_1st/KG_Demon Slayer_Draft_01.json` / KG 추출 프롬프트: 워크스페이스 `req_node_relation.txt`
- 설치된 스킬(다음 세션부터 인식): agent-browser(+CLI v0.34.0 전역), find-skills, **GSD Core v1.11.0(전역 — 2026-08-21 스킬 조정 A안으로 핵심 프로필 8종 축소, 훅 14개·상태줄 유지)**, design-taste-frontend, mcp-builder(Anthropic 공식). GSD 방법론(.planning)은 미사용 — /gsd-*는 명시 호출 시에만(루트 CLAUDE.md 명문화)
- **OmniRoute 평가 결론(2026-08-21)**: 설치 가능하나 **비권고 확정** — Anthropic 구독 OAuth 서드파티 금지(계정 제재 위험)·루트 CA/MITM 보안 이력·KG 품질 역효과·단일 실패점. 설치하지 않았음

## 협업 방식 (압축 후에도 유지할 것)

- 실질 요청마다 **전문가 패널(Workflow 에이전트) 투입** → 결과 종합 → 문서 반영 → 게이트 승인
- **문서가 코드보다 먼저** · 산출물 개정 시 **왜곡·누락 금지**(독립 검증자 대조가 표준 절차)
- 사용자 = SAP FI 컨설턴트 출신 프로그래밍 비전문가, 비유로 소통(기술 번역 후 확인), 사용자 전용 액션(계정·키·결제)은 "사용자 액션 필요"로 명시
- 매 응답 끝: `현재 위치: Phase X.Y / 다음 액션` 한 줄

## M4 재판정 결과 — **완주 · 합격선 8/8 달성** (2026-08-22)

> 실기록 정본 = `docs/M4-JUDGMENT-LOG.md`. **재실행 불요.**

| 단 | 결과 |
|---|---|
| 1단 챗 11문 | **11/11 통과** (A3·B1·B2·B4·C4·D1·D2·D3·E1·E2·E3) |
| 2단 서버 프로브 10문 | **전건 무변경** (선언 외 kgid 추가 0) |
| 3단 손상 프로브 2문 | S19 **복원 적중**(3n·2r = 정상 표기 S03) · S20 **불변** |

**합격선 8개 전건 달성** — 시드 15/15 · kg_cite 11/11(탈락 0) · 부정 대조 3/3(치명 실패 0) · 거짓 부재 **0** · 소요 연속 중앙값 **6.9초** · 회귀 감시 무변경 · 3상태 **121/121** · 호출률 **11/11**.

**핵심 실증 3건**: ①B2에서 M2 예측(시드 5→4, 13n·21r→11n·18r)이 **kgid 단위로 일치** ②같은 노드가 B2에서 잘리고 B4에서 살아남아 **임계값 50% 양방향 증명** ③`window.scene` 순회로 **색 적용 단계까지 기계 판정**(11문 전건 일치).

### 판정 인프라 (이번에 만든 것 — M5·Phase N에서 재사용)

| 도구 | 용도 |
|---|---|
| `scripts/check-highlight.js --snapshot` | 푸시 집합 ↔ 화면 스냅샷 11항 대조 |
| `scripts/refresh-keyword-probes.js --since` | 회차 (question, keywords) 수집 |
| `scripts/check-keywords.js --fixture`(survey) | 회차 키워드 손상·거짓 부재 후보 판정 |
| `bibliomind/tools/measure-latency.mjs --since --fresh` | 모집단 분리 소요 집계. **`--fresh A3#2` 회차 지정 가능**(2026-08-22 수리) |
| **`scripts/check-absence.js --fixture`** | **부재 주장 3유형(노드·속성·관계) Aura 역검증**(2026-08-22 신설 — M4에서는 scratchpad 일회용이었다) |
| 색 검증(§4-b) | `window.scene` 순회로 2층/1층전용/밖 개수 대조 |

**브라우저 전제**: 뷰포트가 0×0이면 캔버스가 생성되지 않는다 — `resize_window`로 크기를 준 뒤 **새로고침**해야 `window.scene`이 읽힌다.

## 결정 대기 — 재발 방지 기전 (설계 완료, 착수 전)

> 설계 패널 결과. 채택안 4기전(A1 판별력 게이트 / A2 도구 자기부인+골든 대조기 / A3 합격선 3칸 체커 / A4 작성자≠검증자 1렌즈). **새 규칙 문서는 전량 기각** — "기록은 6번 재발을 못 막았다".

> **한 번에 1건씩만 여쭙는다**(2026-08-22 오너 지시). **대기열은 2026-08-22 전건 소진됐다.**

| # | 결정 | 회신 | 실행 결과 |
|---|---|---|---|
| 1 | A1 선행 수리 3건 | **3건 전부 승인** | **완료 4/4** — 뮤테이션 생존 7→5, 테스트 295→297. `PREVENTION-MECHANISM.md` §3 |
| 2 | 자동 실행 채널 | **`.githooks` + setup 자동화** | **완료 4/4** — 고의 회귀 2회 차단 실증, 소요 3,954ms. 같은 문서 §7 |
| 3 | 8조 제3조 문면 | **개정 — 전부 필수** | **완료** — 워크스페이스 `CLAUDE.md` 제3조가 정본, 근거는 `OPERATING-RULES-7.md` |
| ~~4~~ | ~~설계 검정을 실행표 v1 대상으로~~ | — | **철회** — M4가 끝나 v1 실행 기회 소멸. 검정은 M5 실행표에 붙인다 |
| 신규 | 공개 저장소 대상 | **코드 + 문서 둘 다** | 위 "공개 저장소" 절 — **구조 병합·push는 M5 이후** |

**재발 방지 기전 정본 = `docs/PREVENTION-MECHANISM.md`**(2026-08-22 신설). 설계가 문서 없이 대화에만 있어 `/compact` 한 번이면 소실될 상태였다.

**주의 — `CLAUDE.md`는 버전 관리 밖이다**: 워크스페이스 루트는 git 저장소가 아니라 제3조 정본 파일에 이력이 남지 않는다. 개정 전문은 `OPERATING-RULES-7.md`(추적됨)에 복사해 뒀다.

**실측 근거**: 뮤테이션 13개 중 **7건 생존**(295케이스가 못 잡음). 살해 입력 2건 확보 — `pickAnchorCandidate('일륜도','일륨도 말고 일륜도')`(정상→손상 바꿔치기), `('전집준 호흠','전집중 호흡은 뭐야?')`(거리 2 복원).

## M4에서 발견된 제품 신호 (M5 이후 개선 큐)

**"어떻게/왜" 질문 유형 전반이 현재 그래프로 답이 안 나온다.** 4문항 연속 같은 유형의 공백 — 일륜도 설명 속성 / 탄지로 계급·부대 / 도깨비화 원인·행위자 / 사부로 영감 직업. 같은 `Event` 라벨인데 `기유가 네즈코에게 재갈을 물린 사건`은 행위자·대상·도구 완비, `네즈코의 도깨비화`는 행위자 부재 — **어떤 조건에서 행위자가 붙는가**가 추출 프롬프트 점검의 축이다.

## M4 미수리 등재 — **2026-08-22 정정 (4건 중 2건이 틀린 기재였다)**

| # | 상태 | 내용 |
|---|---|---|
| ① | ~~해소~~ | `measure-latency --fresh` 문항 단위 → **호출 회차 단위**(`A3#2`). 같은 문항 2회 시 **경고 + 회차·시각 표기**. 2026-08-22 수리 |
| ② | ~~해소~~ | 부재 역검증기를 **저장소 도구로 승격**(`scripts/check-absence.js` + `fixtures/absence-claims.json`). 넓은 정규식 → **속성 계열 사전**. M4 부재 주장 9건 **전건 사실·오탐 0건**(직전 1/9). 2026-08-22 수리 |
| ③ | ~~해소~~ | `refresh-keyword-probes` 파싱 실패 구분 — A1-3에서 수리(`parseFailed` 플래그) |
| ④ | **`[미확인]` — 유일한 잔여** | `--since` 파싱 코드는 두 도구가 **동일**하다(`measure-latency.mjs:66` ≡ `refresh-keyword-probes.js:33`). 실제 차이가 있다면 **비교 대상 이벤트**(질문 발화 시각 vs `tool_use` 호출 시각)일 수 있으나 **확인하지 않았다.** 다음 판정 전 실측 대상 |

**미수리 4건 중 3건 해소 · 1건은 기재 내용 자체가 `[미확인]`.**

### ① 수리가 즉시 드러낸 것 (M4 판정은 불변)

`--fresh A3` 회차 지정을 켜자 **`A3`와 `B1`이 각각 2회 호출**됐음이 드러났다 — 이전 출력은 이를 숨겼다. 즉 M4 합격 판정의 *"연속 5건"* 에 **B1이 두 표**를 던졌다. **중복을 빼도 중앙값은 6.9초로 같다**(B1#1 채택 `[5.0, 5.6, 6.9, 11.1]` / B1#2 채택 `[5.6, 6.9, 6.9, 11.1]` — 둘 다 6.9). **판정 불변이므로 M4를 재개봉하지 않는다.** 다만 M5에서는 이 중복이 처음부터 보인다.

## M5 — **보류 중** (2026-08-22 오너 지시)

*"지금 codex가 작업 중이라서 M5 단계는 미뤘으면 해."* ⇒ **이사님 신호가 있을 때 시작한다.** 준비 상태는 위 표에서 실측 완료 — 시작 직전 `kg_status` 1회 확인이면 충분하다.

**skip이 아니라 defer다** — `PRD.md:96`이 *"병목 시 순서 조정은 가능하나 **범위 축소 아님**"*, `PRD.md:87`이 *"1차 완성 선언은 양 클라이언트 충족 후"*. **1차 완성 선언 전에 반드시 완주해야 한다.** 완화 요인 = Codex `kg_status` 실호출은 슬라이스 0.5에서 이미 성공(`ROADMAP.md:38`) ⇒ M5가 재는 것은 *연결 성립*이 아니라 *품질·준수율*. 그래도 **"맨 마지막"이 아니라 "여유 생기는 대로"** — Codex 표면 문제를 늦게 발견할수록 그 위에 쌓인 슬라이스가 함께 재작업된다.

## 슬라이스 2 — **완료 (2026-08-22, 합격선 5/5)**

프로토콜은 **슬라이스 1이 이미 구현해 둔 상태**였다(§5·§7 동시 구현). 실제 작업은 **검증기 저장소 승격 + 낡은 안내 2건 수리**.

| 산출 | 내용 |
|---|---|
| `scripts/hub-e2e.js` · `npm run hub:e2e` | 허브 실왕복 **8/8** — 라우트 3종·SSE 중계 순서·교차 type 400·본문 상한 413·재생 규칙. `API_PORT`로 **격리 인스턴스**를 띄워 개발 허브(8787) 화면을 오염시키지 않는다 |
| `vizClient` 404 문구 | *"수신 라우트가 아직 없습니다(슬라이스 2 예정)"* → 실제 원인(옛 서버·포트 불일치)을 가리키도록 교체 |
| `App.jsx` `graph.show` | **브라우저 실왕복이 잡은 결함** — `channelNotice`를 지우지 않아 검수 파일명과 *"Neo4j 그래프로 전환했습니다"* 가 동시에 떠 모순된 안내가 됐다. 수리·재현 확인 |

**미등재 한계**: `App.jsx` 수리는 **브라우저 관측으로만** 확인했고 **자동 회귀 테스트가 없다**(`handlePushMessage`가 컴포넌트 내부라 추출 없이 단위 테스트 불가). 다음 결정 후보.

**범위 밖(해당 슬라이스에서)**: `/api/show`·`/api/refresh`를 실제 호출하는 MCP 도구는 아직 없다 — `review_show`는 S4(슬라이스 6), 재빌드는 S5.

## 슬라이스 3 — **완료 (2026-08-22, 합격선 8/8)** · 커밋 `a1e7670`

**대상 URL은 오너 지정 `https://readians.com/`.** 신규: `shared/src/atomicWrite.js`(§2.5) · `pipeline/src/ledger.js` 전체 구현 · `pipeline/src/crawl/index.js` `collectWeb` · `bin/collect.js` CLI. 의존성 3종(`robots-parser`·`tldts`·`gray-matter`)은 **§1.3 승인 6종 안**이라 문서 개정 불요. 테스트 **297 → 334**.

**실측이 잡은 결함 3건** (단위 테스트만으로는 전부 안 보였다):

| # | 결함 | 처리 |
|---|---|---|
| 1 | `[![alt](img)](href)` 에서 **이미지 URL을 링크로 오인** — readians.com 로고가 그 형태 | 이미지 선제거 + 회귀 테스트 |
| 2 | **⛔ robots 차단 우회 — 실제 예절 위반 발생.** `normalizeUrl`이 끝 `/`를 지워 `/api/`→`/api`가 되어 `Disallow: /api/`에 매칭되지 않았다 | `robotsDisallows()` 신설(슬래시 변형 검사) · 회귀 6건 · **잘못 수집한 MD 2건·원장 2건 삭제** |
| 3 | 요약이 *"max_pages를 올리면 이어서 수집합니다"* 로 **거짓 안내** | 사실대로 수정 + `--force`로 이어받기 실증. **"틀린 안내" 3번째 사례**(M3·슬라이스 2에 이어) |

**설계 공백 — 오너 결정 대기**: 멱등 스킵이 요청을 생략하므로 **재실행만으로는 남은 큐를 이어받을 수 없다.** 현 회피책은 `--force`(전체 재수집 — 무키 저율에서 비용 큼). 정공법은 ①큐 잔여를 원장에 저장 ②**스킵된 페이지의 저장된 MD에서 링크 재추출**(네트워크 0회로 BFS 지속). ②가 §1.5 "요청 생략" 규칙을 지키면서 푼다. **TECH-SPEC §1.5 개정 선행.**

**현재 로컬 데이터**: `data/Input/`에 `readians.com` MD **24개** + S2 추출 MD **4개** = **28개**(`data/`는 gitignore — 커밋되지 않음).

**후속 수리(같은 날)**: 재실행 이어받기 공백을 **`TECH-SPEC §1.5` v2.5 개정 → 구현**으로 해소했다. 스킵 시 저장된 MD에서 링크를 재추출해 BFS를 잇는다(**네트워크 요청 0회 유지**). 실증: `--force` 없이 재실행 → 스킵 13건 + **신규 11건**. 커밋 `5a3b2cd`.

## 슬라이스 4 — **완료 (2026-08-22, 합격선 7/7)** · 커밋 `5a3b87d`

**S2 문서 추출 — 전 과정 로컬.** PDF는 `unpdf`(pdf.js 엔진), 이미지는 `tesseract.js`(WASM, kor+eng). 언어팩은 `data/ocr-cache/`에 캐시 완료(kor 2.2MB · eng 5.2MB) — **착수 조건 해소**.

| 검증 | 실측 |
|---|---|
| PDF 3단계 | **449자 ok · 2자 low · 0자 empty** |
| 한글 이미지 OCR | 원본 5줄 **전건 정확 인식 · 오탈자 0** |
| 멱등 | 재투입 시 새 파일 **0개** |
| 테스트 | 334 → **343** (`64+48+40+191`, 도구 합산) |

**스캐폴딩 계약 준수**: 스텁이 선언한 `collectDocs({ path, force })`를 그대로 따랐다 — 내가 붙인 이름(`extractDocument({ file })`)이 기존 스모크 테스트를 깨뜨려, **테스트가 아니라 내 이름을 고쳤다**(저장소 관행 = 기존 테스트 무수정 통과).

**정직 기재 2건**: ①"외부 전송 0건"은 **우리 코드에 대해서만 [실측]** — 라이브러리 내부 캐시 동작은 [문서] 수준이며 패킷 관측은 하지 않았다 ②테스트 픽스처와 그 **재현 스크립트가 저장소 밖(scratchpad)** 이다 — `check-absence`·`hub-e2e`를 승격했던 것과 같은 유형의 미승격이므로 등재한다.

## 슬라이스 1.5 — **완료 (2026-08-22, 합격선 6/6)** · 엔진 플래그 세트 확정

CLI 2종 설치·로그인 후 각 엔진을 **1회씩 실호출**해 KG JSON을 받았다. 둘 다 스키마 검증 통과.

| | Codex | Claude |
|---|---|---|
| 명령 | `cmd /c codex exec --skip-git-repo-check -s read-only -C (data/tmp) -o (파일) -` | `cmd /c claude -p --output-format json` |
| 프롬프트 | **stdin**(argv 금지 — 8,191자 한계·이스케이프 회피) | **stdin** |
| 결과 수취 | `-o` 파일 | 봉투의 `result` 필드 |
| 실패 판별 | 종료 코드 | 봉투의 `is_error` |
| 실측 | 노드 6·관계 5 · **48.9초** | 노드 9·관계 15 · **15.3초** |

**정본이 틀렸던 것 2건 — 이것이 이 슬라이스의 산출물이다**

1. **`--max-turns`는 claude CLI에 없다** → 제거. 그 플래그 없이도 `num_turns: 1` 확인.
2. **`--output-schema`(Codex) 미채택 확정** — structured output이 모든 object에 `additionalProperties:false`를 강제해 400. KG 노드의 `properties`는 자유 객체여야 하므로 **§2.1 스키마 자동 도출과 구조적 비호환**. 없이도 백틱 없는 순수 JSON이 나왔다.

**슬라이스 5 입력**: 어댑터 `timeoutMs` 기본값은 **codex 기준**(3배 이상 느림). 부작용 0건 실측. 같은 입력에서 **엔진별 추출 밀도가 다르다**(6n·5r vs 9n·15r) — 성공 기준 3의 품질 비교 근거.

## 슬라이스 5-A — **완료 (2026-08-22, 합격선 7/7)** · 커밋 `9307f9c`

**실 엔진 CLI 호출 0회 · 구독 소모 0**으로 S3 생성기를 구현·검증했다. 테스트 343 → **383**.

| 산출 | 내용 |
|---|---|
| `resolveEngine.js` | 시작 엔진 선택(인자 > `.env` > codex) + `otherEngine` |
| `engines/run.js` | 공용 spawn·stdin 전달·실패 분류 **5종** |
| `engines/{codex,claude}.js` | 슬라이스 1.5에서 확정한 플래그 그대로 |
| `generate/index.js` | 스킵 판정 · 프롬프트 조립 · **한도 전환** · `bad_output` 교정 1회 · 검증 통과분만 원자 이동 · 실행 단위 스키마 등재 |
| `tools/kgGenerate.js` | MCP 도구 `kg_generate`(`limit` 기본 1) — `tools/list` 노출 확인 |
| `fixtures/fake-engine.js` | 8개 시나리오 가짜 엔진 |

**가짜 엔진이 강제하는 것**: stdin으로 프롬프트가 안 오면 **종료 코드 4로 항의**한다 — *"본문을 argv로 넘기지 않는다"*(§1.13-2)가 시험으로 굳었다.

**구현 중 잡은 결함 2건**: ①`parseEngineJson`이 최상위 배열에서 **첫 객체만 몰래 가져갔다** → 거부로 변경 ②`smoke.test`의 스텁 시험 대상이 낡음 → `rebuildGraph`로 이동.

**남은 것**: **5-B**(실엔진 준수율 5건×2엔진, 약 15분 [추정]) · **5-C**(오너: MCP 타임아웃 상향 + 챗 연속 호출 실측).

**미등재 한계**: 어댑터의 **argv 조립 자체는 계약 테스트가 덮지 않는다**(가짜 spawn으로 대체). 슬라이스 1.5 실호출로 확인했고 5-B에서 다시 확인된다.

## 슬라이스 5-B — **완료 (2026-08-22, 합격선 6/6)** · 단 4/5만 시도

격리 `KG_DATA_DIR`에서 실제 `readians.com` 자료 5건을 두 엔진에 각각 넣었다(`failover:false`).

| 엔진 | 성공 | 중앙값 | 최대 | 총 소요 | 노드·관계 | `schema_version` |
|---|---|---|---|---|---|---|
| **codex** | 3건 | **194.1초** | 229.0초 | 801.0초 | 146·166 | 1 → **4** |
| **claude** | 3건 | **65.3초** | 68.2초 | 241.1초 | 121·145 | 1 → **2** |

**5-A가 못 덮던 argv 조립이 실증됐다** — 두 어댑터 각 3건 실호출 성공. 전환 0건(`failover:false` 준수). 실제 `data/` 4개 지표 **무변화**.

### ⚠️ 미측정 1건 — 후속 필수

**4번째에서 양쪽 다 `rate_limit`**, **5번째(6,474자 — 대상 중 최대)는 시도조차 못 했다.** 실제로 잰 최대 입력은 **2,776자에 229초**이고 6,474자는 그 **2.3배**다 — `[추정]` 500초 이상으로 **기본 타임아웃 600초에 근접**한다. **큰 파일 소요는 `[미확인]`이며 한도 회복 후 반드시 재야 한다.**

### 집계자 추정 빗나감 [기록]

`[추정]` codex 1~3분·claude 30~60초 → **실측 codex 194~229초·claude 50~68초로 양쪽 상회**. *"스모크 소요 × 프롬프트 길이비"* 가 **선형이 아니다.**

### 슬라이스 1.5와 결과가 뒤집혔다

합성 문장(1.5)에서는 claude가 조밀했는데(9n·15r vs 6n·5r) **실자료에서는 codex가 조밀**하다(146·166 vs 121·145). ⇒ **작은 합성 입력으로 잰 엔진 특성은 실자료에 일반화되지 않는다.**

### 남은 것

**5-C(오너)**: MCP 타임아웃 상향 + 챗 연속 호출 실측. 타임아웃 키 이름은 아직 `[미확인]` — `settings.json`의 `timeout`은 **훅용**(10·5초)이지 MCP 도구용이 아님을 실측 확인했다.

## ⚠️ 5-B 판정 정정 — **`rate_limit`은 집계자 오탐이었다** (2026-08-22)

**오너가 사용량 화면으로 반증했다** — Claude 5시간 한도 **12%**, GPT **52% 남음**. 한도는 소진되지 않았다.

**원인 [실측]**: 실패 판정된 `p07`에만 `429`가 있었고(성공 3건은 0건), 그것은 **회사 주소 번지수** — `장안동 429-2`. 한도 정규식에 **맨숫자 429**가 있었고, 검사 범위가 `stdout+stderr`라 **모델이 쓴 KG JSON 본문까지** 신호로 읽었다. 두 엔진이 같은 파일에서 동시에 실패한 것도 이 오탐이다.

**수리**(커밋 `a6056cc`): `HTTP`/`status` 문맥이 있을 때만 429 · **stderr만** 신호 · **정상 종료(0)면 한도 아님** · claude 봉투는 `is_error:true`일 때만 본문을 읽음. 살해 케이스 4건 → **387케이스**.

| 항목 | 기존 | **정정** |
|---|---|---|
| claude 준수율 | 3/4 | **5/5**(p07 재측정 38.8초 성공 + p01 실생성 성공) |
| codex 준수율 | 3/4 | **3/3 확인 + 1건 미확인**(codex로는 재측정 안 함) |
| 큰 파일 소요 | `[미확인]` | **성공 확인**(p01 6,474자 → 27n·32r). 개별 소요는 배치에 묻혀 미측정 |

## 실제 파이프라인 첫 생성 — **검수 대상 6건 확보** (2026-08-22)

오너 지적(*"대상 url 을 알려줬잖아"*)대로 **이미 있는 자료로 실제 생성**했다. `data/Generated`에 **6건**(claude, 실패 0). 남은 대기 **22건**.

**`schema_version`이 1 그대로인 것은 정상** — 생성물의 **미등재 유형 0건**(대조 실행). §2.1의 "기존 유형 우선 재사용"이 작동해 기존 16 라벨·15 관계로 전부 표현했다. 격리 측정에서 신규가 많았던 것은 시드 스키마가 라벨 1개뿐이라 좁았기 때문.

**⇒ 슬라이스 6(S4 검수 워크플로) 착수 조건 충족.**
