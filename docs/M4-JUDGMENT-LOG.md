# M4 재판정 실기록 (2026-08-22)

> 실행 정본은 `M4-REJUDGMENT.md` v2.1. 이 파일은 **회차 실기록**이며 문항별 판정의 정본이다.
> 판정 표면: Claude Code(GraphRAG_1st 루트) · 서버 버전 확인 = §0-7 통과(`kg_status`에 "슬라이스 8" 문구)
> 개선 전 실적과의 대조 기준은 `spike-judgment-log.md`.

## 무효 처리 1건 — 회차 시작 전

| 시도 | 사유 |
|---|---|
| A3 1차 (`s-20260822-164001-jnzf`, 16:40) | **구버전 MCP 서버**. 요약에 M3에서 제거한 `과잉 인용 의심`이 출력됨. 검색 동작 자체는 정상이었으나(1층 3n·2r, 골든 S03과 kgid 전건 일치) 도구 문구 계층이 낡아 ③·판정 문구 대조가 성립하지 않는다. 판정 창 재시작 후 재실행. |

> 이 사고로 실행표에 **§0-7 서버 버전 확인**이 신설됐다(커밋 `e7776b5`). 재실행 시 첫 사용에서 정상 작동을 확인했다.

## 문항별 판정

| 문항 | 질문 일치 | ①호출 | ②시드(계층) | ③인용 | ④3상태(자동) | ⑤소요 | 모델 | 세션 | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| **A3** | ✓ | ✓ | 일륜도 **T1 확정** 1건 | 3/3 verified · 탈락 0 | **11/11 통과** | 회차 종료 후 일괄 | Opus 5 | **clear직후**(창 재시작) | 복원 문구 없음 = **이번 시행에서 손상 미발생**(정상). 1층 kgid가 골든 S03과 전건 일치 |

### A3 상세 근거

- **서버 기록**: `s-20260822-164920-zi8b` · 1층 노드 3 · 관계 2
- **골든 S03(정상 표기) 대조**: `n_acec44b628d14f03` · `n_69fdefdb99c1eb70` · `n_bcfe481dc38bafcd` / `r_f93a98f9c3db0d39` · `r_fed8ce82e1407251` — **kgid 5개 전건 일치**
- **④ 자동 대조**(`check-highlight`): searchId·질문 원문·1층 집합·N·truncated·2층⊆1층·렌더 겹침 없음·M==렌더 총수·citation 정합 — **전 11항 통과**
- **화면 실측**: 2층 = 1층 전체(`rendered.layer1` 0건 / `rendered.layer2` 3노드·2관계) → 노란색 3노드(하가네즈카 호타루·일륜도·카마도 탄지로) + 2관계(`PROVIDED`·`RECEIVED`), 나머지 dim. **이미지와 스냅샷이 일치.**
- **M3 신문구 발화 확인**: `1층의 5건 중 3건을 인용했습니다 — 2층이 1층과 거의 같아 강조 대비가 낮습니다.` (구버전 `과잉 인용 의심` 소멸). 1층 5건 = 노드 3 + 관계 2의 별칭 총수.
- **답변 품질**: 속성 부재를 **첫머리에 고지**(`summary: null`), 그래프가 아는 관계 2개만 서술, 그 이상을 지어내지 않음, `[비블리오마인드 답변]` 표기, 공백을 품질 루프 입력으로 제시. **항목 8 형식 준수.**

### ⑤ 집계 주의 (이 회차에 반드시 적용)

A3는 **판정 창 재시작 직후 첫 질문**이므로 소요 모집단은 **새 컨텍스트**다. 실행표 §2는 A3를 연속 대화로 계획했으므로, 회차 종료 후 집계 시 자동 분류를 덮어써야 한다.

```
node bibliomind/tools/measure-latency.mjs --since 2026-08-22T16:45 --fresh A3
```

## 진행 현황

| 단 | 진행 |
|---|---|
| 1단 챗 11문 | **1/11** (A3 완료) — 다음: B1 |
| 2단 서버 프로브 10문 | 대기(회차 종료 후) |
| 3단 손상 프로브 2문 | 대기 |
