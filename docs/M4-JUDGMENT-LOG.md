# M4 재판정 실기록 (2026-08-22)

> 실행 정본은 `M4-REJUDGMENT.md` v2.1. 이 파일은 **회차 실기록**이며 문항별 판정의 정본이다.
> 판정 표면: Claude Code(GraphRAG_1st 루트) · 서버 버전 확인 = §0-7 통과(`kg_status`에 "슬라이스 8" 문구)
> 개선 전 실적과의 대조 기준은 `spike-judgment-log.md`.

## 무효 처리 1건 — 회차 시작 전

| 시도 | 사유 |
|---|---|
| A3 1차 (`s-20260822-164001-jnzf`, 16:40) | **구버전 MCP 서버**. 요약에 M3에서 제거한 `과잉 인용 의심`이 출력됨. 검색 동작 자체는 정상이었으나(1층 3n·2r, 골든 S03과 kgid 전건 일치) 도구 문구 계층이 낡아 ③·판정 문구 대조가 성립하지 않는다. 판정 창 재시작 후 재실행. |

> 이 사고로 실행표에 **§0-7 서버 버전 확인**이 신설됐다(커밋 `e7776b5`). 재실행 시 첫 사용에서 정상 작동을 확인했다.

## 문항별 판정

| 문항 | 질문 일치 | ①호출 | ②시드(계층) | ③인용 | ④3상태(자동) | ⑤소요 | 모델 | 세션 | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| **A3** | ✓ | ✓ | 일륜도 **T1 확정** 1건 | 3/3 verified · 탈락 0 | **11/11 통과** + **색 검증 통과** | 회차 종료 후 일괄 | Opus 5 | **clear직후** | 복원 문구 없음 = **이번 시행에서 손상 미발생**(정상). 1층 kgid가 골든 S03과 전건 일치. 정본 실행 = `s-20260822-171015-5lmq` |

| **B1** | ✓ | ✓ | 탄지로 **T2 확정** 3건 | 4/4 verified · 탈락 0 | **11/11 통과** + **색 3상태 전건 일치** | 회차 종료 후 일괄 | Opus 5 | 연속 | **골든 S06과 시드·1층 완전 일치(20n·31r)** = M2 무개입의 챗 표면 실증 |

### B1 상세 근거

- **실행**: `s-20260822-171730-0zq5` · 1층 **20노드·31관계** · 2층 5노드·4관계
- **골든 S06(개선 전 기준선) 대조**: 시드 `탄지로:T2` **3건**·1층 **20n·31r**·노드 kgid 집합 **전건 일치** → **T2 하한이 탄지로 경로를 건드리지 않았음이 챗 표면에서 확인됐다.** 2단 서버 프로브가 판별력 0이었던 것과 달리, 이 문항은 **실제로 T2 분기를 통과**한다.
- **④ 자동 대조**: 전 11항 통과
- **색 3상태 검증(§4-b)** — 이번 회차에서 세 상태가 모두 발현했다:

| 상태 | 기대(스냅샷 산술) | 실측 색·개수 | 판정 |
|---|---|---|---|
| 2층 노드 | 5 | `#facc15` **5**(카마도 탄지로·우로코다키 사콘지·카마도 가족·귀살대·귀살대의 최종선별) | **일치** |
| **1층 전용 노드** | 20 − 5 = **15** | 원색 밝기상향(파스텔) **15** | **일치** |
| 밖 노드 | 29 − 20 = **9** | `#475569` **9** | **일치** |
| 2층 관계 | 4 | `#facc15` **4** | **일치** |
| **1층 전용 관계** | 31 − 4 = **27** | `#e2e8f0` **27** | **일치** |
| 밖 관계 | 55 − 31 = **24** | `#475569` **24** | **일치** |

  노란 5개가 답변이 밝힌 인용 4경로(귀살대 가입·카마도 가족·최종선별 통과·우로코다키 소개)의 양 끝과 정확히 대응한다.
- **인용 밀도 안내 미발화**: 4/51 = 7.8%로 50% 문턱 아래 → 문구가 뜨지 않는 것이 정상.
- **답변 품질**: 소속을 **조직/가족 두 층위로 분리**해 답하고, 질문이 조직을 물었다면 답은 귀살대라고 특정. **공백도 특정**했다 — 계급·소속 부대·상급자 정보 없음, "가입했다"까지만 확인됨.

### A3 상세 근거

- **정본 실행**: `s-20260822-171015-5lmq`(17:10) · 1층 노드 3 · 관계 2. *(앞선 `…zi8b`도 버전 확인 후 실행돼 유효했으나, 오너가 A3부터 다시 진행해 이 실행을 정본으로 삼는다. 두 실행의 digest는 동일.)*
- **골든 S03(정상 표기) 대조**: `n_acec44b628d14f03` · `n_69fdefdb99c1eb70` · `n_bcfe481dc38bafcd` / `r_f93a98f9c3db0d39` · `r_fed8ce82e1407251` — **kgid 5개 전건 일치**
- **④ 자동 대조**(`check-highlight`): searchId·질문 원문·1층 집합·N·truncated·2층⊆1층·렌더 겹침 없음·M==렌더 총수·citation 정합 — **전 11항 통과**
- **화면 실측**: 2층 = 1층 전체(`rendered.layer1` 0건 / `rendered.layer2` 3노드·2관계) → 노란색 3노드(하가네즈카 호타루·일륜도·카마도 탄지로) + 2관계(`PROVIDED`·`RECEIVED`), 나머지 dim. **이미지와 스냅샷이 일치.**
- **M3 신문구 발화 확인**: `1층의 5건 중 3건을 인용했습니다 — 2층이 1층과 거의 같아 강조 대비가 낮습니다.` (구버전 `과잉 인용 의심` 소멸). 1층 5건 = 노드 3 + 관계 2의 별칭 총수.
- **답변 품질**: 속성 부재를 **첫머리에 고지**(`summary: null`), 그래프가 아는 관계 2개만 서술, 그 이상을 지어내지 않음, `[비블리오마인드 답변]` 표기, 공백을 품질 루프 입력으로 제시. **항목 8 형식 준수.**

### 색 적용 검증 (§4-b — 이 회차에서 처음 적용)

스냅샷은 "앱이 어느 집합을 2층으로 판정했는가"까지만 증명한다. 실제로 그 색으로 칠했는지를 `window.scene` 순회로 셌다.

| 상태 | 기대(스냅샷 산술) | 실측 색·개수 | 판정 |
|---|---|---|---|
| 2층 노드 | 3 | `#facc15` 메시 **3**(카마도 탄지로·하가네즈카 호타루·일륜도) | **일치** |
| 2층 관계 | 2 | `#facc15` **2** | **일치** |
| 1층 전용 | 0 (2층이 1층 전체) | 파스텔·`#e2e8f0` **0건** | **일치** |
| 밖 노드 | 29 − 3 = 26 | `#475569` **26** | **일치** |
| 밖 관계 | 55 − 2 = 53 | `#475569` **53** | **일치** |

노드 버킷의 객체 33개 중 4개는 이름이 잡히지 않는 부속 객체(라벨류로 추정)이며 색 산술에는 영향이 없다 — 메시 29개(26 dim + 3 노랑)가 그래프 노드 수와 정확히 일치한다. 부속 객체의 정체는 `[미확인]`.

### ⑤ 집계 주의 (이 회차에 반드시 적용)

A3는 **판정 창 재시작 직후 첫 질문**이므로 소요 모집단은 **새 컨텍스트**다. 실행표 §2는 A3를 연속 대화로 계획했으므로, 회차 종료 후 집계 시 자동 분류를 덮어써야 한다.

```
node bibliomind/tools/measure-latency.mjs --since 2026-08-22T16:45 --fresh A3
```

## 진행 현황

| 단 | 진행 |
|---|---|
| 1단 챗 11문 | **2/11** (A3·B1 완료) — 다음: B2 |
| 2단 서버 프로브 10문 | 대기(회차 종료 후) |
| 3단 손상 프로브 2문 | 대기 |
