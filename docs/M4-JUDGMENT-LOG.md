# M4 재판정 실기록 (2026-08-22)

> 실행 정본은 `M4-REJUDGMENT.md` v2.1. 이 파일은 **회차 실기록**이며 문항별 판정의 정본이다.
> 판정 표면: Claude Code(GraphRAG_1st 루트) · 서버 버전 확인 = §0-7 통과(`kg_status`에 "슬라이스 8" 문구)
> 개선 전 실적과의 대조 기준은 `spike-judgment-log.md`.

## 무효 처리 1건 — 회차 시작 전

| 시도 | 사유 |
|---|---|
| A3 1차 (`s-20260822-164001-jnzf`, 16:40) | **구버전 MCP 서버**. 요약에 M3에서 제거한 `과잉 인용 의심`이 출력됨. 검색 동작 자체는 정상이었으나(1층 3n·2r, 골든 S03과 kgid 전건 일치) 도구 문구 계층이 낡아 ③·판정 문구 대조가 성립하지 않는다. 판정 창 재시작 후 재실행. |

> 이 사고로 실행표에 **§0-7 서버 버전 확인**이 신설됐다(커밋 `e7776b5`). 재실행 시 첫 사용에서 정상 작동을 확인했다.

## 문항별 판정

| 문항 | 질문 일치 | ①호출 | ②시드(계층) | ③인용 | ④3상태(자동) | ⑤소요 | 모델 | 세션 | 비고 |
|---|---|---|---|---|---|---|---|---|---|
| **A3** | ✓ | ✓ | 일륜도 **T1 확정** 1건 | 3/3 verified · 탈락 0 | **11/11 통과** + **색 검증 통과** | 회차 종료 후 일괄 | Opus 5 | **clear직후** | 복원 문구 없음 = **이번 시행에서 손상 미발생**(정상). 1층 kgid가 골든 S03과 전건 일치. 정본 실행 = `s-20260822-171015-5lmq` |

| **B1** | ✓ | ✓ | 탄지로 **T2 확정** 3건 | 4/4 verified · 탈락 0 | **11/11 통과** + **색 3상태 전건 일치** | 회차 종료 후 일괄 | Opus 5 | 연속 | **골든 S06과 시드·1층 완전 일치(20n·31r)** = M2 무개입의 챗 표면 실증 |

| **B2** | ✓ | ✓ | 네즈코 **T2 확정** 4건 | 4/4 verified · 탈락 0 | **11/11 통과** + **색 3상태 전건 일치** | 회차 종료 후 일괄 | Opus 5 | 연속 | **M2 핵심 명제 실증** — 시드 5→4, 1층 13n·21r→**11n·18r**, 제거 kgid까지 예측과 동일 |

| **B4** | ✓ | ✓ | 호타루 **T2 확정** 1건 | 3/3 verified · 탈락 0 | **11/11 통과** + **색 3상태 전건 일치** | 회차 종료 후 일괄 | Opus 5 | 연속 | **B2와 짝 — 같은 노드가 정반대로 처리됨.** 골든 S09와 완전 일치(무개입) |

| **C4** | ✓ | ✓ | 네즈코 **T2** 4건 + **재갈 T1** 1건 = 5건 | 5/5 verified · 탈락 0 | **11/11 통과** + **색 3상태 전건 일치** | 회차 종료 후 일괄 | Opus 5 | 연속 | 13n·21r→**11n·18r** 예측 일치. **`재갈` T1 불변 = 워터폴 단락 실증** |

| **D1** | ✓ | **✓ 호출 2회** | 시드 0건(기대대로 — 그래프에 부재) | 빈 인용 제출 ✓ | **11/11 통과** + **잔상 소거 실증** | 회차 종료 후 일괄 | Opus 5 | **clear직후** | 유명 IP 함정 통과. `[비블리오마인드 답변]` **미표기**(정확) |

| **D2** | ✓ | **✓ 호출** | 탄지로 **T2** · 1층 20n·31r | 7/7 verified · 탈락 0 | **11/11 통과** + **색 3상태 전건 일치** | 회차 종료 후 일괄 | Opus 5 | **clear직후** | **개선 전 침묵 실패 2회 재현 지점 — 이번엔 호출.** 사다리 1단(질의 규칙) 효력 유지 확인 |

| **D3** | ✓ | **✓ 호출** | 네즈코 T2 4건 + 탄지로 T2 3건 = **7건** · 1층 24n·42r | 4/4 verified(관계 3·노드 1) · 탈락 0 | **11/11 통과** + **색 3상태 전건 일치** | 회차 종료 후 일괄 | Opus 5 | **clear직후** | **합성 검증 일치** — 다중 키워드 결과가 개별 골든의 합집합과 정확히 같다 |

| **E1** | ✓ | **✓ 호출 2회** | 시드 0건(기대대로 — 노드 부재) | 빈 인용 제출 ✓ | **11/11 통과** + 잔상 소거 | 회차 종료 후 일괄 | Opus 5 | **clear직후** | **치명 실패 0.** 개선 전 이 자리에서 났던 **키워드 손상 재발 없음** |

### E1 상세 근거 — **부정 대조 + 키워드 손상 회귀 확인**

- **실행**: `s-20260822-181416-9lkq` · 시드 0건 · 1층 0n·0r
- **①호출**: `무잔` → 0건 → **표기 변형을 의심해 `키부츠지`로 1회 재검색** → 0건. 항목 5 준수이면서, **부재 원인을 좁히려는 시도**까지 했다.
- **치명 실패 0**: 환각 답변도 허위 인용도 없다. 그래프 밖 지식(무잔 = 귀신의 시조·최종 흑막)을 밝히되 *"이 진술은 지식그래프의 근거가 아니며, **현재 로드된 그래프가 어떤 작품인지와도 대조하지 못했다**"* 고 적었다 — 오늘 답변 중 **가장 보수적인 처리**다.
- **표기 규율**: `[비블리오마인드 답변]` **미표기**(정확).
- **④ 자동 대조**: 전 11항 통과. 잔상 소거 확인 — 노드 29 전부 원색·관계 55 전부 기본색.
- **부재 원인 2갈래 제시**: ①작품 미적재 ②표기 차이로 검색 미포착. `kg_status`로 갈린다고 안내 — **공백을 품질 루프 입력으로 되돌리는** 확정 원칙 그대로다.

#### 🔬 회차 전체 키워드 손상 조사 (합격선 4번 절차 (a) — 실행표 v2 신설분의 첫 가동)

```
node scripts/refresh-keyword-probes.js --since 2026-08-22T16:45
node scripts/check-keywords.js --fixture mcp-server/fixtures/keyword-probes-m4.json
```

| 항목 | 실측 |
|---|---|
| 수집 프로브 | **16건** · 키워드 토큰 **20개** |
| 질문 채널 | tool-arg 16건 (직전 발화 대체 0건) |
| **손상 의심** | **0건** |
| **거짓 부재 후보** | **0건** |

**개선 전 이 자리에서 `혈귀` → `혐귀` 손상이 발생했으나(실측 손상률 5.4%), 이번 회차에서는 20토큰 전건 무손상.** 손상은 확률적 사건이므로 이것이 손상률 0을 뜻하지는 않는다 — **이번 회차에 거짓 부재가 없었다**는 것이 확인된 명제다.

### D3 상세 근거 — **다중 키워드 합성의 검증**

- **실행**: `s-20260822-181021-jyc3` · 1층 **24노드·42관계** · 2층 3노드·3관계
- **합성 검증**(이 조합은 골든 케이스에 없으므로 개별 케이스로 합성해 대조):

| 구성 | 시드 | 1층 노드 |
|---|---|---|
| 골든 S07 `네즈코` | 4건 | — |
| 골든 S06 `탄지로` | 3건 | — |
| **합계 / 합집합** | **7건** | **24** |
| **실제 챗 D3** | **7건** | **24** |

  다중 키워드 검색이 **개별 결과의 합성과 정확히 일치**한다 — 키워드별 독립 판정이 다시 확인된다.
- **하한의 국소성 확인(중요)**: `하가네즈카 호타루`는 `네즈코` 시드에서는 잘렸지만, 이 검색에서는 **`탄지로` 경로의 1홉 이웃으로 1층에 들어왔다**(화면 색 `#fea8c6` = 1층 전용). **하한은 시드 선별 단계에만 작용하고 1홉 확장을 막지 않는다** — 설계대로다.
- **④ 자동 대조**: 전 11항 통과. `rendered.layer1`을 `layer1 − layer2`로 재구성한 값(21n·39r)이 화면 색 실측(파스텔 21·`#e2e8f0` 39)과 정확히 일치.
- **색 3상태**: 노드 29 = 2층 **3** + 1층전용 **21** + 밖 **5** / 관계 55 = 2층 **3** + 1층전용 **39** + 밖 **13** — 전건 일치. 밖에 남은 5개는 `다이쇼 시대·인간·만화 1~13화·단행본 1~2권·애니메이션 1~5화`로 **그래프 주변부뿐**이다.
- **인용 구성 일치**: 답변이 밝힌 "관계 3 / 노드 1"이 스냅샷의 2층 3관계·(그 양 끝 포함) 3노드와 정합.
- **답변 품질**: `OLDER_BROTHER_OF` 직접 관계와 `MEMBER_OF` 두 경로로 **이중 확인**했고, 혈연 외 관계(PROTECTED·ATTEMPTED_TO_TAKE_FOR_TREATMENT·공격 사건)까지 덧붙였다.

### D2 상세 근거 — **과거 실패 자리의 재확인 + 3중 교차 검증**

- **실행**: `s-20260822-180653-lnjl` · 1층 **20노드·31관계** · 2층 5노드·5관계
- **①호출(핵심)**: 개선 전 판정에서 이 문항은 **침묵 실패가 2회 재현**됐고(모델이 도구를 부르지 않고 사전지식으로 답함), `GraphRAG_1st/CLAUDE.md`에 질의 규칙을 명문화하는 **사다리 1단**을 적용한 뒤에야 통과했다. **이번 회차에서도 호출** — 그 교정이 **다른 세션·다른 회차에서도 유지된다**는 확인이다.
- **3중 교차 검증**(같은 키워드 `탄지로`를 쓰는 세 경로가 일치해야 한다):

| 경로 | 1층 |
|---|---|
| B1 (챗, 17:17) | 20n · 31r |
| **D2 (챗, 18:06)** | **20n · 31r** |
| 골든 S06 (서버 프로브) | 20n · 31r |

  **노드·관계 kgid 집합이 셋 다 동일.** 서로 다른 시각·다른 컨텍스트(연속 vs `/clear` 직후)의 두 챗 실행과 서버 프로브가 같은 결과를 낸다 — **결정론이 챗 표면에서 확인됐다.**
- **④ 자동 대조**: 전 11항 통과
- **색 3상태**: 노드 29 = 2층 **5** + 1층전용 **15** + 밖 **9** / 관계 55 = 2층 **5** + 1층전용 **26** + 밖 **24** — 전건 일치. *(1층전용 값을 `layer1 − layer2`로 재구성해 화면 색 개수와 대조했고 정확히 맞았다.)*
- **답변 품질**: 부재 유형을 **"노드는 있으나 해당 속성 부재"** 로 정확히 특정(항목 8). 나아가 **행동 기록에서 성격을 읽는 것은 자기 해석임을 `[추정]` 등급으로 분리**했다 — 그래프 사실과 추론의 경계를 스스로 그은 모범 사례.

### D1 상세 근거 — **호출률 함정 통과 + 잔상 소거 실증**

- **실행**: `s-20260822-180325-ypxv`("귀멸의 칼날") → `s-20260822-180334-y6gz`("귀멸") — 서버 기록 2건으로 **2회 호출 확인**
- **①호출(합격선 8번, v2 신설)**: 사전지식으로 답해버리기 가장 쉬운 문항인데 **호출했다.** 시드 0건 후 **핵심어를 줄여 1회 재호출**한 것도 인용 지침 항목 5 준수다.
- **③인용**: 빈 인용 제출(항목 4 준수) → `citationStatus: none`
- **표기 규율**: 그래프 근거가 없으므로 `[비블리오마인드 답변]`을 **붙이지 않았다**(항목 7). 그래프 밖 지식(주인공 = 카마도 탄지로)은 밝히되 **"공백의 가시화가 정답"** 이라고 스스로 규정했다.
- **④ 자동 대조**: 전 11항 통과. `active:false`·`emptyResult:true`·`citation:none` 분기가 모두 정상 판정됐다.
- **잔상 소거 실증(§7.6)**: 직전 C4의 하이라이트가 **완전히 지워졌다** — 노드 29개 **전부 원색**(dim 0·노랑 0), 관계 55개 **전부 기본색 `#94a3b8`**. 빈 1층은 **화면을 dim하지 않고 이전 하이라이트를 대체·소거**한다는 설계가 색 단위로 확인됐다. *(이 동작은 오너가 이전에 확정·구현한 것으로, 이번 회차에서 색으로 재확인한 것이다.)*
- **부정 대조 계열 일관성**: 골든의 S17(무잔)·S18(혈귀)도 시드 0·unmatched·0n으로 같은 형태다.

### C4 상세 근거 — **워터폴 단락 구조의 실증**

- **실행**: `s-20260822-175947-f33o` · 1층 **11노드·18관계** · 2층 5노드·5관계
- **3자 대조**(골든 M03):

| | 시드 | 1층 | 계층 |
|---|---|---|---|
| 개선 전 M03 | 6건 | 13n · 21r | 네즈코:T2, 재갈:T1 |
| 개선 후 예측 | 5건 | 11n · 18r | 네즈코:T2, **재갈:T1** |
| **실제 챗 C4** | **5건** | **11n · 18r** | 네즈코:T2, **재갈:T1** |

  노드 kgid 집합까지 예측과 일치.
- **"재갈 함정" 해소**: 설계 단계에서 `재갈`의 T2 원점수에 하한(50%)에 걸릴 후보가 있어 위험 신호로 보였으나, **`재갈`은 T1 완전일치라 워터폴이 T2 분기에 진입조차 하지 않는다**는 것이 근거였다. 이번 실행에서 **개선 전·후 모두 `재갈:T1`·같은 시드로 불변**임이 확인돼 그 근거가 챗 표면에서 실증됐다.
- **하한은 같은 검색 안에서 다른 키워드에만 작용**: 같은 호출에서 `네즈코`는 하한을 거쳐 4건으로 줄고(하가네즈카 호타루 `#475569` dim), `재갈`은 손대지 않았다. **키워드별 독립 판정**이 확인된다.
- **④ 자동 대조**: 전 11항 통과
- **색 3상태**: 노드 29 = 2층 **5** + 1층전용 **6**(=11−5) + 밖 **18**(=29−11) / 관계 55 = 2층 **5** + 1층전용 **13**(=18−5) + 밖 **37**(=55−18) — 전건 일치. `재갈`은 `#facc15`.
- **답변 품질**: **직접 관계가 없다는 것을 먼저 밝히고** 사건 노드를 매개로 한 3-홉 경로(대상·도구·행위자)로 답했다. 주변 관계(SPARED·STOPPED)를 언급하되 **"그래프는 이 둘과 재갈 사건을 인과로 연결하지 않았다"** 고 명시해 추론과 기록을 분리했다.

> **품질 루프 신호 보강**: 모델이 `기유가 네즈코에게 재갈을 물린 사건`(행위자·대상·도구 완비)과 `네즈코의 도깨비화`(행위자 부재)를 **같은 Event 라벨인데 추출 품질이 다르다**고 대조했다. B2의 신호에 "어떤 조건에서 행위자가 붙는가"라는 구체적 조사 축이 더해졌다.

### B4 상세 근거 — **임계값 50%의 양방향 증명 완성**

- **실행**: `s-20260822-173625-03q6` · 1층 **3노드·3관계** · 2층 3노드·2관계
- **골든 S09 대조**: 개선 전 == 개선 후 == 실제 챗, 노드 kgid 집합까지 전건 일치 → **무개입 확인**
- **대칭 증명** — 같은 노드 `n_69fdefdb99c1eb70`(하가네즈카 호타루):

| 문항 | 그 노드의 지위 | 하한 판정 | 화면 색 |
|---|---|---|---|
| **B2** "네즈코는 어떻게…" | **오탐**(부분 문자열 일치, 1위 대비 0.39) | **잘림** | `#475569` dim |
| **B4** "호타루는 무엇을…" | **정탐**(T2 1위) | **생존** | `#facc15` 노랑 |

  **하한이 노드를 미워하는 게 아니라 질문 대비 적합도를 본다**는 것이 kgid 단위로 증명됐다. 이 두 문항이 함께 통과해야 "50%가 옳은 임계값"이 성립하며, **성립했다.**
- **④ 자동 대조**: 전 11항 통과
- **색 3상태**: 노드 29 = 2층 **3** + 1층전용 **0** + 밖 **26** / 관계 55 = 2층 **2** + 1층전용 **1**(`#e2e8f0`) + 밖 **52** — 전건 일치
- **경계값 확인(신규)**: 인용 3건 / 1층 별칭 6건 = **정확히 50%**인데 밀도 안내가 **뜨지 않았다**. 판정식이 `submitted > layer1Total × 0.5`(초과)라 경계값은 배제된다 — 설계대로다.
- **답변 품질**: 답이 **있는** 경우의 모범 — 속성("대장장이")과 관계 2건을 근거로 답하고, 없는 것(재료·공방·제작 과정)도 함께 특정했다. 직전 질의와의 연결(`일륜도` 관계 재등장)도 스스로 짚었다.

### B2 상세 근거 — **이 회차의 결정적 증거**

- **실행**: `s-20260822-173247-u21f` · 1층 **11노드·18관계** · 2층 4노드·4관계
- **3자 대조** (개선 전 골든 ↔ 개선 후 예측 ↔ 실제 챗):

| | 시드 | 1층 |
|---|---|---|
| 개선 전 골든 S07 | 5건 | 13n · 21r |
| 개선 후 예측(postM2) | 4건 | 11n · 18r |
| **실제 챗 B2** | **4건** | **11n · 18r** |

  **노드 kgid 집합이 예측과 완전 일치**하며, 제거된 2개(`n_69fdefdb99c1eb70` 하가네즈카 호타루 · `n_acec44b628d14f03` 일륜도)가 **실제 화면에서도 빠졌다**(색 조회 결과 둘 다 `#475569` dim). 오너가 승인한 **동반 효과가 예측대로 발생**했다.
- **관측 채널이 챗까지 도달했다**: 모델이 답변 말미에 *"하가네즈카 호타루가 '네즈코' 부분 문자열 일치로 후보에 올랐다가 **점수 기준(0.39)**에서 걸러졌습니다"* 라고 스스로 보고했다. 이 0.39는 M2에서 신설한 `data.seedFloorCuts`의 `ratio`(실측 0.3919)이며, **하한이 왜 잘랐는지를 사용자에게 설명할 수 있는 상태**가 됐다는 뜻이다.
- **④ 자동 대조**: 전 11항 통과
- **색 3상태 검증**: 노드 29 = 2층 **4** + 1층전용 **7**(=11−4) + 밖 **18**(=29−11) / 관계 55 = 2층 **4** + 1층전용 **14**(=18−4) + 밖 **37**(=55−18) — **전건 일치**
- **답변 품질**: *"어떻게"* 에 답이 없음을 **비교로 특정**했다 — 같은 결과 안의 `기유가 네즈코에게 재갈을 물린 사건`에는 행위자·도구가 붙어 있는데 도깨비화 사건만 비어 있다는 대조. 나아가 **독자의 추론과 그래프의 기록을 분리**했다(몰살 사건과 도깨비화는 인과로 연결돼 있지 않다).

> **품질 루프 신호(제품 발견)**: 모델이 3문항 연속 같은 유형의 공백을 지적했다 — ①일륜도 설명 속성 부재 ②탄지로 계급·부대 부재 ③도깨비화 사건의 원인·행위자 부재. **"어떻게/왜" 질문 유형 전반이 현재 그래프로 답이 안 나온다는 신호**이며, 추출 프롬프트가 인과 관계(`CAUSED_BY`·`HAS_AGENT`)를 사건 노드에 충분히 붙이지 못하는지 점검할 근거다. **M5 이후 개선 큐 등재 대상.**

### B1 상세 근거

- **실행**: `s-20260822-171730-0zq5` · 1층 **20노드·31관계** · 2층 5노드·4관계
- **골든 S06(개선 전 기준선) 대조**: 시드 `탄지로:T2` **3건**·1층 **20n·31r**·노드 kgid 집합 **전건 일치** → **T2 하한이 탄지로 경로를 건드리지 않았음이 챗 표면에서 확인됐다.** 2단 서버 프로브가 판별력 0이었던 것과 달리, 이 문항은 **실제로 T2 분기를 통과**한다.
- **④ 자동 대조**: 전 11항 통과
- **색 3상태 검증(§4-b)** — 이번 회차에서 세 상태가 모두 발현했다:

| 상태 | 기대(스냅샷 산술) | 실측 색·개수 | 판정 |
|---|---|---|---|
| 2층 노드 | 5 | `#facc15` **5**(카마도 탄지로·우로코다키 사콘지·카마도 가족·귀살대·귀살대의 최종선별) | **일치** |
| **1층 전용 노드** | 20 − 5 = **15** | 원색 밝기상향(파스텔) **15** | **일치** |
| 밖 노드 | 29 − 20 = **9** | `#475569` **9** | **일치** |
| 2층 관계 | 4 | `#facc15` **4** | **일치** |
| **1층 전용 관계** | 31 − 4 = **27** | `#e2e8f0` **27** | **일치** |
| 밖 관계 | 55 − 31 = **24** | `#475569` **24** | **일치** |

  노란 5개가 답변이 밝힌 인용 4경로(귀살대 가입·카마도 가족·최종선별 통과·우로코다키 소개)의 양 끝과 정확히 대응한다.
- **인용 밀도 안내 미발화**: 4/51 = 7.8%로 50% 문턱 아래 → 문구가 뜨지 않는 것이 정상.
- **답변 품질**: 소속을 **조직/가족 두 층위로 분리**해 답하고, 질문이 조직을 물었다면 답은 귀살대라고 특정. **공백도 특정**했다 — 계급·소속 부대·상급자 정보 없음, "가입했다"까지만 확인됨.

### A3 상세 근거

- **정본 실행**: `s-20260822-171015-5lmq`(17:10) · 1층 노드 3 · 관계 2. *(앞선 `…zi8b`도 버전 확인 후 실행돼 유효했으나, 오너가 A3부터 다시 진행해 이 실행을 정본으로 삼는다. 두 실행의 digest는 동일.)*
- **골든 S03(정상 표기) 대조**: `n_acec44b628d14f03` · `n_69fdefdb99c1eb70` · `n_bcfe481dc38bafcd` / `r_f93a98f9c3db0d39` · `r_fed8ce82e1407251` — **kgid 5개 전건 일치**
- **④ 자동 대조**(`check-highlight`): searchId·질문 원문·1층 집합·N·truncated·2층⊆1층·렌더 겹침 없음·M==렌더 총수·citation 정합 — **전 11항 통과**
- **화면 실측**: 2층 = 1층 전체(`rendered.layer1` 0건 / `rendered.layer2` 3노드·2관계) → 노란색 3노드(하가네즈카 호타루·일륜도·카마도 탄지로) + 2관계(`PROVIDED`·`RECEIVED`), 나머지 dim. **이미지와 스냅샷이 일치.**
- **M3 신문구 발화 확인**: `1층의 5건 중 3건을 인용했습니다 — 2층이 1층과 거의 같아 강조 대비가 낮습니다.` (구버전 `과잉 인용 의심` 소멸). 1층 5건 = 노드 3 + 관계 2의 별칭 총수.
- **답변 품질**: 속성 부재를 **첫머리에 고지**(`summary: null`), 그래프가 아는 관계 2개만 서술, 그 이상을 지어내지 않음, `[비블리오마인드 답변]` 표기, 공백을 품질 루프 입력으로 제시. **항목 8 형식 준수.**

### 색 적용 검증 (§4-b — 이 회차에서 처음 적용)

스냅샷은 "앱이 어느 집합을 2층으로 판정했는가"까지만 증명한다. 실제로 그 색으로 칠했는지를 `window.scene` 순회로 셌다.

| 상태 | 기대(스냅샷 산술) | 실측 색·개수 | 판정 |
|---|---|---|---|
| 2층 노드 | 3 | `#facc15` 메시 **3**(카마도 탄지로·하가네즈카 호타루·일륜도) | **일치** |
| 2층 관계 | 2 | `#facc15` **2** | **일치** |
| 1층 전용 | 0 (2층이 1층 전체) | 파스텔·`#e2e8f0` **0건** | **일치** |
| 밖 노드 | 29 − 3 = 26 | `#475569` **26** | **일치** |
| 밖 관계 | 55 − 2 = 53 | `#475569` **53** | **일치** |

노드 버킷의 객체 33개 중 4개는 이름이 잡히지 않는 부속 객체(라벨류로 추정)이며 색 산술에는 영향이 없다 — 메시 29개(26 dim + 3 노랑)가 그래프 노드 수와 정확히 일치한다. 부속 객체의 정체는 `[미확인]`.

### ⑤ 집계 주의 (이 회차에 반드시 적용)

A3는 **판정 창 재시작 직후 첫 질문**이므로 소요 모집단은 **새 컨텍스트**다. 실행표 §2는 A3를 연속 대화로 계획했으므로, 회차 종료 후 집계 시 자동 분류를 덮어써야 한다.

```
node bibliomind/tools/measure-latency.mjs --since 2026-08-22T16:45 --fresh A3
```

## 진행 현황

| 단 | 진행 |
|---|---|
| 1단 챗 11문 | **9/11** — A~C 5문 + D군 3/3 + E1. 다음: **`/clear` → E2** |
| 2단 서버 프로브 10문 | 대기(회차 종료 후) |
| 3단 손상 프로브 2문 | 대기 |
