# 슬라이스 10 — MVP 통합 검증 실행표 (검증 계약)

> 작성: 2026-08-28. 합격선 정본 = ROADMAP 슬라이스 10 행("성공 기준 1~9 전 항목 체크, 엔진 2종×클라이언트 2종 매트릭스 → Phase 4 진입") + PRD §5 성공 기준 1~9.
> **검증 전략(계약)**: 2026-08-28 저장소 통합은 코드 불변 스냅샷 이식이다 — 따라서 **자료 기반 기존 실측 증거는 이월**하고, 환경(저장소 루트·MCP 등록 경로·클라이언트 재등록)에 민감한 **실왕복 경로만 통합 환경에서 재검증**한다. 전 매트릭스 재판정은 하지 않는다(코드 동일성이 근거 — 이식 커밋 `0a73b6d` = GraphRAG_1st@`719f280` 트리). 판정 5값: 달성·미달·미측정·오너판정대기·면제(WAIVED).

## 매트릭스 — 성공 기준 × 증거 × 신환경 재검증

| # | 성공 기준 (PRD §5) | 이월 증거 [실측] | 신환경 재검증 | 담당 |
|---|---|---|---|---|
| 1 | 수집(S1) — 상한 10·파일명 규칙·멱등·재시도 | 슬라이스 3 합격선 8/8(실크롤 10/10) + 8.5 MCP 왕복 멱등(스킵 1·시도 0) | R1: 통합 폴더 기준 collect 왕복 스모크(멱등·실패 환원) | 집계자 |
| 2 | 추출(S2) — 디지털 PDF·이미지, 외부 전송 없음 | 슬라이스 4 합격선 7/7(OCR 5줄 전건 정확) + 8.5 왕복 | R1에 포함(collect_docs 스킵 왕복) | 집계자 |
| 3 | 생성(S3) — 스키마 통과 KG, **두 엔진 각 1회** | 슬라이스 5 준수율 **10/10**(codex 5·claude 5, failover=false) + 챗 실측 4건 | R2: 통합 환경 각 클라이언트에서 생성 1건씩(엔진 기본 codex — 엔진 축은 이월로 충족) | 오너 |
| 4 | 조작 원칙(S4·S5) — 검수→표시→승인→주입 무복붙 | 슬라이스 6 합격선 9/9 + 2026-08-28 실검수(승인 1·반려 1·재빌드 151/226) | R3: 통합 환경에서 검수 1건 왕복(표시→판정→재빌드) | 오너 |
| 5 | **하이라이트 ★ — 양 클라이언트** 1층 무조건·2층 인용·탈락 안내 | M4 재판정(Claude 21문) + M5(Codex 21문 합격선 7/7) — 판정축 완결 | R4: 각 클라이언트 질문 1문 — 1층+2층(파티클) 화면 확인 | 오너 |
| 6 | 교차 자료 연결 — 자료 경계 넘는 하이라이트 | 2026-08-23 실증(리디안솔루션 병합 — p02 16경로+p05 3경로 동시) | R4의 질문을 교차형으로 선정해 겸검증 | 오너 |
| 7 | 되돌리기 — 반려 재빌드 복원·재주입 멱등 | 슬라이스 7(2회 연속 동일) + 2026-08-28 반려 실사용 | R3의 재빌드가 겸검증(수치 = 직전과 대조) | 오너 |
| 8 | 실패 보고 — 실패 요약이 챗 응답으로 | 다수 실증(§4.1 전 도구·toolShape #7·collect 실패 환원) | R1의 실패 환원 케이스가 겸검증 | 집계자 |
| 9 | 클론 재현 — README만으로 데모 재현 | 2026-08-28 새 클론 스모크(install→setup→**584**→도구 **15종**) + README = 실행 순서 그대로 | R5: 푸시 허브 e2e(신환경 9/9) — 데모 인프라 확인 | 집계자 |

**매트릭스 축 충족 근거**: 엔진 2종 = 슬라이스 5(각 5회) / 클라이언트 2종 = M4·M5(각 21문) + R2~R4가 통합 환경에서 각 클라이언트를 재점검.

## 신환경 재검증 실행 기록

- **R1 (집계자)**: 통합 폴더 기준 MCP stdio 왕복 — 결과: **7/7 통과 [실측 2026-08-28]** (tools/list 15종·collect_web 멱등 스킵 1/시도 0·collect_docs 스킵·실패 환원 — 격리·외부 접촉 0). → 성공 기준 1·2·8의 신환경분 충족.
- **R5 (집계자)**: `npm run hub:e2e` 신환경 — 결과: **9/9 통과 [실측 2026-08-28]** (라우트 3종·SSE·재생 규칙·health). → 성공 기준 9의 데모 인프라 충족.
- **R2·R3·R4 (오너 — 클라이언트당 1세트)**: ①`kg_status`(새 서버 확인) ②질문 1문(교차형 — 예: "리디안솔루션은 어떤 회사야?") + 3D 화면 확인 ③검수 1건(한 클라이언트에서만) ④생성 1건(한 클라이언트에서만). 결과: (기입 대기)

## 최종 판정표 (제4조 — 완주 후 기입)

(기입 대기)
